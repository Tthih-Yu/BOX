# 物料拉动系统 · 修复与发布记录

> 本文两个用途：一是记录**标准的重新编译与重启步骤**（第一部分，固定流程，照做即可）；二是按时间倒序累积**每次改动/修 bug 的记录**（第二部分，新的写在最前面）。
> 配套阅读：`项目文档.md`（系统全貌）、`80端口访问技术说明.md`（Nginx 与网络链路）。

---

## 目录

- [第一部分：重新编译与重启标准流程](#第一部分重新编译与重启标准流程)
  - [一、先搞清楚系统是怎么跑起来的](#一先搞清楚系统是怎么跑起来的)
  - [二、只改了后端 Java](#二只改了后端-java)
  - [三、只改了前端](#三只改了前端)
  - [四、改了 Nginx 配置](#四改了-nginx-配置)
  - [五、改了数据库结构](#五改了数据库结构)
  - [六、发布后自检清单](#六发布后自检清单)
  - [七、出问题时怎么回滚](#七出问题时怎么回滚)
- [第二部分：修复记录](#第二部分修复记录)

# 第一部分：重新编译与重启标准流程

## 一、先搞清楚系统是怎么跑起来的

本机（Ubuntu）当前的实际运行方式，**不是 Docker**，本机没装 docker：

| 组件 | 运行方式 | 关键路径 |
| --- | --- | --- |
| 后端 | systemd 服务 `material-pull-backend.service`，直接 `java -jar` | `backend/target/material-pull-system-backend-0.8.4.jar` |
| 前端 | 无进程，Nginx 直接读静态产物目录 | `frontend/dist` |
| 入口 | 系统自带 Nginx，监听 80 端口 | `/etc/nginx/sites-available/material-pull.conf` |
| 数据库 | MySQL，后端以 `--spring.profiles.active=mysql` 启动 | — |

由此推出三条很重要的结论：

1. **前端改完只要重新 build 就生效**，不用重启任何服务，因为 Nginx 每次请求都去读 `dist` 目录里的文件。
2. **后端改完必须重新打 jar 再重启服务**。只跑 `mvn compile` 是不够的，服务加载的是 jar，不是 `target/classes`。
3. 服务定义里的 `ExecStartPre` 只在 **jar 文件不存在时**才自动构建：

```
ExecStartPre=/bin/bash -c 'test -f .../material-pull-system-backend-0.8.4.jar || mvn ... package'
```

所以 jar 已经存在时，`systemctl restart` **不会**帮你重新编译，它会拿旧 jar 再启动一次。改了代码只重启不打包，是最容易踩的坑。

查看当前运行状态：

```bash
systemctl status material-pull-backend.service --no-pager
ps aux | grep "[j]ava -jar"
```

## 二、只改了后端 Java

两步：打包，然后重启。

```bash
cd /opt/apps/material-pull/backend
mvn -o clean package -DskipTests -q
sudo systemctl restart material-pull-backend.service
```

说明几个参数：

- `-o` 离线模式，用本地已下载的依赖，内网环境下能避免卡在连仓库上。首次引入新依赖时要去掉 `-o`。
- `-DskipTests` 跳过测试，加快发布。**改了业务逻辑时建议先单独跑一次** `mvn -o test` 再打包。
- 打包耗时约 1 分钟，jar 约 93 MB。

确认 jar 真的是新的（时间戳应该晚于你改代码的时间）：

```bash
ls -la /opt/apps/material-pull/backend/target/*.jar
```

重启后后端需要十几秒才能真正接上，用健康检查确认，不要只看 `systemctl status` 显示 active：

```bash
sleep 15 && curl -s http://127.0.0.1:8080/api/actuator/health
```

期望返回 `{"status":"UP"...}`。启动失败或报错看日志：

```bash
tail -n 100 /opt/apps/material-pull/logs/backend.log
```

注意重启期间接口会中断十几秒，正在扫码或提交任务的现场设备会收到失败。**尽量避开生产高峰重启。**

## 三、只改了前端

```bash
cd /opt/apps/material-pull/frontend
npm run build
```

`npm run build` 会先跑 `vue-tsc` 做类型检查，类型报错会直接中断构建，不会产出半成品。构建完就已经生效了，**不需要重启 Nginx，也不需要重启后端**。

验证产物是新的：

```bash
stat -c '%y %n' /opt/apps/material-pull/frontend/dist/index.html
```

如果浏览器上看不到改动，几乎都是浏览器缓存。Nginx 给 js/css 配了 7 天 `immutable` 缓存，但文件名带内容 hash，正常构建后文件名会变，不该出现缓存问题。真遇到就强刷（Ctrl+Shift+R）。

## 四、改了 Nginx 配置

**这里有个坑，务必先读。**

线上 `/etc/nginx/sites-available/material-pull.conf` 与仓库里的 `deploy/nginx/material-pull.conf` **内容已经不一致**：线上多了一段 `/zebra-print/` 斑马标签打印中心的反代路由（转发到 `127.0.0.1:8091`），仓库版本里没有。

所以**绝对不要**拿仓库版本直接覆盖线上配置，那会让打印中心 404。正确做法是先 diff，确认差异后只改要改的那部分：

```bash
diff <(sed 's/\r$//' /etc/nginx/sites-available/material-pull.conf) \
     /opt/apps/material-pull/deploy/nginx/material-pull.conf
```

（线上文件是 CRLF 换行，仓库是 LF，`sed` 那段是消除换行差异带来的整文件误报。）

应用配置的标准流程，三步都别省：

```bash
sudo cp /etc/nginx/sites-available/material-pull.conf{,.bak}
sudo install -m 644 <你准备好的配置> /etc/nginx/sites-available/material-pull.conf
sudo nginx -t && sudo systemctl reload nginx
```

- 第一步备份，出问题能立刻还原。
- `nginx -t` 先做语法检查，`&&` 保证检查不过就不 reload，避免把线上打挂。
- 用 `reload` 而不是 `restart`：reload 是平滑加载，不断开现有连接。

顺带记一下 `location` 匹配规则，改路由时容易搞错：nginx 前缀匹配取**最长前缀**，与写的先后顺序无关；但正则 `location ~*` 优先级高于普通前缀匹配，用 `^~` 可以让前缀匹配抢在正则之前。所以 `/tools/` 和 `/zebra-print/` 都写成 `^~`，避免它们下面的 js/css 被静态资源那条正则规则接管。

## 五、改了数据库结构

SQL 变更脚本放在 `backend/src/main/resources/db/mysql/`，按版本号命名（如 `V0.8.6__task_print_status.sql`）。

项目**没有接 Flyway/Liquibase**，这些脚本不会自动执行，需要手工在 MySQL 里跑。生产 profile 的 `ddl-auto` 是 `validate`，只校验不改表——表结构和实体对不上时后端会**直接启动失败**，这是有意的保护。

所以顺序必须是：**先执行 SQL，再重启后端**。反过来后端会起不来。

执行前务必先备份：

```bash
bash /opt/apps/material-pull/deploy/scripts/db-backup.sh
```

## 六、发布后自检清单

```bash
# 1. 后端进程活着，且启动时间是刚刚
systemctl status material-pull-backend.service --no-pager | head -5

# 2. 后端健康检查为 UP
curl -s http://127.0.0.1:8080/api/actuator/health

# 3. 80 端口入口能出页面（应返回 200）
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1/

# 4. 通过 Nginx 也能打到后端
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1/api/actuator/health

# 5. 日志里没有新的异常
tail -n 50 /opt/apps/material-pull/logs/backend.log | grep -i "error\|exception" || echo "无异常"
```

最后在浏览器上实际点一遍这次改动到的功能。接口通不代表业务对。

## 七、出问题时怎么回滚

后端回滚：如果打包前留了上一版 jar，直接换回去重启即可。**建议养成打包前先备份的习惯**：

```bash
cp target/material-pull-system-backend-0.8.4.jar ~/jar-backup/$(date +%Y%m%d-%H%M).jar
```

Nginx 回滚：

```bash
sudo cp /etc/nginx/sites-available/material-pull.conf.bak /etc/nginx/sites-available/material-pull.conf
sudo nginx -t && sudo systemctl reload nginx
```

数据库回滚：用 `deploy/scripts/db-restore.sh` 配合备份文件。这一步影响最大，动手前先确认备份文件是对的。

---

# 第二部分：修复记录

## 2026-09-01 14:00 · 生产稳定性与范围逻辑调整

- 生产后端已稳定运行 DataScope `0.9.2`，健康检查 `/api/health/ready` 返回 `success=true`、`status=UP`。
- 生产环境文件已备份，并设置 `MATERIAL_PULL_MYSQL_DDL_AUTO=validate`，避免 Hibernate 在启动时自动修改生产表结构。
- 重启期间旧进程因 Redis/依赖销毁阶段超时被 systemd 强制结束；新进程随后正常启动，当前服务无持续性故障。
- DataScope 范围逻辑调整：账号绑定工厂但配送区域为空时，视为该工厂全部配送区域；Task 列表、待打印任务和 PrintJob 分页均支持该规则。
- 增加三山工厂范围字典脚本 `deploy/scripts/datascope-add-sanshan-scope.sql`，包含 `SS-LP`、`SS-M32T FL3-IP`、`SS-LP-自动盲栓区`；不修改业务历史数据。
- 用户管理界面已明确提示“配送区域（留空=全工厂）”。已有账号需退出后重新登录以刷新 Session 范围。
- 现场仍需继续观察历史打印任务、未打印任务、三山全区域账号、跨工厂访问和 PAD/微信扫码等潜在问题。
- 用户管理范围选择改造：`/users/scope-options` 合并工厂字典与料号映射中的实际配送区域；前端配送区域改为独立弹窗多选，并支持输入手动新增区域值。

## 2026-09-01 14:30 · 前端入口 403 修复

- 前端重新构建后发现 `frontend/dist` 由 `nobody:nogroup` 创建，文件权限为 `660`，Nginx 无法读取 `index.html`，导致 `/` 返回 403。
- 已确认根因不是后端或权限接口；处理方式为将 `dist` 目录统一设为 `755`、静态文件设为 `644`，随后平滑 reload Nginx。
- 后端健康检查保持 `status=UP`，未修改数据库和业务数据。

## 2026-09-01 · 恢复仓库员出货标签区域筛选

- `FactoryPrint.vue` 恢复仓库员账号顶部的工厂/配送区域筛选器。
- 保留原有红蓝（红白高亮）闪烁提示：有待打印任务的区域按钮持续闪烁；筛选仍受后端 DataScope 范围限制，不会扩大权限。
- ADMIN、SYSTEM 和 WAREHOUSE 均可看到筛选器；仓库账号只能切换自己被授权且当前有数据的范围。
- 代码已通过前端类型检查；生产静态文件重新构建前需先修正 `frontend/dist` 权限。

## 2026-09-01 · 用户区域与物料用量看板细节优化

- 用户配送区域显示统一将 `EOVA/EOY` 展示为 `E0VA/E0Y`，底层值保持兼容既有数据。
- 用户区域多选按钮优化为紧凑摘要，完整区域在独立弹窗中查看和选择，避免表格外部文字溢出。
- 物料用量看板“配送区域分布”饼图改为直接显示“区域：数量”标签，不再依赖鼠标悬停。
- 移除“取消分析”图表，替换为“工作量指标”，展示未完成任务的状态数量和总工作量。
- 根据后续要求移除“工作量指标”卡片及甘特图，改为“每日任务量趋势”：按日期统计有效任务数，柱顶常驻显示数量并叠加趋势线；空日期显示 0。

## 2026-09-01 · DataScope 改造与生产发布准备

- 完成账号范围基础设施：用户工厂字段、工厂/配送区域字典、用户区域关系、版本号和审计表；生产已执行 `V0.9.3～V0.9.10`，共初始化 1 个工厂、17 个区域和 20 个范围索引。
- 完成认证链改造：登录范围写入 Session/Redis，Filter 恢复范围，ThreadLocal 请求结束清理，范围变更使旧 Session 失效；Fail Closed 策略生效。
- 完成业务隔离：Task、扫码、Mapping、PrintJob、Inventory、Box、Label、计划链、Dashboard、日志、WebSocket、AGV 等入口增加服务端范围校验；已分配区域的仓库账号隐藏出货标签的工厂/区域切换器，ADMIN 保留。
- 完成账号方案：保留现有账号不改动；确认新增 `500001～500005` 五个弋江 WAREHOUSE 账号及 17 条区域关系。账号初始化脚本已在隔离环境验证，生产尚未写入。
- 完成密码规则调整：最低长度由 10 位改为 6 位，不再强制大小写、数字和特殊字符组合。
- 完成数据库安全准备：生产只读预检通过；生产 Expand Schema 已完成；最新部署前备份为 `db-backups/material_pull-datascope-preflight-20260901-113050.sql.gz`，校验文件存在，权限 `0600`。
- 完成历史数据只读汇总和逐条审计：Task 847 条区域冲突、1 条无候选；PrintJob 3,610/843/2 三类；Task→Mapping 覆盖率 99.84%，PrintJob→Task 覆盖率 99.96%。业务决定历史范围保持 `NULL`、不回填，普通范围账号不可见，ADMIN 可见。
- 完成构建和隔离验收：后端离线 Maven 构建 126 项测试通过；前端构建通过；候选 JAR `material-pull-system-backend-0.9.2.jar` SHA-256 为 `3278a93e611d624590a69ceb2c998d02482650695726e5be33cb01c2b0cf5396`；隔离环境完成登录、跨区域 403、Session 失效、Redis/WebSocket、多实例和回滚恢复验证。
- 完成旧版部署信息留档：现网 `material-pull-backend.service` 仍运行 `0.8.4`；已从 Git 提交 `134bea2` 独立重建并恢复旧版 JAR，SHA-256 为 `99998bc549600292eaa9dbc86abce2b1e5dbc81b5e68bad9f0c2d0ab01d20dd4`，详情记录于 `db-backups/material-pull-old-version-20260901.txt`。
- 发布决定：用户确认本人承担开发、测试、运维和部署审批，允许进入受控生产应用替换窗口；真实 Android/微信扫码回归按决定跳过。切换顺序为旧版 JAR 备份、停止旧服务、启用 `0.9.2`、健康检查、权限冒烟，异常立即回滚。
- 切换前详细检查：旧版 `0.8.4` 与新版 `0.9.2` JAR 均存在且 Manifest 版本正确；两者 SHA-256 已记录；候选 systemd unit 静态校验通过；Java 17、Nginx 和前端产物存在；所有 DataScope 脚本 `sh -n` 通过；最新备份与 Migration 回执均保留为 `0600`。当前执行环境无法访问 systemd DBus 和 root-only 环境文件，未执行重启或生产读写。
- 新版 JAR 内容复核确认包含 `DataScopeService`、`AuthTokenFilter`、`RequestContext`、`ScopePrincipal`、用户范围审计和改造后的 `TaskService`，确实为权限分离版本。旧账号数据库记录未改动；`admin` 保持全局，其他暂空范围旧账号可登录但访问范围数据时 Fail Closed。
- 2026-09-01 12:40 已完成生产应用切换：备份旧 unit 后安装 DataScope `0.9.2` unit，执行 `daemon-reload` 和后端重启；systemd 状态为 `active (running)`，启动安全校验通过，`/api/health/ready` 返回 `success=true`、`status=UP`。新增账号未写入，历史数据未回填。
- 2026-09-01 · DataScope 紧急整套回退：因旧后端与新版前端混用导致 `/api/users/scope-options`、`/api/scan/preview` 等请求 500，已从 Git 提交 `134bea2` 重建旧前端并恢复 `frontend/dist`；新版前端备份保留在 `frontend/dist-datascope-0.9.2-backup-20260901-rollback`。后端应保持 `0.8.4` unit，数据库 Expand Schema 保留不删除；回退只恢复应用版本，不执行数据回滚。
- 2026-09-01 · 料号映射用途修复：备份后按同工厂、同物料、同工位/地址的双记录规则，将排序第一条设为 `NORMAL`、第二条保留 `URGENT`，事务更新 920 条；修复后启用映射为 `NORMAL 929`、`URGENT 1023`。
- 2026-09-01 · 历史打印任务可见性修复：范围账号查询 PrintJob 时，历史 PrintJob 自身范围为空的记录通过关联 Task 的工厂/配送区域判定；已有完整快照仍按自身范围校验。代码已重新编译，待重启服务后生效。

## 2026-08-24 - 28 · 微信小程序扫码功能开发与联调

- 功能位置：`androidAPP/WX_USE`，为物料拉动系统微信小程序扫码端，独立于 `Android_USE` 通用 Android APP 和 `M71_USE` 硬件扫码枪工程。
- 扫码页面：`pages/scanner/` 使用微信摄像头 `scanCode` 模式，扫码成功后通过页面事件回传首页，避免重复扫码并提供取消、失败和重新扫码状态。
- 业务流程：首页扫码后先请求 `/scan/preview` 解析物料和包装信息；用户确认数量后再请求 `/scan/empty`（或设置页配置的业务动作路径）提交申请，保留原始扫码内容、设备号、设备型号、员工号和扫码时间。
- 网络配置：`utils/settings.js`、`utils/api.js` 支持内网直连和外网中转地址切换，统一处理 API 基地址、Token、超时、HTTP 错误和登录信息；默认提交路径为 `/scan/empty`。
- 兼容性：沿用现有扫码预览/提交接口和请求字段，不要求后端协议或现场设备升级；后端上线 DataScope 后，最终工厂与配送区域仍由服务端账号范围解析和校验，前端字段不能越权改写。
- 验证结果：完成小程序扫码页面、预览、数量确认、提交、失败重试和网络模式配置联调；本次生产发布不修改小程序代码，真实客户端回归按业务决定暂缓。

## 2026-08-21 · 通用安卓 APP 手机后置摄像头扫码验证完成（已验证）

用户已在 Windows 开发电脑手动同步并编译 `androidAPP/Android_USE` 的以下文件：

- `app/src/main/java/com/wanshijie/aptiv/MainActivity.kt`
- `app/src/main/java/com/wanshijie/aptiv/data/AppSettings.kt`

本次验证结果：手机可以正常打开后置摄像头预览框并完成条码扫码，扫码结果继续进入原有物料预览、数量确认及内网/外网提交流程。通用手机 APP 不再唤醒 M71 硬件扫码头；`androidAPP/M71_USE` 保持原扫码枪实现不变。

此前因 ZXing 版本差异出现的 `CameraSettings`、`requestedCameraId`、`isAutoFocusEnabled` 以及设备登记状态变量编译错误已在源码中修正。当前通用 APP 版本为 `1.6.0 (8)`，扫码依赖为 `zxing-android-embedded:4.3.0`。

## 2026-08-21 · 出货标签预览卡顿优化（已上线）

出货标签页此前每次刷新会一次读取最多1000条任务、1000条打印记录和全部料号映射；预览及批量打印还会按标签重复请求条码SVG。数据量增长后，这些请求、JSON解析和页面计算会偶发阻塞预览。

- 页面默认每30秒自动刷新，仍允许人工关闭或调整间隔；
- 待打印任务和打印记录改为服务端分页，每页固定20条，后端同时强制单次最多20条；其余页只在用户点击页码时加载；
- 打印记录的状态和打印方式过滤下沉到数据库分页查询，分页总数与筛选结果一致；
- 预览弹窗打开时清除自动刷新定时器，关闭后恢复，避免预览期间刷新列表和争抢网络/渲染资源；
- 条码SVG按仓库代号缓存，并对同时发生的相同请求做合并；预览、批量打印和历史补打共享缓存，最多保留200个仓库代号；
- 出货打印页不再额外加载整张料号映射表，工厂与配送区域直接使用当前任务快照；
- 新增 `GET /api/tasks/printable?page=0&size=20`；`GET /api/print-jobs` 增加兼容式 `page/size/channel` 参数，原无分页调用保持可用；
- 后端离线 Maven 打包成功，前端 `vue-tsc + vite build` 成功且 `frontend/dist` 已更新；
- 后端已于 2026-08-21 16:50:58 使用新JAR完成重启，`material-pull-backend.service` 为 `active/running`，`/api/health/ready` 返回 HTTP 200/UP，本次分页与缓存优化已正式上线。

## 2026-08-21 · 料号映射单条删除后仍显示（已上线）

根因是单条 `DELETE /api/mappings/{id}` 只将记录设置为 `enabled=false`，而映射列表仍查询全部记录，因此刷新后该行继续显示；批量删除则是物理删除，接口语义不一致。生产数据中确认存在3条由该缺陷产生的停用映射。

- 单条删除改为真正物理删除，并在事务内立即 `flush`，与批量删除保持一致；
- 历史补货任务保存的是映射字段快照，数据库也没有外键引用 `m_material_mapping`，删除映射不会破坏历史任务；
- 通用分页表格删除当前页最后一条时自动退回上一页，避免出现空白页；
- 后端离线 Maven 编译成功，前端 `vue-tsc + vite build` 成功并更新 `frontend/dist`；
- 修复发布后，原有3条停用记录可在页面再次点击删除完成物理清理。
- 后端已于 2026-08-21 16:38:14 完成重启；服务 `active/running`、`NRestarts=0`，8080正常监听，`/api/health/ready` 返回 HTTP 200/UP。

## 2026-08-21 · 打印标签“送货人工号”统一替换为“配送区域”（已上线）

系统生成、渲染和打印的标签不再展示或下发送货人工号，原标签底部第三栏统一改为“配送区域”，取补货任务或工厂标签中的 `deliveryArea`。

- 后端打印任务 JSON 删除 `delivererEmployeeNo`，新增 `deliveryArea`；
- ZPL 标签底部第三栏由“送料人工号”改为“配送区域”，值取任务配送区域；
- 浏览器标签、出货打印缩略图、标签中心列表/表单/预览全部改为配送区域；
- 标签中心打印 payload 增加 `deliveryArea`，工厂标签建档使用已有 `areaCode` 列持久化该值，不新增数据库字段；
- 工厂标签 CSV 导入模板和导入字段由 `delivererEmployeeNo` 改为 `deliveryArea`；
- 后端离线 Maven 构建成功，前端 `vue-tsc + vite build` 成功，`frontend/dist` 已更新；
- 新后端 JAR 已生成并于 2026-08-21 16:29:26 完成服务重启；`material-pull-backend.service` 为 `active/running`、`NRestarts=0`，`/api/health/ready` 与 `/api/actuator/health` 均返回 HTTP 200/UP。重启前的旧 JAR 备份位于 `/tmp/material-pull-label-delivery-area-backup-20260821/`。

## 2026-08-21 · 外网安卓扫码轻量中继第一阶段（链路已贯通）

### 背景

阿里云服务器已重置为 Ubuntu 24.04，机器只有约 1.6 GiB 内存，不适合运行完整 material-pull、MySQL、Redis 或大量业务任务。公司内网也不能开放公网入站端口，因此采用“外网 APP → 阿里云轻量指令箱 ← WinServer ← 公司 Linux Worker”的主动轮询架构，所有扫码业务仍由内网 material-pull 执行。

### 本次改动

- 将临时目录 `network-tsst` 规范重命名为 `external-scan-relay`，整理为 `cloud-api/`、`win-relay/`、`linux-client/`、`docs/`。
- 云端 Python API 新增 APP 设备登记、独立设备 Token、指令提交、状态查询和最近记录接口。
- 新增 Worker 原子领取、10～15 秒长轮询、30 秒租约、续租、成功/失败回传、最多 3 次尝试和过期处理。
- SQLite 启用 WAL、`busy_timeout`、外键和队列/设备/租约索引；阿里云只存短期指令与结果。
- 云端 `requestId` 作为全链路幂等键，后续 Worker 会把它传给 material-pull 的 `X-Idempotency-Key`。
- 保留旧 `/api/data`、`/api/tasks`、`/api/tasks/{id}/result`，避免当前 WinServer 旧 EXE 立即失效。
- Nginx 新增 APP 登记限流、APP 请求限流、64 KiB 指令限制、256 KiB Worker 结果限制和固定路径反代。
- 新增环境变量模板、云端部署说明和幂等 `deploy-cloud.sh`，部署时自动生成并保护真实 Token、安装 systemd/Nginx 并执行双层健康检查；APP登记发放码、APP设备凭证、WinServer API Token、内网 Device Key 相互独立。

### 验证情况

- `python3 -m py_compile` 通过。
- 云端 API 4 组自动化测试全部通过：设备鉴权、完整指令生命周期、幂等冲突、错误租约拒绝、可重试失败重新入队、旧任务接口兼容。
- 公司 Linux Worker 已完成，4 组模拟中继/内网接口集成测试全部通过：成功执行、幂等头传递、503可重试、非法操作永久失败和空队列。
- WinServer C# 源码已增加 Worker 固定路由、查询参数和 `X-Worker-Id` 透传、路径参数校验；公司 Linux 无C#工具链，待在WinServer编译新版EXE。
- 阿里云已安装 Python、Nginx、SQLite、Certbot，`tthih.top` HTTPS 证书已签发，有效期至 2026-11-19；自动续期timer已启用。
- 云端服务已部署：API仅监听 `127.0.0.1:18082`，公网只开放Nginx 80/443；HTTP返回301，未知路径404，未授权APP/Worker接口401。
- SSH主机指纹已经人工与服务器本机 ED25519 指纹核对一致。

### 当前状态与下一步

云端代码已部署到 `tthih.top`，本机与公网健康检查均通过。WinServer 已换用支持 `/aliyun-relay/worker/commands/*` 固定路由的新版 `AliyunApiRelay.exe`，并完成 ApiToken 对齐。公司 Linux 已安装、启用并启动 `external-scan-worker.service`，服务无重启记录。2026-08-21 使用只读扫码值 `13799816,物料架-01-F03,备用` 完成真实端到端验收，阿里云指令最终为 `SUCCEEDED`，内网返回仓库 `116175`、库位 `G-27-B-3` 和工位 `物料架-01-F03`。下一步改造安卓 APP 的设备登记、外网提交和结果轮询界面。

### 安卓 APP 外网模式迁移

- 将 `androidAPP/M71_USE` 中可复用的M71扫码广播、Compose界面、内网API和更新功能迁移到空目录 `androidAPP/Android_USE`，排除IDE缓存、本机配置和旧APK；
- 定位“测试连接404”为旧APP强制追加 `/api` 后请求 `/api/health/ready`，而阿里云仅提供 `/health`；
- 新增公司内网/手机外网双模式。外网模式使用 `https://tthih.top/health`、`/app/v1/device/register` 和 `/app/v1/commands`；
- 新增首次设备登记、设备Token持久化、Token与设备编号绑定、扫码指令提交及最长60秒结果轮询；
- 外网预览和正式提交继续复用原有扫码确认界面，业务仍由公司内网 `/api/scan/*` 执行；
- APP版本提升为 `1.5.0 (7)`，已由用户在另一台安装有Android SDK的电脑上完成编译；
- `androidAPP/M71_USE` 作为M71实体扫码枪专用工程继续独立保留；`androidAPP/Android_USE` 用于通用Android手机和外网扫码。仅清理 `Android_USE` 的重复源码压缩包、默认示例测试及未使用依赖。

## 2026-08-19 · 料号映射全量搜索、分页提速与导入模板更新（待重启确认）

### 现象与根因

- 料号 `36127210` 在导出的 Excel 中存在，但在“料号映射”页面输入关键字无法找到。
- 数据库核查确认当前共有 1846 条映射；`36127210` 有两条启用记录（仓库代号 `140437`、`140438`），按页面排序位于第 1799 条。
- 原列表接口只返回按物料号排序后的前 1000 条，而导出接口返回全部记录，因此第 1000 条之后的数据只能导出、不能在页面中搜索。
- 临时改为一次性返回全部数据后虽然能够搜索，但浏览器需要加载和渲染全部 1846 条记录，页面打开速度明显下降。

### 改动

- 料号映射列表改为后端分页，默认每页 50 条，支持切换 20、50、100、200 条，不再首次加载全部记录。
- 关键字过滤改为数据库搜索并增加 300 毫秒防抖，支持按物料号、仓库代号、仓库料号和总装地址查询；搜索范围覆盖全部数据，不受当前页限制。
- 页面增加总数、页码、每页条数和跳页控件；新增、编辑、删除及导入后的刷新继续使用分页数据源。
- 未携带分页参数的旧接口调用仍返回完整列表，保留配送区域下拉等现有内部调用的兼容性。
- 更新 `frontend/public/import_templates/mappings.csv` 及文档副本：模板表头与后端 13 个规范导入字段对齐，补充“仓库料号”列，并完善“工厂”示例值。
- Excel 导出继续导出全部料号映射，不受列表分页影响。

### 验证与发布

- 生产数据库已确认 `36127210` 两条记录均存在且启用。
- 后端执行 `mvn -o clean package` 成功，生成新的 `material-pull-system-backend-0.8.4.jar`；项目当前无测试源码。
- 前端执行 `npm run build` 成功，`vue-tsc` 与 Vite 构建通过，静态产物已更新至 `frontend/dist`。
- `git diff --check` 通过；无数据库结构变化，无需执行迁移脚本。
- 后端分页版本需执行 `sudo systemctl restart material-pull-backend.service` 后生效；重启后应确认服务为 `active (running)`，并强制刷新浏览器验证分页和 `36127210` 搜索。

---

## 2026-08-13 · 工位用料重做为物料用量看板，任务改为逻辑取消（已上线）

### 统计口径

- 现场扫码成功并生成任务即计入“扫码申请”，按任务 `createdAt`（北京时间）归属日期；重复扫码成功生成多条任务时全部累计。
- 除 `CANCELLED` 外的所有任务状态均计入有效申请；取消任务保留在申请量和取消量中，但不计有效用量。
- 不跨物料直接合计数量。顶部展示扫码申请次数、有效申请次数、涉及物料种数和取消次数；数量只在同一物料、同一单位内累计。
- 料号映射是物料全集，未扫码物料也显示。数量和归属使用任务历史快照，页面同时展示当前映射；工厂、工位、仓储地址、总装地址或配送区域变化时标记“映射已变更”。
- 已经物理删除的旧任务无法恢复，历史统计以数据库当前保留的最早任务为起点。

### 页面改动

- 原“工位用料”菜单和 `StationMaterials.vue` 整体重做为“物料用量看板”；原 `/station-materials` 基础数据接口和实体保留，扫码、标签与计划业务继续使用，不受页面替换影响。
- 默认最近 30 天，支持今天、近 7 天、近 30 天、本月、上月和自定义日期，日期边界按北京时间包含起止日全天。
- 日期、工厂、物料、工位、配送区域和任务状态六类筛选全局联动，支持多选和一键重置。
- 图表包括申请/有效/取消趋势、活跃物料排行（次数/数量切换）、工厂分布、工位排行、配送区域分布和取消分析。
- 明细表以料号映射全集展示当前属性、扫码/有效/取消次数、同单位有效用量、最近扫码时间和映射变化标记。

### 后端与历史保护

- 新增只读接口 `GET /api/material-usage-dashboard?from=yyyy-MM-dd&to=yyyy-MM-dd`，一次返回日期范围内任务快照和料号映射全集。
- 原 `DELETE /api/tasks/{taskNo}` 路径保持兼容，但内部不再物理删除：释放库存锁定、取消打印与 AGV 后，将任务状态置为 `CANCELLED` 并保留历史；已完成任务禁止取消。
- 前端保留兼容回退：若新统计接口暂不可用，则读取现有 `/tasks` 与 `/mappings`，但兼容模式最多覆盖任务接口最近 1000 条。

### 验证与发布

- 前端 `npm run build` 通过，`vue-tsc` 与 Vite 构建成功。
- 后端源码在临时目录执行 `mvn -o test` 编译成功（项目无测试源码）。
- 正式 jar 于 2026-08-13 16:00:31 构建，后端服务于 16:00:44 重启并保持 `active (running)`。
- 无数据库结构变化，无迁移脚本。

---

## 2026-08-13 · 网页端“现场扫码”安全隐藏（已上线）

- 当前系统暂不需要浏览器内的“现场扫码工作台”，因此移除 `App.vue` 菜单项、`permissions.ts` 路由权限元数据和 `router.ts` 的 `/scan` 路由。
- `SiteScan.vue` 源文件保留，方便未来恢复；未删除或修改后端 `/scan/*` 接口、安卓 APP 扫码、扫码生成任务及相关业务服务。
- 直接访问网页 `/scan` 不再加载扫码工作台；安卓终端扫码链路不受影响。
- 前端生产构建通过并更新 `frontend/dist`，无需重启 Nginx 或后端。

---

## 2026-08-13 · 浏览器原生打印与历史标签补打（已上线）

### 现象与需求

- “浏览器打印”和“打印本类别”虽然调用了 `window.print()`，但打印内容先在固定尺寸的子窗口中打开；部分现场浏览器或高缩放屏幕下，原生打印预览的操作区域显示不完整，用户看不到可点击的“打印/取消”按钮。
- 标签可能因缺纸、卡纸、误操作或首次打印未实际出纸而需要补救；任务一旦离开待打印列表，原页面没有单张补打入口。

### 根因

- 旧实现使用 `window.open(..., 'width=760,height=...')` 创建受限网页窗口，再从父窗口调用子窗口的 `print()`。原生打印预览受到小窗口尺寸和系统显示缩放影响，容易把浏览器自身的操作区挤出可视范围。
- 打印记录此前仅用于查询状态，没有复用作业 `payload` 中已经保存的标签历史快照。

### 改动

- “浏览器打印”“打印本类别”以及复用批量打印方法的入口统一改为：
  1. 在当前页面创建隐藏 iframe；
  2. 将单张或批量标签 HTML 写入 iframe；
  3. 等待文档与字体就绪后调用 `iframe.contentWindow.print()`；
  4. 打印对话框结束后自动移除 iframe。
- 不再创建固定尺寸网页弹窗，点击后直接显示浏览器完整的原生打印对话框，打印机选择、“打印”和“取消”均由浏览器提供。
- “打印记录”表最右侧新增固定“补打标签”按钮：
  - 每次只补打一张；
  - 优先解析该历史打印作业保存的 `payload`，还原当时的条码、物料、仓储地址、工位地址、数量、盒子大小及配送区域，避免任务后续变化导致补打内容漂移；
  - 重新请求真实 CODE_128 条码并直接调起浏览器原生打印；
  - 补打后新增一条 `BROWSER` 打印记录，保留操作追踪；
  - 已完成任务保持终态，不会因补打重复推进业务状态。
- 仅修改 `frontend/src/views/FactoryPrint.vue`，未新增接口、数据库字段或迁移脚本。

### 验证与发布

- `npm run build` 通过：`vue-tsc` 与 Vite 生产构建均成功。
- `git diff --check -- frontend/src/views/FactoryPrint.vue` 通过。
- 本机 Nginx 直接读取 `frontend/dist`，生产构建完成后前端静态资源已更新，无需重启 Nginx 或后端。
- 浏览器原生 API 无法可靠区分“真正出纸”和“用户在对话框中取消”；系统沿用现有口径，把成功调起的浏览器打印记为浏览器打印记录。现场未出纸时使用本次新增的“补打标签”处理。

---

## 2026-08-12 · 任务与打印按工厂分类（V0.9.2，已上线）

- 补货任务和打印作业新增可空 `factory` 快照字段；历史空值统一展示为“未维护工厂”，不反向回填。
- 映射扫码、真实标签、盒子及计划建单均从料号映射取得工厂；映射后续修改不改变已有任务。
- 打印作业继承任务工厂，打印代理 payload 和浏览器批次标题携带工厂；AGV 本期未改。
- 料号映射导出表头统一为“工厂”；导入兼容“工厂”与 `factory`，自动去除前后空格，只允许弋江、三山，非法行保留原数据且不影响合法行。
- 仓库任务与出货打印页面固定显示“弋江、三山、未维护工厂”，支持独立折叠、浏览器记忆及筛选命中自动展开。
- 出货打印禁止跨工厂勾选和批量打印，同工厂可跨细分组；组内新增仓储地址自然升降序。
- 数据库脚本：`backend/src/main/resources/db/mysql/V0.9.2__task_print_factory.sql`。
- 验证：`npm run build` 成功；`mvn -o clean package` 成功（项目无测试源码，Maven 报告 No tests to run）；`git diff --check` 成功。
- 生产部署（2026-08-12 17:30 起）：
  - 备份：`mysqldump --no-tablespaces` + `pipefail` 生成 `/opt/apps/material-pull/db-backups/material_pull-20260812-173016.sql.gz`，`gzip -t` 校验通过。
  - 迁移：升级前 `t_replenishment_task.factory`、`t_print_job.factory` 两字段均不存在（状态一致），执行 `V0.9.2__task_print_factory.sql` 成功。
  - 结构核对：两个字段均允许 NULL；上线后复核实际类型为 `VARCHAR(255)`（迁移脚本声明 `VARCHAR(64)`，当前差异不影响工厂分类功能）；索引 `idx_task_factory_created`、`idx_print_factory_created` 均为 `(factory, created_at)`。
  - 重启：`sudo systemctl restart material-pull-backend.service`，服务 `active (running)`（Main PID 791322）。
  - 健康检查：`/api/actuator/health` 返回 `{"status":"UP",...}`。
  - 接口抽查：`/api/tasks`、`/api/print-jobs` 均返回 HTTP 401（未登录），路由与应用正常。
- 当前状态：V0.9.2 已在生产完成。功能无阻塞；保留一项结构记录差异：任务与打印表的 `factory` 实际为 `VARCHAR(255)`，后续如需严格统一长度再另行迁移。未改 AGV 库/代码，未执行其他迁移，未删除数据，未重新构建。

> 新记录写在最前面。每条建议包含：现象、根因、改动、验证、遗留事项。
> 写「根因」时请写清楚**为什么会发生**，而不只是写改了什么，方便以后遇到同类问题快速定位。

---

## 2026-08-12 · 料号映射新增“工厂”字段（双工厂数据隔离基础参数）

### 需求与边界

系统同时供两个工厂使用，料号映射需要记录每条数据所属工厂，为后续处理跨工厂数据冲突提供基础参数。本次只新增并开放维护“工厂”字段：

- 字段由员工在“料号映射”页面手工维护；
- 允许为空，不给历史数据自动填默认工厂；
- 暂不参与扫码匹配、周计划计算、标签打印、权限或数据过滤，避免在业务口径未确定前改变现有流程。

### 改动

- 数据库脚本 `backend/src/main/resources/db/mysql/V0.9.1__mapping_factory.sql`：给 `m_material_mapping` 新增可空字段 `factory VARCHAR(64)`。
- `MaterialMappingEntity` 新增 `factory` 属性，保存时只做去首尾空格与空字符串转 NULL。
- “料号映射”列表、新增和编辑窗口增加“工厂”列。
- 料号映射导入支持表头“工厂 / 所属工厂 / 生产工厂”。
- 料号映射导出增加 `factory` 列。
- 前端与文档目录中的 `mappings.csv` 模板增加“工厂”列，示例数据留空，由员工自行维护。

### 数据库升级与验证

- 升级前已生成并校验有效备份：
  `/opt/apps/material-pull/db-backups/material_pull-20260812-155900.sql.gz`。
- 已执行 V0.9.1 SQL，并通过 `SHOW COLUMNS FROM m_material_mapping LIKE 'factory'` 确认：
  `factory varchar(64) NULL DEFAULT NULL`。
- 前端 `npm run build` 通过，生产静态文件已更新。
- 后端 `mvn clean package -DskipTests` 通过，最新 jar 构建时间为 2026-08-12 15:54:06。
- `git diff --check` 通过。

### 当前发布状态

后端已于 2026-08-12 17:32 随 V0.9.2 发布完成重启，并成功加载最新 jar；健康检查返回 `UP`。料号映射保存与导出新逻辑已正式生效。

---

## 2026-08-11 · 出货标签按配送区域/总装地址分组并支持类别一键打印

### 需求

设计分布在两个工厂，所有待打印出货标签一次混在一起容易送错区域。因此“出货标签打印”页面需要先按业务类别分组，再在某个类别内一次打印全部标签。

### 实现

修改前端 `frontend/src/views/FactoryPrint.vue`：

- 新增分组切换：
  - **按配送区域**：使用任务字段 `deliveryArea`；
  - **按总装地址**：使用任务字段 `deliveryAddress`；缺失时依次回退到 `sendStationAddress`、`stationName`、`stationCode`。
- 每个分类显示标签数量，并提供两个独立操作：
  - **打印本类别**：打开浏览器打印窗口，生成该类别全部真实 CODE_128 条码，并提交浏览器打印记录；
  - **提交本类别**：把该类别全部任务逐条提交到本地打印代理队列，需要先填写打印机名称。
- 原有“全部筛选结果打印/提交”保留，搜索条件仍可与分组组合使用。
- 当前分组只影响待打印任务；已经生成打印作业的记录不会重复出现在待打印分类中。

后端不需要新增接口：任务实体已有 `deliveryArea`（配送区域）和 `deliveryAddress`（总装地址）字段，现有 `GET /api/tasks` 已返回；批量操作复用现有 `POST /api/print-jobs`。

### 验证

- `npm run build` 通过。
- `FactoryPrint.vue` lint 无错误。
- 两个工厂/多个配送区域和总装地址会被分成独立类别，类别内可单独一键打印，避免不同类别混打。

---

## 2026-08-11 · 周计划当天用量改为每天 0 点自动刷新

### 需求与口径

- `m_material_mapping.quantity` 继续按现有设计存「当天用量」，字段拆分问题暂不修改，保留在 `潜在问题清单.md` P0，待现场确认标签应印当天总需求还是一盒容量。
- 原来需要计划员手动选日期点击「刷新当天用量」；现改为每天 0 点自动按当天日期刷新。

### 改动

- 新增 `WeeklyPlanScheduler`，默认按 `Asia/Shanghai` 时区每天 `00:00:00` 调用 `WeeklyPlanCalcService.calculate(today, true)`。
- 自动刷新与前端手动刷新共用同一个计算入口，计算口径、跳过规则和写库行为完全一致。
- 使用现有 `LockProvider`，锁键包含日期；多实例部署时同一天只允许一个实例执行，锁租约 1800 秒。
- 当天无周计划、没有有效 BOM 或所有计划数量为 0 时，仅输出 WARN 并跳过，不影响后端服务；未维护单根用量的映射仍保持原数量不动。
- 新增配置：

```yaml
app.scheduler.weekly-plan-refresh-cron: ${MATERIAL_PULL_WEEKLY_PLAN_REFRESH_CRON:0 0 0 * * *}
app.scheduler.weekly-plan-zone: ${MATERIAL_PULL_WEEKLY_PLAN_ZONE:Asia/Shanghai}
```

如需临时调整执行时间，可用环境变量覆盖；Spring cron 为六段格式「秒 分 时 日 月 周」。

### 验证

- `mvn -o test` 通过，调度器、cron 占位符和依赖注入编译正常。
- 发布后通过健康检查与启动日志确认配置加载；首次真实自动写库将在下一个上海时区 0 点执行。

---

## 2026-08-11 · Android APP 1.4.0：修复正式提交丢失工位/用途，并规范远程发布

### 现象

扫码枪扫出内容明确为：

```text
13609637,物料架-11-E06,备用
```

料号映射中也确实存在 `13609637 + 物料架-11-E06` 的 A/B 仓库代号，但点击确认发送后，后端仍报：

```text
物料号 13609637 对应多个总装地址，本次扫码未识别出工位地址，已拒绝建单
```

### 根因

不是新 jar 未生效。现场核对结果：jar 构建时间 `10:39:04`、服务启动时间 `10:39:39`，运行中的确是新代码。真正的问题在 Android APP 的**预览 → 确认提交**两阶段流程：

1. 第一次预览时，APP 把完整二维码 `13609637,物料架-11-E06,备用` 发给后端。
2. 后端预览响应里的 `scannedCode` 是拆分后的纯物料号 `13609637`。
3. APP 用响应中的 `scannedCode` 覆盖了原始扫码值。
4. 员工点击“发送”时，APP 正式提交的只剩 `13609637`，工位和“备用”均已丢失。
5. 后端因此只能看到物料号；该物料对应两个总装地址，安全兜底正确拒绝建单。

另发现后端 `LabelResolverService.normalize()` 原先直接删除二维码内部换行，会把：

```text
13609637(备用)
物料架-11-E06
```

粘成无法拆解的一段。

### 改动

Android `MainActivity.kt`：

- `fetchScanPreview()` 构建 `ScanPreview` 时，`scanCode` 始终保留扫码枪的**原始字符串**，不再使用后端预览响应中拆分后的 `scannedCode` 覆盖。
- 正式提交因此仍会发送完整的「物料号、工位、使用/备用」，联合匹配所需信息不会丢失。

后端：

- `ScanService.splitMaterialAndStation()` 兼容逗号、中文逗号、竖线、分号和换行分隔。
- 兼容 `13609637(备用)` / `13609637（备用）` 这种用途写在物料号后的版式。
- `LabelResolverService.normalize()` 将二维码内部换行转为逗号，而不是直接删除；扫码枪尾部换行仍会被边界清理。

### Android 版本与发布

- `androidAPP/app/build.gradle.kts`：`versionCode 5 → 6`，`versionName 1.3.0 → 1.4.0`。
- 按约定由用户自行构建 APK，文件统一命名为 `aptiv-<versionName>.apk`，直接放到：

```text
/opt/apps/material-pull/apk-updates/
```

- `deploy/scripts/publish-android-apk.sh` 已兼容源文件就是目标文件的情况，同路径时跳过复制，避免 `cp` 报“是同一个文件”。
- 部署文档 `新更新/2026/8/6/2.安卓APP远程更新部署说明.md` 已将旧路径 `/home/tthih/IE` 全部更新为 `/opt/apps/material-pull`，并改为上述固定命名与直放目录流程。

### 远程更新版本误报修复

发布时 `latest.json` 曾被误写为 `versionCode=7, versionName=1.4.0`，而 APK 实际为 `versionCode=6, versionName=1.4.0`。APP 按 `versionCode` 判断更新，因此安装 1.4.0 后仍提示发现 1.4.0。

现已恢复一致：

```text
versionCode = 6
versionName = 1.4.0
apkUrl = /updates/android/aptiv-1.4.0.apk
```

以后必须同步递增，例如：`1.4.1 → versionCode 7`、`1.4.2 → versionCode 8`，不能只增加 `versionCode` 而沿用同一个 `versionName`。

### 验证

- 后端修复已编译打包通过；Android APK 按用户要求由用户自行构建。
- `aptiv-1.4.0.apk` 已发布，大小 `12,393,232` 字节。
- `latest.json` 与 APK 下载地址均返回 HTTP 200。
- `latest.json` 当前为 `versionCode=6 / versionName=1.4.0`；手机更新后再次检查已显示正常，不再重复提示同版本更新。

---

## 2026-08-11 · 扫码「物料号 + 总装地址」联合匹配加固（地址归一化 + 报错可操作化）

### 现象

员工用 APP 扫码后出的仓库标签有印错风险：同一个物料可能被多个工位共用，若只按物料号选映射，会取到别的工位的仓库代号，标签和拉料都错。

### 根因

双条件匹配（条件1 物料号、条件2 总装地址）本来就在 `ScanService.chooseMapping()` 里，**但有两个漏洞**：

1. **静默兜底**：工位地址为空时直接跳过条件2，然后 `candidates.get(0)` 任取第一条。对跨工位物料等于随机挑工位。
2. **地址精确比对**：库里 76 条地址含波浪号且全角 `～` 与半角 `~` 混用，`equalsIgnoreCase` 会把「看起来一样」的地址判为不等，误报「条件2未满足」让本该成功的扫码失败。

用真实数据量化后确认了必要性与安全性：

| 核对项 | 实测 |
|---|---|
| 启用物料总数 | 506 |
| **跨多个工位的物料** | **113**（单物料最多对应 6 个地址） |
| 总装地址为空的映射 | 0（数据质量没问题，问题在代码） |
| 扫码记录总数 | 668 |
| **扫码工位地址为空的记录** | **0**（APP 一直都在传工位，所以兜底分支现场走不到） |
| 去重「物料号+工位」组合匹配率 | 163 / 164 |

补充结论：684 个「物料号+总装地址」组合中 598 个对应多条记录（最多 4 条），但那些是 **A/B 双盒**，
总装地址相同、工位不会印错，A/B 选择由既有轮换机制负责。所以「物料号+总装地址唯一确定**工位**」成立。

### 改动

`service/ScanService.java`：

- 新增 `normalizeAddress()`：比较前归一全角/半角波浪号、连字符、括号、空格与大小写。
  **只归一字符形态、不删分隔符**，因此 `物料架-04-D04` 与 `物料架-04-D4` 仍判为不同，避免过度匹配。
- `normalizedEquals()` 改用归一化后比较。
- 条件2 失败时的报错带上该物料**已维护的全部总装地址**清单，现场可直接对照判断是二维码印错还是映射缺记录。
- 兜底分支加保护：工位地址为空且该物料对应多个总装地址时，拒绝建单并列出候选地址，不再任取第一条。
  （现场 APP 一直传工位，此分支实际不触发，仅防止静默出错。）

### 验证

- 地址匹配用例 9/9 通过，含全角/半角波浪号、大小写、首尾空格必须匹配，以及 `手工台-34-A02` vs `手工台-34-B02`、`物料架-04-D04` vs `物料架-04-D4` 必须不匹配。
- 打包重启后核对运行的确实是新 jar：jar 构建 09:46:11、服务启动 09:47:47、`normalizeAddress` 与新报错文案均已在 jar 内、健康检查 UP。

### 遗留事项

发现一条真实数据问题（**未处理**，已记入 `潜在问题清单.md`）：

```
扫码：35452248 @ TH80-OLE1-07-A05-A06
映射里该物料的地址：TH80-OLE-03-A03-A04、手工台-14-A01~A02、手工台-32-A03、手工台-32-B03、手工台-37-A01、手工台-37-B01
```

`OLE1` vs `OLE`、`07` vs `03` 两处都不同，不是字符形态问题。需现场判断是补料号映射记录还是重印二维码。

---

## 2026-08-11 · 周计划联动改为「按天」刷新料号映射数量（修正放大 7 倍的错误）

### 现象

上一版把**整周总量**写进了料号映射的数量列。而现场口径是：`quantity` 应为**当天**用量，每天随当天计划动态变化。按原实现，数量会被放大约 7 倍。

### 根因

我按指导书「周计划 → 覆盖料号映射数量」直接理解成了「一周总量」，没有核实现场对这个字段的期望粒度是「天」。
班次数量本来就是按「日期+班次」行存的，具备按天取数的能力，只是计算层按周汇总了。

### 改动

后端：

- `repository/WeeklyPlanShiftQtyRepository.java`：新增 `sumByProductCodeOnDate()`，按日期取各 8D 号「白班+夜班」合计。
- `service/WeeklyPlanCalcService.java`：入口由 `(planYear, weekNo)` 改为 `(planDate)`；
  - 单根用量为空或 ≤0 视为「员工还没维护」，**跳过不计算**（避免任何数 ×0 把数量刷成 0），计入 `skippedNoUsage`；
  - BOM 缺组件、组件缺映射由「错误」降级为「提示」(`warnings`)，不再整体阻断，可算的照常落库，算不了的**保持原数量不动**；
  - 预览结果加 `oldQuantity`/`warehouseCode`，刷新前能看清每条会从多少变成多少。
- `controller/WeeklyPlanController.java`：`POST /weekly-plan/calculate?planDate=YYYY-MM-DD&apply=`。

前端 `views/WeeklyPlan.vue`：「计算周次」改为日期选择器，按钮改为「刷新当天用量」，弹窗展示跳过条数与原值→新值对比。

### 同批修复：弋江/三山工厂隔离

原冲突检测只按「年份+周次+8D号」，与 SQL 注释和文档声明的业务键不一致。
后果：A 计划员传弋江、B 传三山，只要有同一个 8D 号，B 确认覆盖会把 A 的整行连班次数据删掉。

改为「年份+周次+**工厂**+8D号」：`findConflicts` 加 factory 参数（空值用 `is null` 判定，避免 `= null` 恒假漏检），
`detectConflicts` 去重键带工厂，`supersedeConflicts` 按「工厂+8D号」成对删除。两厂互不冲突、互不覆盖。

### 验证

- 后端编译打包通过、前端构建通过、无 lint 错误。
- 改造后重跑解析往返测试仍正确：14 班×10 = 140、7 个白班×5 = 35，8D 号未被科学计数法破坏；
  真日期单元格、合计行+签字区两个边界场景照旧通过。
- **未验证**：计算引擎实际写库这一段。1284 条映射 `single_unit_usage` 全为 NULL，现在跑会全部走「跳过」分支，一条都不会刷。

### 遗留事项

见 `潜在问题清单.md`：`quantity` 一字两用（既是每盒容量又是当天需求量，会影响出货标签印的数量）、
多映射写入相同全量需求（用户确认按现状用）、每天自动刷新尚需定时任务。

---

## 2026-08-11 · BOM 周计划联动（简单BOM + 周计划导入 + 自动计算覆盖料号映射数量）

### 需求

打通「周计划 → BOM → 料号映射数量」链路：计划员上传一周的生产计划，系统按 BOM 把物料展开到组件，乘以单根用量算出各组件需求量，直接覆盖料号映射的数量，替代人工逐条改。

### 关键设计（为什么这么做）

**为什么周计划分三张表**：计划行按 8D 号存一条，七天白/夜数量单独按「日期+班次」行存（`t_weekly_plan_shift_qty`）。若把 14 个班次做成 14 个列，跨月跨年周、班次增减都要改表结构；行存后这些都不用动表。

**为什么日期要单独给年份**：模板日期只有「月/日」不带年份，无法自证归属年。所以上传时由计划员选年份；跨年周（周次 ≤2 却出现 12 月、周次 ≥50 却出现 1 月）按周次自动往前/后归年，避免 12 月末那一周被算到错误年份。

**为什么计算引擎「有错就整体不落库」**：需求量会直接覆盖料号映射的数量，是拉动的直接依据。若容忍部分失败，会出现「一半组件是新数量、一半是旧数量」的混合状态，现场按这种数据拉料必错且难排查。所以 BOM 缺组件、组件缺映射、单根用量未维护，一律收集成错误清单整体拦下，一条都不写。

**为什么模板要后端动态生成**：模板含合并单元格（标题跨全表、每个日期跨白/夜两列），静态 CSV 表达不了；且日期要按起始日自动算，必须服务端生成 xlsx。

### 改动

数据库（新增 3 张表，`db/mysql/V0.9.0__weekly_plan.sql`）：
- `t_weekly_plan_batch`：上传批次，记录年份/周次/Excel 版本/上传人。
- `t_weekly_plan_row`：计划行（按 8D 号），带库内 `version` 乐观锁字段用于冲突覆盖判定。
- `t_weekly_plan_shift_qty`：班次数量，按「日期+班次(DAY/NIGHT)」行存。

后端：
- `service/WeeklyPlanService.java`：Excel 解析（标题取 WKxx 与版本、两行表头、按日期列定位白/夜、当天=白+夜、跳过合计与签字区、8D 号取数值防科学计数与前导零丢失、跨年周归年）；冲突检测与覆盖；`buildTemplate()` 用 POI 生成模板。
- `service/WeeklyPlanCalcService.java`：计算引擎（周计划按 8D 汇总 → 活跃 BOM 展开到组件 → 累加 → 乘单根用量 → 覆盖 `MaterialMapping.quantity`），支持 `apply=false` 仅预览。
- `controller/WeeklyPlanController.java`：导入、批次列表、明细分页、删除、计算（预览/覆盖）、模板下载。
- `service/SimpleBomService.java`：补单条 `saveRow()` / `deleteRow()`，仅允许维护**当前有效批次**，增删后同步批次行数。

前端：
- 新页 `views/WeeklyPlan.vue`：上传（带年份）、冲突确认弹窗、明细查看、计算预览/覆盖。
- 新页 `views/WeeklyPlanTemplate.vue`：填客户/生产工厂/项目/客户号 + 选起始日，自动算七天日期预填进模板再下载；页面实时预览周次与七天日期。
- `views/SimpleBom.vue`：补单条新增/编辑/删除（原先只有导入与批次删除，缺单条增删改）。
- 路由/权限/菜单：`/weekly-plan`、`/weekly-plan-template` 归入「基础数据」组。

### 验证

- `mvn -o clean package -DskipTests` 通过，jar 正常产出。
- `npm run build` 通过，无 lint 错误。
- **未做**端到端实测（需服务重启后才能跑）：生成模板 → 填数据 → 上传解析 → 计算覆盖 这个闭环尚未用真实数据走通。

### 遗留事项

- 解析规则是按样本布局写的，真实 Excel 若列偏移/合并方式不同需微调。**上线前务必用一份真实周计划试上传**，重点核对：解析出的周次、周数量是否等于七天白+夜之和、合计与签字区是否被正确跳过。
- 起始日未强制必须为周一，选哪天就从哪天连续算七天。
- 多计划员各自独立账号密码（避免互相覆盖）尚未实现，当前靠冲突确认弹窗兜底。
- BOM 单条增删改仅作用于当前有效批次；无有效批次时会提示先导入并启用。

### 部署要点

本次**有新建表**，必须先备份、再执行 `V0.9.0__weekly_plan.sql`、然后才能重启后端（生产 `ddl-auto=validate`，表不存在会启动失败）。

---

## 2026-08-10 · 新增「普通管理员(SUB_ADMIN)」角色（准管理员，权限低于最高管理员）

### 需求

要一个权限低于最高管理员的管理员角色：账号由最高管理员注册；先要求能上传料号映射，随后扩展为「除管理员专属功能外都能用，具体页面由最高管理员在『菜单权限』页勾选控制」的次级管理员。

### 关键设计（为什么这么做）

`RoleInterceptor` 里 `ADMIN` 和 `SYSTEM` 是**全局放行**（命中直接 return true）。所以新角色**绝不能复用 ADMIN**，否则等同最高管理员、且能在用户管理页把自己改成 ADMIN 造成提权。

采用「准管理员」对称模型，前后端判定一致：

- 后端 `RoleInterceptor`：SUB_ADMIN 除**管理员专属接口**（`@RequireRoles` 只写 `{ADMIN}`）外全部放行；新增 `isAdminOnly()` 判定。
- 前端 `auth.ts` 的 `hasAnyRole`：SUB_ADMIN 除「仅 ADMIN/SYSTEM」的功能外全部放行。

提权底线：用户角色(`/users`)、系统参数(`/configs`)、菜单权限配置(`/menu-permissions` 读写)、维护恢复(`/maintenance` 整类) 都是 `{ADMIN}` 专属，SUB_ADMIN 一律进不去。

### 改动

后端：
- `enums/UserRole.java`：新增 `SUB_ADMIN("普通管理员")`。
- `security/RoleInterceptor.java`：SUB_ADMIN 准管理员放行逻辑 + `isAdminOnly()`。
- `controller/MenuPermissionController.java`：`/menu-permissions/mine` 授权加 SUB_ADMIN（否则登录后拉菜单白名单吃 403）。
- `controller/ImportController.java`：SUB_ADMIN 仅限 `mappings` 类型导入（可覆盖上传），不能导入其它类型。
- `controller/BasicDataController.java`、`MetaController.java`：料号映射查询/增删改/导出、元数据接口显式放开 SUB_ADMIN（拦截器已兜底，这里保持注解清晰）。

前端：
- `auth.ts`：`UserRole` 增 `SUB_ADMIN`；`hasAnyRole` 准管理员逻辑。
- `permissions.ts`：`planner` 集并入 SUB_ADMIN（料号映射页写操作全开）；`/mappings` 路由放行。
- `views/Mappings.vue`：导入按钮用含 SUB_ADMIN 的角色集。

### 菜单显示规则

- 最高管理员**未**在「菜单权限」页给普通管理员勾选时：默认可见**全部非管理员页面**（`menusForRole` 返回 null 不收窄 + 前端准管理员放行）。
- 一旦勾选并保存：普通管理员菜单**只显示勾选项**（白名单收窄）；但那 4 个管理员专属页即使勾了也进不去。

### 验证

- 后端 `package` + 重启 `material-pull-backend.service` 后 `{"status":"UP"}`；前端 `build` 通过。
- 普通管理员 sanshan 登录：料号映射页全部按钮可用（导入/覆盖上传/导出/批量删除/新增/编辑/删除）；可进各非管理员页；访问用户角色页被拒。
- `api/menu-permissions/mine` 不再 403。

### 遗留事项

- 控制台 `el-checkbox ... label ... deprecated` 是 Element Plus 废弃提示，无害，暂不处理。

---

## 2026-08-10 · 料号映射新增「单根用量」字段（周计划联动计算用）

### 需求

料号映射增加「单根用量」（一件产品消耗该组件的数量），供后续周计划自动计算「组件需求量 = 计划数量 × 单根用量」。字段允许为空，空表示尚未维护，**不填充任何默认数据**（由后期人员维护）。

### 改动

- 数据库：新增 `backend/src/main/resources/db/mysql/V0.8.8__mapping_single_unit_usage.sql`，给 `m_material_mapping` 加列 `single_unit_usage DECIMAL(18,4) NULL`。
- 实体 `MaterialMappingEntity`：新增可空字段 `singleUnitUsage`，`normalize()` 不强制默认值（保留「未维护」语义）。
- 导入 `ImportService`：mappings schema 增列、表头别名「单根用量」、新增 `numOrNull/parseNumOrNull`（空值返回 null，不落成 0）。
- 导出 `BasicDataController`：`MAPPING_EXPORT_COLUMNS` 增列。
- 前端 `Mappings.vue`：数量列后新增「单根用量」列。
- 模板：`frontend/public/import_templates/mappings.csv` 与 `docs/import_templates/mappings.csv` 增列，数据留空。

### 部署顺序（重要）

生产 `ddl-auto=validate`，必须**先跑 SQL 再重启后端**，否则实体与表结构不一致会导致后端启动失败。执行 SQL 前先 `db-backup.sh` 备份。

### 验证

- `SHOW COLUMNS FROM m_material_mapping LIKE 'single_unit_usage'` 返回一行。
- 后端重启 `{"status":"UP"}`（validate 通过即证明字段与实体对齐）；前端页面出现「单根用量」列。

---

## 2026-08-10 · 文档纠正：后端服务名与项目路径

本文此前记录的后端 systemd 服务名 `material-pull-backend-dev.service` 与项目路径 `/home/tthih/IE` 均已过时，导致重启时报 `Unit ... not found`。已全文更正为实际值：

- 服务名：`material-pull-backend.service`（不带 `-dev`）。
- 项目路径：`/opt/apps/material-pull`。

以 `systemctl list-units --type=service | grep material` 查到的为准。

---

## 2026-08-06 · 登录页新增 APP 下载入口（下载页 + CSP/MIME 两个坑）

### 现象

需求：在 Web 登录界面加一个入口，点进去是一个提供 APK 下载的 HTML 页面，供扫码枪浏览器打开安装。实现过程中踩了两个坑：

1. 下载页最初放在 `apk-updates/download.html`，浏览器打开时直接把 HTML 当文件下载，而不是渲染。
2. 换位置后页面能打开，但脚本不执行，控制台报 CSP 错误：`Executing inline script violates ... script-src 'self'`。

### 根因

1. Nginx 的 `location ^~ /updates/android/` 块里 `default_type application/octet-stream`，该目录只声明了 json/apk 两种 MIME，`.html` 落到默认二进制类型，被浏览器当附件下载。
2. 站点 CSP 头是 `script-src 'self'`，禁止一切内联 `<script>`。下载页把 JS 直接写在 `<script>` 标签里，被 CSP 拦截，导致读取 `latest.json`、渲染按钮的逻辑全部不执行。

### 改动

1. 下载页移到前端静态根目录 `frontend/public/app-download.html`，由主站按标准 `text/html` 提供（Vite 构建时 `public/` 原样拷进 `dist/`）。删除旧的 `apk-updates/download.html`。
2. 内联脚本抽成同源外部文件 `frontend/public/app-download.js`，HTML 里用 `<script src="/app-download.js">` 引用，满足 `script-src 'self'`。
3. 登录页 `frontend/src/views/Login.vue`：版权行上方新增胶囊按钮「下载扫码枪 APP」（`Iphone` 图标），链接指向 `/app-download.html`。
4. 前端重新构建。

下载页逻辑：`fetch('/updates/android/latest.json')` 动态显示版本号、大小、发布时间、更新说明、SHA-256，下载按钮指向当前 APK。发布新版本后页面自动更新，无需改代码。

### 验证

```bash
curl -I http://127.0.0.1/app-download.html   # 200，Content-Type: text/html
curl -I http://127.0.0.1/app-download.js     # 200，Content-Type: application/javascript
curl -o /dev/null -w '%{http_code}\n' http://127.0.0.1/updates/android/download.html  # 404（旧文件已删）
```

浏览器打开 `http://<IP>/app-download.html` 页面正常渲染、无 CSP 报错，下载按钮可点。

### 遗留事项

- 下载页是纯静态页，改动它需要重新 `npm run build`（不像 `apk-updates/` 目录发布 APK 那样即时）。
- 若日后收紧或调整 CSP，注意外部脚本仍需同源。

---

## 2026-08-06 · 安卓 APP 远程更新服务器端（/updates/android/ + 发布脚本）

### 现象

APP 端新增了「检查更新 / 下载 / 安装」功能（Android 侧另行开发，不在本仓库工作区）。APP 会请求 `<服务器>/updates/android/latest.json` 判断版本，再下载 APK。服务器端此前完全没有对应支撑：Nginx 无 `/updates/android/` 路由、无发布脚本、无 APK 存放目录，APP 检查更新会拿到 404。

### 根因

不是 bug，是 APP 新增功能带来的服务器端缺口。另外部署文档《安卓APP远程更新部署说明.md》里的路径写的是 `/opt/apps/material-pull`，但本机项目实际部署在 `/opt/apps/material-pull`（`/opt/apps/material-pull` 不存在，已部署 Nginx 的 `root` 指向 `/opt/apps/material-pull/frontend/dist` 可证实）。若照文档原样部署会指向不存在的目录。

### 改动

1. 新增发布脚本 `deploy/scripts/publish-android-apk.sh`（已 `chmod +x`）：
   - 入参 `<APK路径> <versionCode> <versionName> ["更新说明"]`；
   - 自动复制 APK 到 `apk-updates/`、计算 SHA-256 与文件大小、原子生成 `latest.json`（先写临时文件再 `mv`，避免设备读到半成品）；
   - 基准路径固定为 `/opt/apps/material-pull`，非文档里的 `/opt/apps/material-pull`。
2. 仓库 `deploy/nginx/material-pull.conf` 增加 `location ^~ /updates/android/`，`alias` 到 `/opt/apps/material-pull/apk-updates/`，设置 apk/json MIME 与 `no-cache`。
3. 已部署配置 `/etc/nginx/sites-available/material-pull.conf` 同步插入该块（保留备份，未整份覆盖，因为仓库版与线上版内容不一致）。
4. 创建 `/opt/apps/material-pull/apk-updates/`，放行 www-data 读取权限。
5. 约定：源 APK 先手动放到 `/opt/apps/material-pull/upload/`（中转，位置随意），发布目录 `apk-updates/` 只由脚本管理。

`latest.json` 字段：`versionCode` / `versionName` / `apkUrl`（相对路径 `/updates/android/xxx.apk`）/ `fileName` / `fileSize` / `sha256` / `releaseNotes` / `publishedAt`。

### 验证

首版发布 `versionCode=5`、`versionName=1.3.0`：

```bash
cd /opt/apps/material-pull
./deploy/scripts/publish-android-apk.sh ./upload/app-release_1.3.0.apk 5 1.3.0 "新增APP内检查更新功能"
curl -fsS http://127.0.0.1/updates/android/latest.json   # HTTP 200，字段正确
curl -I   http://127.0.0.1/updates/android/aptiv-1.3.0.apk # HTTP 200，Content-Type: application/vnd.android.package-archive
```

`latest.json` 的 `fileSize` 与 APK 的 `Content-Length`（12393232）一致。

### 遗留事项

- 首版带更新功能的 1.3.0 仍需通过 USB 手动安装一次；此后签名一致且 `versionCode` 递增即可在 APP 内更新。
- APP 端读取的字段名需与脚本产出的对齐（若 APP 用 `downloadUrl` 或需绝对 URL，需改脚本）。用真机点一次「检查更新」确认。
- 当前为 HTTP 内网方案；SHA-256 只防下载损坏，链路完整性最终由 Android 应用签名校验保障。条件允许可升级 HTTPS。

---

## 2026-08-06 · 新版安卓 APP 两阶段扫码适配（扫码预览 + 本次申请数量/单位）

### 现象

新版安卓 APP 改成两阶段扫码：先调 `POST /api/scan/preview` 只查询物料基础数据并显示后台默认数量/单位，员工可改数量和单位后再调 `POST /api/scan/empty` 建单。后端未适配前，APP 扫码会在 `/api/scan/preview` 收到 404，无法进入确认流程。

### 根因

不是 bug，是 APP 侧新增流程带来的后端接口缺口：

1. 后端原本没有 `/scan/preview` 接口，扫码只有「一步建单」的 `/scan/empty`。
2. 补货任务只有 `requestQty`，没有「本次申请单位」字段，APP 允许员工填任意单位（如「托盘」）无处落库。
3. 员工修改的数量/单位必须只写进本次任务，**不能回写**料号映射、标签、盒子、物料主数据这些基础数据，否则会污染后台默认值，导致下次扫码取到被改过的数。

### 改动

**后端 · 新增只读预览接口**

- `ScanController` 新增 `POST /scan/preview`，复用 `ScanService.preview(...)`。现有 `AuthTokenFilter` 已统一放行 `/scan/*`，无需加放行规则。
- `ScanService.preview(...)` 用 `@Transactional(readOnly = true)`，复用既有的扫码解析、标签解析、料号映射选择逻辑，但**不保存标签、不切盒状态、不建任务、不写正式成功日志**。返回默认数量优先取盒子标准数量、退回标签标准数量；默认单位依次取标签单位 → 物料主数据单位 → 「个」。
- 注入 `MaterialRepository`，新增 `findMaterialUnit(...)` 查物料主数据单位。

**后端 · 本次任务数量与单位落库（不碰基础数据）**

- `ScanDtos.ScanRequest` 新增 `requestQty`/`requestUnit`；`ScanResult` 新增 `requestUnit`；新增 `ScanPreviewResult` 预览返回类（补 `import java.math.BigDecimal`）。
- `ReplenishmentTaskEntity` 新增 `requestUnit`（默认「个」），`normalize()` 补空值默认与 trim。
- 三处建单逻辑（`createTaskFromMapping`/`createTaskFromLabel`/`createTask`）改用 `resolveRequestQty(req, 默认值)` 与 `resolveRequestUnit(req)`：未传时用后台默认数量/「个」，单位允许任意文本但限 32 字符，不做换算、不设白名单。这些方法只给新任务赋值，**不调用任何基础数据实体的 set 方法**。
- 幂等摘要 `RequestDigest.sha256(...)` 加入 `requestQty`/`requestUnit`，使同一扫码值的「100 个」与「250 托盘」得到不同摘要，不会被幂等误判为重复。
- 各建单成功/重复返回结果补 `requestUnit`。

**数据库 · 加字段**

- 新增 `backend/src/main/resources/db/mysql/V0.8.7__task_request_unit.sql`：给 `t_replenishment_task` 加 `request_unit VARCHAR(32) NOT NULL DEFAULT '个'`。生产 `ddl-auto=validate`，已按「先执行 SQL 再重启后端」顺序落地。

**前端 · 仓管任务页显示单位**

- `views/WarehouseTasks.vue` 的「需求数量」列由纯数字改为 `数量 + 单位`（`{{ row.requestQty }} {{ row.requestUnit || '个' }}`）。打印标签版式（`browserPrint()` 与后端 ZPL）本次未改。

### 涉及文件

| 文件 | 改动 |
| --- | --- |
| `backend/.../dto/ScanDtos.java` | 加 `BigDecimal` 导入、`ScanRequest` 加数量/单位、`ScanResult` 加单位、新增 `ScanPreviewResult` |
| `backend/.../entity/ReplenishmentTaskEntity.java` | 新增 `requestUnit` 字段与 `normalize()` 默认值 |
| `backend/.../controller/ScanController.java` | 新增 `POST /scan/preview` |
| `backend/.../service/ScanService.java` | 注入 `MaterialRepository`、幂等摘要、三处建单、`resolveRequestQty/Unit`、`findMaterialUnit`、`preview`、返回补单位 |
| `backend/src/main/resources/db/mysql/V0.8.7__task_request_unit.sql` | 新增加字段脚本 |
| `frontend/src/views/WarehouseTasks.vue` | 需求数量列追加单位 |

### 验证情况

已验证：后端 `mvn clean package -DskipTests` BUILD SUCCESS、无 lint 错误；前端 `npm run build`（含 `vue-tsc` 类型检查）通过；MySQL `request_unit` 字段已加并确认（`varchar(32) NOT NULL DEFAULT '个'`）；后端已重启（新 PID），`/api/health/ready` 返回 UP，`POST /api/scan/preview` 空 body 返回 **HTTP 400（非 404）**，证明新接口已随新 jar 生效。

**未验证：** 未用现场真实扫码值跑端到端（preview 返回正数 `defaultQty` → empty 带 `requestQty/requestUnit` 建单 → 确认基础数据 `quantity/standard_qty/unit` 不变 → 下次预览仍返回后台原始默认值）。建议拿一个真实物料号/扫码值实测一遍。

### 遗留事项

1. `/scan/preview` 与 `/scan/*` 一样是弱鉴权（见项目文档第 12 章严重项 1），生产建议启用设备密钥 + IP 白名单。
2. 单位不做白名单/换算是本次明确的产品取舍：员工输入任意文字，仓管按任务上显示的原文处理。若日后要统一单位口径，需另做单位字典与换算。
3. 前端构建产物已更新，浏览器若仍显示旧列请强刷（Ctrl+Shift+R）。

---

## 2026-07-31 · 料号映射导入超时 + 以仓库代号为唯一主键做覆盖更新

### 现象

在「料号映射」页导入数据，前端弹出 `导入失败 / timeout of 30000ms exceeded`。数据量小的文件能成功，数据量一大就必然失败。

### 根因

这里其实是**两个问题叠在一起**，报错信息只暴露了表层的那个。

**表层：30 秒是前端自己掐断的。** `frontend/src/api.ts` 里 axios 实例配的是 `timeout: 30000`，全局默认值。这个值对普通接口合理，但导入要上传文件再逐行入库，30 秒远远不够。报错是浏览器端产生的，**后端当时可能还在正常处理**，所以这个提示会误导人以为后端挂了。

**根本：导入的写库方式是逐行的，往返次数随行数线性增长。** 原来每一行映射都要：

1. 查一次 `findByWarehouseCodeAndEnabledTrue` 找存量记录；
2. 再 `saveMapping` 写一次，且 `saveMapping` 上有 `@Transactional`，每行一个独立事务；
3. 如果这行出错，再单独 insert 一条错误明细。

也就是每行至少 2 次数据库往返、1 次事务提交。几千行就是上万次往返，累积起来必然超过 30 秒。文件解析本身早就改成流式了，不是瓶颈；**瓶颈在数据库往返次数**。

另外还有个逻辑分裂：覆盖已有记录的判断写在 `ImportService` 里，字段校验写在 `BasicDataService.saveMapping` 里，两条链路口径不一致，容易出现导入能过、手工新增报错这类不一致行为。

### 改动

**后端 · 批量 upsert，把往返次数从 O(n) 降到 O(n/500)**

- `BasicDataService.saveMapping` 拆成两半：`normalizeMapping`（纯内存校验补齐，不碰数据库）+ `saveMapping`（落库）。导入和单条保存从此共用同一套字段规则。
- 新增 `upsertMappingsByWarehouseCode(List<...>)`：一个事务内只做 **1 次存量查询 + 1 次批量写入**。以「仓库代号」为唯一标识，已有启用记录则覆盖其 id 而非新增，因此不会再报「该仓库代号已有启用映射」。同一批文件内代号重复时用 `LinkedHashMap` 去重、**取最后一条**，符合「以最后上传为准」的口径。
- 仓库层新增 `findByWarehouseCodeInAndEnabledTrue`，一次 `IN` 查询拿回整批存量。
- `ImportService` 新增 `WRITE_BATCH_SIZE = 500`，映射行先攒进缓冲区，满 500 行落一次库；错误明细同样攒批 `saveAll`。解析结束调用 `finish()` 把尾批写完。
- 保留一条**降级路径**：批量写入失败时无法定位到具体是哪一行的问题，此时退化成逐行 upsert，把错误准确归到出错的那一行，保证「错误明细」弹窗里的行号仍然可用。

**前端 · 放宽超时并给出进度**

- `api.ts` 导出 `UPLOAD_TIMEOUT_MS = 600000`（10 分钟），仅用于导入上传，不影响其他接口的 30 秒默认值。
- `ImportButton.vue` 加上传进度：按钮显示 `上传中 x%`，传完转 `后台处理中…`，避免长任务看起来像卡死。
- 超时提示文案改成说明「文件可能仍在后台处理，请刷新列表确认结果」，而不是笼统报失败——因为前端超时确实不代表后端失败。

**Nginx · 给导入路径单独放宽**

`/api/` 原本 `proxy_read_timeout 120s`，即使前端等 10 分钟，Nginx 也会在 2 分钟时把连接掐断。新增 `location /api/imports/`：超时 600s，并 `proxy_request_buffering off`（大文件不必先在 Nginx 落盘缓冲完再转发给后端）。

放在 `/api/` 之前，前缀匹配取最长前缀，能正确命中。

### 涉及文件

| 文件 | 改动 |
| --- | --- |
| `backend/.../service/ImportService.java` | 攒批缓冲、`WRITE_BATCH_SIZE`、`buildMapping`、降级逐行 |
| `backend/.../service/BasicDataService.java` | 拆出 `normalizeMapping`、新增 `upsertMappingsByWarehouseCode` |
| `backend/.../repository/MaterialMappingRepository.java` | 新增 `findByWarehouseCodeInAndEnabledTrue` |
| `frontend/src/api.ts` | 新增 `UPLOAD_TIMEOUT_MS` |
| `frontend/src/components/ImportButton.vue` | 上传超时、进度显示、超时文案 |
| `frontend/nginx.conf`、`deploy/nginx/material-pull.conf` | 新增 `/api/imports/` 段 |

### 验证情况

已验证：后端 `mvn -o clean package` 通过，前端 `npm run build`（含 `vue-tsc` 类型检查）通过，两侧无 lint 错误。

**未验证：没有用现场真实的大文件跑过一次实际导入。** 建议拿实际那份文件试一次，确认行数与耗时。

### 遗留事项

1. `application.yml` 里 `spring.transaction.default-timeout: 15`（秒）。500 行一批的 upsert 正常情况余量充足，但如果现场数据库磁盘慢，可能需要调小 `WRITE_BATCH_SIZE` 或调大这个超时。**没有擅自改，因为它影响所有事务。**
2. 仓库里的 `deploy/nginx/material-pull.conf` 与线上配置已脱节（线上多 `/zebra-print/` 段），建议把那段补回仓库让两边对齐，否则下次有人直接拿仓库版本覆盖就会踩坑。
3. 前端 10 分钟、Nginx 600s 只是把天花板抬高，**不是无上限**。如果现场数据量继续增长到 10 分钟都不够，正确方向是改成异步导入：上传后立刻返回批次号，前端轮询进度，而不是继续加大超时。
