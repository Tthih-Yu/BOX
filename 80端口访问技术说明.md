# 物料拉动系统 · 80 端口全公司访问技术说明

> 本文解释：本系统如何做到「全公司不同网段的设备，只要浏览器打开一个网址就能访问」。面向需要理解或维护网络架构的人。
> 配套阅读：`项目文档.md` 第 7 章（网络与 80 端口映射，含完整排查证据）。本文侧重「技术原理与配置逐行解读」。

---

## 目录

- [一、一句话结论](#一一句话结论)
- [二、为什么必须是 80 端口](#二为什么必须是-80-端口)
- [三、整体架构与访问链路](#三整体架构与访问链路)
- [四、Nginx 配置逐段解读](#四nginx-配置逐段解读)
- [五、为什么全公司设备都能访问](#五为什么全公司设备都能访问)
- [六、为什么前后端同源、没有跨域问题](#六为什么前后端同源没有跨域问题)
- [七、换 IP / 换位置为何不受影响](#七换-ip--换位置为何不受影响)
- [八、安全边界](#八安全边界)
- [九、常见问题与自检](#九常见问题与自检)

## 一、一句话结论

在 Ubuntu 服务器本机装一个 **Nginx**，让它监听 **80 端口**，对外统一提供服务：

- 用户访问 `/`（网页）→ Nginx 直接返回前端静态文件（`frontend/dist`）。
- 用户访问 `/api/...`（接口）→ Nginx 把请求转发（反向代理）给本机后端 `127.0.0.1:8080`。

全公司设备只要在浏览器打开 `http://<服务器IP>/` 就能用。核心就一点：**所有流量都走 80 端口，而公司网络只放行 80。**

---

## 二、为什么必须是 80 端口

这是本系统架构最关键的约束，不是随便选的。

公司内网里，Windows 服务器和这台 Ubuntu **不在同一网段**，中间隔着一台公司的**有状态安全设备**（防火墙/安全网关一类）。实测发现它按**目标端口白名单**拦截流量：

- 标准 Web 端口 **80：放行** ✅
- 非标准端口（8080、8443、18000 等）：TCP 能握手，但一旦开始传输 HTTP 数据，就被这台设备**注入 RST 包掐断连接** ❌

关键证据是抓包比对 TTL：正常数据包 TTL=127（来自 Windows），而掐断连接的 RST 包 TTL=63（Linux/BSD 特征），证明 RST 不是通信双方发的，而是**中间设备伪造的**。加密也没用——TLS 的 ClientHello 在非标准端口照样被掐。

> 完整排查过程、分层测试表、TTL 证据表见 `项目文档.md` 第 7.5 节。

**结论**：既然只有 80 端口能穿过这台设备，就让服务走 80。后端本身跑在 8080，但 8080 只在本机内部用（`127.0.0.1`），对外一律经 Nginx 的 80 端口出去。

---

## 三、整体架构与访问链路

```text
        公司各网段的电脑 / 手机 / 扫码枪
                    │  浏览器访问 http://<服务器IP>/
                    │  （80 端口，是唯一能穿过公司安全设备的端口）
                    ▼
        ┌───────────────────────────────┐
        │   Ubuntu 服务器                 │
        │                                │
        │   Nginx  (监听 0.0.0.0:80)      │  ← 对外唯一入口
        │     ├─ /            → 前端静态文件 frontend/dist
        │     ├─ /api/        → 反代 127.0.0.1:8080 (后端)
        │     ├─ /api/ws      → 反代 127.0.0.1:8080 (WebSocket)
        │     └─ /tools/      → 独立 Excel 工具（静态）
        │                                │
        │   后端 Spring Boot              │
        │   (只监听 127.0.0.1:8080)       │  ← 不直接对外
        │                                │
        │   MySQL (127.0.0.1:3306)        │  ← 只本机
        └───────────────────────────────┘
```

要点：**只有 Nginx 的 80 端口对外暴露**；后端 8080 和数据库 3306 都只绑定本机回环地址，外部网络碰不到它们，只能通过 Nginx 转发访问。

## 四、Nginx 配置逐段解读

以下逐段解读真实配置 `deploy/nginx/material-pull.conf`（部署到 `/etc/nginx/sites-available/material-pull.conf`）。

### 4.1 监听 80、接受任意 IP

```nginx
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    root /home/tthih/IE/frontend/dist;
    index index.html;
```

- `listen 80 default_server`：监听 80 端口（IPv4 + IPv6），`default_server` 表示没有更精确匹配时都由它兜底。
- `server_name _`：`_` 是通配，**不绑定任何具体域名/IP**，所以不管用户用哪个 IP 访问都能命中。这是「换 IP 不用改配置」的关键（见第七节）。
- `root ... /dist`：网页根目录指向前端构建产物。

### 4.2 上传体积上限

```nginx
client_max_body_size 512m;
```

允许最大 512MB 请求体，配合后端流式解析支持大批量数据导入（Excel/CSV）。与后端 multipart 上限保持一致。

### 4.3 安全响应头

```nginx
add_header X-Content-Type-Options nosniff always;
add_header X-Frame-Options SAMEORIGIN always;
add_header Referrer-Policy no-referrer-when-downgrade always;
add_header Content-Security-Policy "default-src 'self'; connect-src 'self' ws: wss:; ... script-src 'self'; ..." always;
```

- 防 MIME 嗅探、防点击劫持（禁止被跨站 iframe 嵌入）、限制 referrer。
- `Content-Security-Policy`（CSP）限制页面只能加载**同源**的脚本/样式（`script-src 'self'`）。这也是为什么前端「浏览器打印」要用父窗口触发子窗口打印——内联脚本会被这条 CSP 拦掉。

### 4.4 前端静态页面与 SPA 路由

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
location ~* \.(?:js|css|png|jpg|...|woff2?)$ {
    expires 7d;
    add_header Cache-Control "public, max-age=604800, immutable";
    try_files $uri =404;
}
```

- `try_files $uri $uri/ /index.html`：先找真实文件，找不到就回退到 `index.html`。这是**单页应用（SPA）**的标准写法——Vue Router 的前端路由（如 `/tasks`、`/print`）在服务器上没有对应文件，全部交给 `index.html` 由前端接管。
- 第二段：js/css/图片等静态资源缓存 7 天，加速二次访问。

### 4.5 后端接口反向代理（核心）

```nginx
location /api/ {
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 10s;
    proxy_read_timeout 120s;
    proxy_send_timeout 120s;
}
```

- 凡是 `/api/` 开头的请求，Nginx 转发给本机后端 `127.0.0.1:8080`。**用户浏览器从头到尾只和 80 端口打交道，感知不到 8080 的存在。**
- `X-Real-IP` / `X-Forwarded-For`：把用户真实 IP 传给后端（否则后端只看到 127.0.0.1）。
- 超时 120s，兼顾稍慢的批量接口。

### 4.6 WebSocket（实时推送）单独处理

```nginx
location /api/ws {
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    ...
    proxy_read_timeout 3600s;
}
```

WebSocket 需要 HTTP 协议「升级」，所以必须单独设 `Upgrade` / `Connection "upgrade"` 头，否则实时消息（任务刷新、大屏看板）连不上。超时拉长到 1 小时，避免长连接被中途断开。

### 4.7 监控端点限内网

```nginx
location /api/actuator/ {
    allow 127.0.0.1;
    allow 10.0.0.0/8;
    allow 172.16.0.0/12;
    allow 192.168.0.0/16;
    deny all;
    proxy_pass http://127.0.0.1:8080;
}
```

Actuator 监控指标（内存、连接池等）只允许本机和三个私有网段访问，其它一律拒绝，防止监控信息外泄。

### 4.8 附带的独立 Excel 工具（与本系统隔离）

```nginx
location ^~ /tools/ {
    alias /home/tthih/tools/;
    ...
}
location /tools-api/ {
    proxy_pass http://127.0.0.1:9000/;
    ...
}
```

同一个 80 入口还挂了一个独立的 Excel 工具（静态页在 `/tools/`，Python 后端预留在 9000 端口）。`^~` 保证 `/tools/` 优先匹配，与物料系统互不干扰。9000 未启动时 `/tools-api/` 返回 502 属正常。

## 五、为什么全公司设备都能访问

三个条件叠加，才做到「全公司任意设备打开就能用」：

1. **走的是 80 端口**：这是公司安全设备唯一放行的端口（见第二节），所以跨网段的流量不会被 RST 掐断。任何网段的设备到这台服务器的 80 都是通的。
2. **Nginx 监听 `0.0.0.0:80`**：`0.0.0.0` 表示监听服务器所有网卡地址，不限来源。加上 `server_name _` 接受任意访问地址，所以谁来都接。
3. **前端是纯静态 + 标准 HTTP**：前端已构建成静态 HTML/JS/CSS，任何有浏览器的设备（电脑、手机、平板、安卓扫码枪）都能打开，不挑操作系统、不用装客户端。

所以使用者不需要任何配置，只要：打开浏览器 → 输入 `http://<服务器IP>/` → 登录。

---

## 六、为什么前后端同源、没有跨域问题

「跨域（CORS）」问题的根源是：网页所在的地址，和它请求的接口地址，**协议/域名/端口三者有任一不同**，浏览器就会拦截。

本架构下，用户看到的一切都来自**同一个地址、同一个 80 端口**：

- 网页：`http://<IP>/`
- 接口：`http://<IP>/api/...`

协议（http）、IP、端口（80）**完全一致**，浏览器认为是同源，根本不触发跨域检查。这就是「前后端同源」——不是靠在后端配 CORS 白名单去「允许跨域」，而是**从架构上让跨域根本不发生**。

对比：如果前端在 5173、后端在 8080（开发模式那样），就是跨域，得额外配置。生产用 Nginx 80 统一入口后，这个麻烦自然消失。

---

## 七、换 IP / 换位置为何不受影响

这台服务器可能换位置插网线导致 IP 变化。本架构对此免疫，靠两点：

1. **服务端零改动**：
   - Nginx `server_name _` 接受任意 IP，换了 IP 也照样命中。
   - 反代目标写的是 `127.0.0.1:8080`（本机回环），跟外部 IP 无关，永远指向本机后端。
   - 所以换 IP 后，Nginx 和后端**都不用改任何配置**。

2. **访问地址自动播报**：
   - 有个开机自启的小服务（`material-pull-url-announcer`），检测到 IP 变化时（约 15 秒内）自动把最新访问地址写到家目录的 `物料拉动系统-访问地址.txt`。
   - 换位置后，管理员打开这个文件就知道新地址，发给同事即可。

> 没用 `主机名.local`（mDNS）这类域名方案，因为实测组播默认不跨三层网段、中间设备也不转发，跨网段用不了。要稳定域名得找 IT 在内网 DNS 分配。

---

## 八、安全边界

- **对外只开 80 一个口**：后端 8080、数据库 3306 都只绑定 `127.0.0.1`，外部网络无法直连，只能经 Nginx 转发，缩小了攻击面。
- **当前是 HTTP 明文**：内网环境可接受。如需加密可另配 443（`frontend/nginx-tls.conf` 模板 + 自签证书脚本），但注意公司安全设备对 443 的放行需另行验证，不保证像 80 一样通。
- **监控端点限内网**：`/api/actuator/` 仅私有网段可访问。
- **安全响应头 + CSP**：防点击劫持、防 MIME 嗅探、限制脚本来源。
- **扫码接口鉴权**：`/api/scan/*` 默认免登录（兼容现有安卓设备），生产建议启用设备密钥 + IP 白名单加固（见 `项目文档.md` 第 12 章）。

---

## 九、常见问题与自检

**本机能开、别的网段打不开**
> 几乎一定是没走 80 端口。确认访问地址是 `http://<IP>/`（不带 `:8080` 之类）。非标准端口会被公司安全设备掐断。

**打不开 / 白页**

```bash
# 后端是否正常（应返回 200）
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1/api/health/ready
# Nginx 是否在跑
systemctl status nginx
# 前端是否已构建
ls /home/tthih/IE/frontend/dist/index.html
```

**登录报 403 / 跨域错误**
> 本架构同源，正常不会有。若出现，检查是不是有人绕过 Nginx 直接访问了 8080，或有残留进程占用 8080：`ss -ltnp | grep 8080`。

**换了 IP 后同事打不开**
> 让他们用 `~/物料拉动系统-访问地址.txt` 里的最新地址。

**改了 Nginx 配置后生效**

```bash
sudo nginx -t && sudo systemctl reload nginx   # 先测试语法再热重载，不断连接
```

