# 物料拉动系统 · 潜在问题清单

> 最后更新：2026-08-18
>
> 仅记录未处理或待验证问题。按 P0（生产动作/数据完整性）、P1（安全/容灾）、P2（稳定性/性能）、P3（数据/工程治理）排序；关闭后移入《修复与发布记录.md》。

## 审计说明

累计扫描后端、前端、Android、Windows 打印代理、数据库工具、SQL、部署脚本和文档，约 600 个项目文件。2026-08-18 对当前工作树补做全量静态审计：覆盖后端 186 个 Java 文件（约 13,107 行）、前端 41 个 Vue/TS 文件（约 6,494 行），并复核 `application.yml`、数据库迁移 SQL、Nginx 和公共静态脚本。本轮严格只读，未编译、未访问数据库、未请求业务接口、未操作运行服务或进程。历史生产 MySQL 只读核对为 34 张表、约 18.31 MB。以下问题分为代码直接成立的缺陷和需运行时验证的风险。

## P0 · 生产动作与数据完整性

### 1. 周计划与标签共用 quantity

- **现状**：`m_material_mapping.quantity` 原本表现为每盒容量，周计划却写入当天总需求；扫码建单和 ZPL 又把它当打印数量。
- **风险**：标签可能把“一天需求”印成“一盒数量”，造成拣货放大。
- **建议**：拆分盒容量和日需求字段，或建立独立需求结果表；启用前确认现场口径。

### 2. 多条映射重复写入全量需求

- **现状**：同一料号的多个交货区、A/B 双盒映射都写相同全量需求。
- **风险**：汇总或多标签场景重复计算。
- **建议**：按日期、组件、工厂、交货区存需求并确认分摊规则。

### 3. 打印代理可能重复领取

- **现状**：`claimForAgent()` 先查 RENDERED/SENT 再更新，没有明确行锁或 SKIP LOCKED。
- **风险**：两个代理可能同时拿到同一 ZPL；乐观锁提交冲突时内容可能已返回。
- **建议**：条件原子更新、悲观锁或 FOR UPDATE SKIP LOCKED；代理按 printJobNo 持久化防重。

### 4. 打印/AGV外部调用早于数据库提交

- **风险**：外部已接单而本地事务回滚，重试后重复打印或重复派发。
- **建议**：事务内保存本地作业和可靠 Outbox，提交后异步下发；外部接口使用稳定幂等键。

### 5. 打印与 AGV 回调缺少严格状态机

- **风险**：迟到回调可让 PRINTED/ARRIVED 状态倒退；重复回调产生副作用；AGV 非法枚举可能返回 500。
- **建议**：建立迁移矩阵，保存事件 ID、序号和时间，拒绝重复/倒退；非法状态返回 4xx。

### 6. WebSocket 在事务提交前推送

- **风险**：前端先显示新状态，数据库随后回滚，刷新后状态倒退。
- **建议**：在事务 AFTER_COMMIT 推送，并携带业务版本号。

### 7. 浏览器打印直接标记成功

- **风险**：用户取消、浏览器拦截、缺纸或卡纸仍统计为 PRINTED。
- **建议**：改成“已生成/待确认”，由代理回调或操作员确认。

### 7A. 幂等记录可能先于主事务提交成功

- **证据**：`TaskService.action()` 和 `ScanService.scanEmpty()` 在主事务内调用 `idempotencyService.finish()`；`IdempotencyService.begin/finish/fail()` 均为 `REQUIRES_NEW`。
- **风险**：幂等记录先独立提交为 `DONE`，随后主业务事务若提交失败，数据会回滚，但重试仍被判定为已成功并拦截。
- **建议**：幂等完成状态仅在主事务 `AFTER_COMMIT` 后写入；失败或提交异常必须允许安全重试。

### 7B. 打印成功会绕过物流状态直接完成任务

- **证据**：`TaskService.completeByPrint()` 对所有非终态任务直接扣减库存（若可用）、完成盒状态并写 `COMPLETED`；浏览器打印也会触发该路径。
- **风险**：任务可能未接单、未拣货、未配送、未收货就因打印动作关闭；浏览器取消打印仍可能闭环任务。
- **建议**：打印状态和任务物流状态解耦；只有真实收货或明确授权的人工确认才能完成任务。

### 7C. 缺少库存记录时任务仍可完成

- **证据**：`tryDeductInventory()` 在仓库料号为空或找不到库存记录时直接返回，调用方随后仍设置任务为 `COMPLETED`。
- **风险**：形成“任务已完成、库存未扣减”的账实差异。
- **建议**：生产任务完成必须满足库存记录存在且扣减成功；确需无库存模式时应显式配置并审计，不能静默跳过。

### 7D. AGV 未配置时仍可能标记已派发

- **证据**：AGV URL 为空时创建 `CANCELLED` 作业并继续主流程，交付任务仍可能写入 `agvDispatched=true` 和 AGV 作业号。
- **风险**：页面和任务状态显示已派发，但没有 AGV 接单；后续判断可能不再重试。
- **建议**：区分 `NOT_CONFIGURED/SKIPPED/SENT`，只有外部确认接单后才能置已派发。

### 7E. 空盒回收 AGV 失败后可能无法重试

- **证据**：空盒回收保存 `returnAgvJobNo` 后，重复保护主要检查该字段是否存在，没有确认对应 AGV 作业是否已取消或失败。
- **风险**：首次派发因未配置或外部失败成为 `CANCELLED/FAILED` 后，现场再次操作仍被当成重复请求。
- **建议**：重复判断应基于开放状态；失败/取消作业允许使用新幂等键重新派发并保留关联链。

### 7F. 无表头导入静默丢失第一行

- **证据**：`ImportService.BatchAccumulator.accept()` 第一次收到行时始终建立列映射后 `return`，即使首行没有识别成表头、已回退为位置映射也不会处理该行。
- **风险**：旧格式或无表头文件的第一条业务数据无报错丢失。
- **建议**：显式判断是否识别为表头；只有真实表头才跳过，否则按位置映射立即处理首行。

### 7G. 导入不是全文件原子事务

- **现状**：导入按行或按 500 行批次提交，解析中途失败时前面批次可能已落库，批次最终却标记失败。
- **风险**：用户可能把“导入失败”理解为没有数据变化，重试后产生覆盖、重复或混合版本数据。
- **建议**：明确采用“全有或全无”或“可恢复分批”语义；后者需要批次版本、可回滚记录、进度状态和明确的部分成功提示。

## P1 · 安全、数据库与容灾

### 8. 数据库工具允许任意 SQL 和批量删除

`dbtool/server.py` 支持全库增删改，默认用户 root、默认口令 changeme。生产应默认停用；必须使用时仅绑定本机、SSH 隧道、独立只读账号，写权限临时授权并审计。

### 9. 扫码接口默认无设备密钥/IP白名单

默认 `require-device-key=false` 且 CIDR 为空，员工号只显示不校验。生产应强制每设备独立密钥、厂区 CIDR、吊销轮换和防重放。

### 10. 数据库工具 Web 防护不足

未发现 CSRF token；Cookie 无 Secure，可走 HTTP，未见登录限速。应仅本机/HTTPS，增加 CSRF、Origin 校验、Secure Cookie、限速和破坏性操作二次认证。

### 11. 备份自动化与恢复验证不完整

有脚本和 3 份 2026-08-12 备份，但未确认每日 timer/cron、失败告警和恢复核对。应每日自动备份，日/周/月保留，并定期隔离恢复。

### 12. 主库与备份同机

磁盘、整机、勒索或误格式化可能同时损坏主库和备份。应同步到 NAS、独立磁盘或异机并校验哈希。

### 13. MySQL Binlog 未启用

无法按时间点恢复。应启用 Binlog、保留期和空间告警，并演练全量备份加 Binlog 恢复。

### 14. 数据库结构不能确定性重建

无 Flyway/Liquibase；首次依赖 ddl-auto=update，之后手工零散 SQL。应以生产 schema 建基线，引入 Flyway，并在空 MySQL 上迁移验证。

### 15. 关键业务关系无数据库外键

任务、打印、AGV、批次依赖字符串关联，可能形成孤儿记录。先巡检，再为稳定关系补外键或质量告警。

### 16. 生产可能误连 H2 空库

默认 Profile 使用 H2，只有标准 systemd 显式启用 MySQL。生产应在未指定数据库时拒绝启动或移除 H2。

### 17. 备份凭据和权限需收紧

避免命令参数传密码；使用 600 权限 client 配置、专用低权限备份账号和统一文件所有者。

### 18. 生产入口主要为 HTTP

同网段可能窃听 token、设备密钥和业务数据。应启用 HTTPS，HTTP 只重定向，客户端严格验证证书。

### 18A. SUB_ADMIN 后端授权范围过宽

- **证据**：`RoleInterceptor` 对 `SUB_ADMIN` 的规则是：只要接口注解不是“仅 ADMIN”，就自动放行，不要求注解中实际包含 `SUB_ADMIN`。
- **风险**：可通过只标注 `PLANNER`、`WAREHOUSE` 或 `LINE` 的接口。任务服务有部分二次校验，但主数据、库存、BOM、计划、打印和 AGV 并非全部具备同等防护；与前端“仅部分管理能力”的说明不一致。
- **建议**：删除隐式准管理员规则，严格要求角色出现在 `@RequireRoles` 中；生成接口权限矩阵并增加自动化测试。

### 18B. SYSTEM 被视为全局管理员

- **证据**：`RoleInterceptor` 对 `ADMIN` 或 `SYSTEM` 无条件返回放行。
- **风险**：当前外部 API Key 路径有限，但未来新增 SYSTEM 会话来源或端点匹配遗漏时，将自动获得管理员接口能力。
- **建议**：SYSTEM 也必须按接口显式授权，不应全局绕过角色注解。

### 18C. 登录缺少暴力破解防护

- **现状**：未发现按账号/IP 的失败计数、限速、指数退避、临时锁定、验证码或告警。
- **风险**：网络入口可被持续尝试账号密码，固定弱初始密码进一步放大风险。
- **建议**：增加账号和 IP 双维度限速、失败告警、短期锁定和审计；首次登录强制改初始密码。

### 18D. WebSocket 主题缺少订阅级授权

- **证据**：握手需要登录，但简单消息代理开放 `/topic/**`，未发现入站通道授权规则。
- **风险**：任意登录角色可自行订阅任务、盒子、AGV、告警等其他角色页面使用的主题；菜单隐藏不能构成数据隔离。
- **建议**：增加 STOMP 入站授权或按角色拆分主题，推送 DTO 只包含必要字段。

### 18E. 安卓下载页存在 DOM XSS

- **证据**：`frontend/public/app-download.js` 将 `latest.json` 的 `versionName`、`publishedAt`、`releaseNotes` 和错误文本直接拼入 `innerHTML`；`apkUrl` 直接赋给链接。
- **风险**：发布元数据被误写或篡改后，公开下载页可执行脚本或跳转危险协议。
- **建议**：全部使用 `textContent`/DOM 节点构造；下载 URL 仅允许同源 HTTPS/相对路径，并校验固定目录和文件扩展名。

### 18F. 前端 Token 存放在 localStorage

- **风险**：任何同源 XSS 都可读取 8 小时 Bearer Token；公共下载页 XSS 会放大该风险（若同源访问）。
- **建议**：优先使用 `HttpOnly + Secure + SameSite` Cookie；若暂不改认证架构，至少缩短会话、强化 CSP、清除全部 XSS并增加设备/会话管理。

### 18G. 部署示例带固定弱凭据且默认 dev

- **证据**：`material-pull.env.example` 默认 `SPRING_PROFILE=dev`，包含固定设备 Key 和 `Admin123456`。
- **风险**：复制示例后忘记修改会启用开发能力、复用设备密钥并留下弱管理员密码。
- **建议**：示例不提供可用秘密；生产 profile 为默认安全值，缺少随机密钥或管理员初始化密码时拒绝启动。

## P2 · 稳定性、性能与容量

### 19. Outbox 不是可靠消息闭环

当前先推送再用 `String.valueOf(payload)` 写表；写入异常被吞掉，无消费、状态更新、补发和清理。payload 非规范 JSON，超过 4000 字符可能静默丢失。应明确它是可靠队列还是日志，并补齐相应实现。

### 20. Outbox 占全库约 42%

约 2702 行、7.73 MB；全库约 18.31 MB。应增加 `(status,next_retry_at,created_at)` 索引，归档/清理成功数据，监控体积和最老事件。

### 21. 仪表盘与维护逻辑全表加载

多处 findAll 后在 Java 聚合，扫描日志取 1000 再截 20。应改用数据库 COUNT/GROUP BY、Top20、分页/流式处理和短时缓存。

### 22. 多个列表固定截断 1000 条

旧任务、标签、打印、AGV、导入批次可能静默消失，关联/导出也可能漏数。应统一服务端分页并返回 total，业务计算不得用 UI 上限代替全集。

### 23. 512 MB 上传和长请求

并发导入会占用线程、临时磁盘、连接和事务，也有压缩炸弹风险。应降低上限、限制并发、异步/分批导入并清理临时文件。

### 24. JVM 内存较高且无资源基线

当前约 859 MB，systemd 未设 MemoryMax，启动未见明确 Xmx。应采集堆/GC/延迟/导入峰值后设置上限和限流。

### 25. 日志/导入错误无保留策略

当前 t_import_error 4523 行、log_task 2058 行，主要靠人工删除，部分表缺时间索引。应定义保留期、补索引并分批归档/删除。

### 26. 定时任务锁租约可能过期

自动打印/超时检查租约 300 秒，周计划 1800 秒。多实例长任务可能重复执行；应续租或使用数据库任务租约。

### 27. 多处异常被静默忽略

Outbox、Redis、标签/任务辅助逻辑存在 ignored catch。应记录限频 WARN、traceId、业务号，明确允许降级与必须失败的边界。

### 28. 打印取消可能拖累本地事务

外部取消 URL 为空或失败时，本地任务取消可能回滚。应异步取消，并分开记录本地/外部取消状态和补偿重试。

### 29. 缺少监控告警闭环

有 Prometheus 指标但未见抓取、规则、通知和仪表盘。应覆盖服务、5xx/延迟、JVM、磁盘、MySQL、Outbox、定时任务和备份。

### 30. 前端公共包过大

构建产生约 1.03 MB、1.25 MB 两个 JS 包。应拆 vendor、按需引入 Element Plus/ECharts并设置体积预算。

### 30A. 仓库任务“已打印”统计错误

- **证据**：`WarehouseTasks.vue:isPrinted()` 除 `FAILED/CANCELLED` 外，对任意非空 `printStatus` 返回 true。
- **风险**：`CREATED/RENDERED/SENT` 都会被统计为已打印，现场可能误以为标签已出纸。
- **建议**：只有 `PRINTED` 计入已打印；其余状态分别展示待生成、待领取、已下发和失败。

### 30B. 批量浏览器打印反馈字符串损坏

- **证据**：`FactoryPrint.vue` 当前成功提示为“： 张”，补打提示为“已调起补打：”，没有数量或业务号。
- **风险**：操作员无法从反馈确认打印范围，增加重复点击和重复打印概率。
- **建议**：显示工厂、类别、任务数、成功记录数和失败数，并提供业务号。

### 30C. 打印记录和条码生成失败被静默吞掉

- **现状**：多个浏览器打印路径对后台打印记录保存使用空 `catch`；批量条码生成失败也继续打印。
- **风险**：页面显示成功但无审计记录，或打印出条码空白的标签。
- **建议**：打印前必须确认所有条码生成成功；记录保存失败要明确提示“已调起但未记账”，并支持补记或重试。

### 30D. 定时刷新与 WebSocket 存在请求竞态

- **现状**：仓库任务和大屏同时由定时器、WebSocket、手工刷新和操作后刷新触发 `load()`，没有取消旧请求或响应序号。
- **风险**：较早发出的慢请求可能最后返回并覆盖新状态，造成界面短暂倒退或误操作。
- **建议**：使用递增请求版本、AbortController 或单飞刷新；只接纳最新响应。

### 30E. Dashboard 重复创建 ECharts 实例

- **证据**：每次刷新都 `echarts.init(el)`，未复用实例，也未在卸载时 `dispose()`。
- **风险**：重复实例、事件残留、内存增长，且窗口变化后图表不自适应。
- **建议**：缓存实例，更新只调用 `setOption`；卸载时 dispose，并处理 resize。

### 30F. 菜单白名单请求失败时前端默认放开

- **现状**：请求前先设置 `whitelistLoaded=true`，失败后将白名单设为 null，当前会话不再重试。
- **风险**：权限服务临时故障时显示比配置更多的菜单；后端仍会拦截，但造成界面越权暴露和大量 403。
- **建议**：失败应安全收敛并允许重试，不能把“加载失败”解释为“未配置”。

### 30G. 前端列表刷新异常处理不一致

- **现状**：多个页面直接 `onMounted(load)` 或操作后裸调用 `load()`，依赖全局拦截器提示；部分 Promise rejection 无页面级处理。
- **风险**：页面可能保留旧数据、关闭对话框但刷新失败，用户无法区分写入失败与仅刷新失败。
- **建议**：统一 loading/error/stale 状态，写操作结果与刷新结果分开提示。

### 30H. Redis 与内存会话过期语义不一致

- **现状**：内存会话校验实体 `expiresAt`；Redis 会话读取时不校验该字段，且每次请求刷新 Redis TTL。
- **风险**：集群模式活跃会话可能超过登录响应显示的到期时间，单机模式则按绝对时间过期。
- **建议**：明确采用绝对过期或滑动过期，并在两种存储实现中保持一致；登录响应同步展示真实规则。

### 30I. 导入错误明细自身可能保存失败

- **现状**：错误记录拼接整行全部单元格，超宽或超长输入可能超过数据库列限制。
- **风险**：真正的导入错误被二次保存异常覆盖，批次统计和错误明细不完整。
- **建议**：逐字段和整行限长，保存结构化摘要、原始文件哈希与行号；错误落库失败必须有独立日志和告警。

## P3 · 数据维护与工程治理

### 31. 单根用量未维护

`single_unit_usage` 曾核对为 1284 条全 NULL，周计划计算全部跳过；维护后必须重新验证 P0 第1、2项。

### 32. 真实扫码地址不匹配

`35452248 @ TH80-OLE1-07-A05-A06` 与映射地址在 OLE1/OLE、07/03 上不一致，不是字符归一化问题。需核对二维码或映射。

### 33. 工位用料表为空

`m_station_material` 为 0 行，系统使用兜底值；需确认是未启用模块还是应维护的数据。

### 34. 物料主数据为空

`m_material` 为 0 行，单位默认“个”；模板表为空但打印硬编码 ZPL，模板页面与真实实现分离。

### 35. 周计划行版本不递增

`t_weekly_plan_row.version` 长期为 0，冲突弹窗误导。应使用真正 @Version 或移除展示。

### 36. 周计划解析未充分用真实文件验证

需用脱敏真实 Excel 覆盖合并单元格、公式、列偏移、签字区和手工格式。

### 37. 周计划起始日未强制周一

若业务周必须从周一开始，应前后端同时校验。

### 38. 多计划员责任边界不足

同厂计划员可能互相覆盖。应使用独立账号、数据权限，并记录导入/覆盖操作者。

### 39. 没有自动化测试

后端无测试目录，前端无测试脚本，打印代理和 Android 也未见测试。优先覆盖扫码防重、状态机、库存、并发领取、回调幂等、AGV、周计划、权限和迁移。

### 40. 部署配置重复且路径漂移

存在两份 Compose、多份 Nginx、四份 systemd；模板路径与当前路径不一致。应保留唯一生产模板，移除 Java 包目录中的 Compose，并自动比对线上配置。

### 41. 发布版本混乱

JAR/镜像仍为 0.8.4，功能迁移已到 V0.9.2。应统一版本，并在健康接口返回 Git commit、构建时间和 schema 版本。

### 42. 构建目录与运行账号耦合

Maven 无法覆盖运行账号生成的 target 文件，服务又直接运行 target 中 JAR。应独立构建不可变制品并原子发布。

### 43. 缺少依赖漏洞、许可证和 SBOM 检查

应在隔离 CI 使用官方源扫描并生成 SBOM，按季度小步升级；未联网核验前不认定具体 CVE。

### 44. 备份和大型安装包位于源码工作区

数据库备份应迁专用挂载并异机复制，DEB/APK 应进入制品库或受控离线介质。

## 建议处置顺序

1. 冻结周计划写 `quantity` 的上线条件，拆分单盒数量与日需求量。
2. 修复幂等 `DONE` 早于主事务提交的问题，并增加提交失败回归测试。
3. 修复打印唯一领取、代理端持久防重、浏览器假成功和打印自动关闭任务。
4. 建立打印/AGV严格状态机，将外部调用改为提交后可靠异步执行。
5. 禁止无库存扣减完成任务，修复 AGV 未配置仍标记已派发及失败后不可重试。
6. 收紧 `SUB_ADMIN/SYSTEM` 权限；加固扫码设备、登录限速和 WebSocket 订阅授权。
7. 修复无表头导入丢首行、部分提交语义、安卓下载页 XSS 和前端打印反馈。
8. 停用或隔离数据库工具，启用 HTTPS，建立异机备份、恢复演练、Binlog 和 Flyway。
9. 明确 Outbox 是可靠队列还是日志，完善消费、重试、清理和提交后推送。
10. 优化全表查询、1000 条截断、上传并发、日志保留、前端竞态和资源限制。
11. 补自动化测试、告警、版本标识和统一发布配置。

## 附录 · 排查 SQL

```sql
SELECT line_material_code, box_size, quantity, single_unit_usage
FROM m_material_mapping WHERE enabled=1
ORDER BY line_material_code, mapping_order LIMIT 20;

SELECT COUNT(*) total, SUM(single_unit_usage IS NOT NULL) maintained
FROM m_material_mapping WHERE enabled=1;

SELECT status, COUNT(*) rows_count, MIN(created_at) oldest, MAX(created_at) newest
FROM sys_outbox_event GROUP BY status;

SELECT p.print_job_no, p.task_no
FROM t_print_job p
LEFT JOIN t_replenishment_task t ON t.task_no=p.task_no
WHERE p.task_no IS NOT NULL AND t.id IS NULL;

SELECT a.agv_job_no, a.task_no
FROM t_agv_job a
LEFT JOIN t_replenishment_task t ON t.task_no=a.task_no
WHERE a.task_no IS NOT NULL AND t.id IS NULL;
```

## 待运行时验证

- EXPLAIN ANALYZE 高频 SQL，并核对 MySQL 慢查询、连接、buffer pool、磁盘和碎片。
- 检查30天日志中的5xx、死锁、超时、重复打印、Outbox/打印/AGV及定时任务异常。
- 两台模拟代理并发领取，验证重复打印。
- 构造数据库提交失败，验证外部打印/AGV幽灵作业。
- 构造主事务最终提交失败，确认幂等记录不会残留 `DONE`。
- 在无库存记录、浏览器取消打印、AGV URL 为空场景验证任务不会错误完成或标记已派发。
- 用无表头 CSV/XLSX 验证第一条业务数据不会丢失；在中途解析失败时核对已写入范围和批次状态。
- 使用 SUB_ADMIN/SYSTEM/LINE 逐接口验证后端权限矩阵，并尝试跨角色订阅 WebSocket 主题。
- 篡改测试环境 `latest.json` 的展示字段与下载 URL，验证下载页输出编码和协议白名单。
- 隔离恢复备份并记录真实 RPO/RTO。
