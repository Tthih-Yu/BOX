# 物料拉动系统 · 潜在问题清单

> 本文记录**已发现但尚未处理**的问题，以及需要现场确认口径才能定方案的事项。
> 处理完一条就把它移到 `修复与发布记录.md`，保持本文只留未解决项。
> 配套阅读：`项目文档.md`（系统全貌）、`修复与发布记录.md`（已完成的改动）。
>
> 最后更新：2026-08-11

## 目录

- [P0 · 需要定方案](#p0--需要定方案)
- [P1 · 数据待维护](#p1--数据待维护)
- [P2 · 次要与留档](#p2--次要与留档)
- [附录 · 排查用 SQL](#附录--排查用-sql)

---

## P0 · 需要定方案

### 1. 料号映射「数量」列一字两用：既是每盒容量，又是当天需求量

**现状**：`m_material_mapping.quantity` 有两个互相冲突的用途。

消费链路（现有）：

```
mapping.quantity → task.requestQty → 打印载荷 "qty" 字段 + ZPL 标签数量栏 → 印到纸上
```

具体在 `ScanService` 两处：建单 `task.setRequestQty(resolveRequestQty(req, mapping.getQuantity()))`、
APP 扫码预览 `r.defaultQty = guard.positive(mapping.getQuantity(), ...)`。

写入方（周计划联动，V0.9.0 新增）：`WeeklyPlanCalcService` 把「当天用量」写进同一个字段。

**冲突点**：原始数据里 quantity 是**每盒容量**，证据是同一料号换盒型数值就变：

| material_code | request_qty | mapping_qty | box_size |
|---|---|---|---|
| 13799814 | 500.00 | 500.00 | G |
| 13799814 | 500.00 | 180.00 | C |

分布也是典型盒容量式整数（200 有 182 条、500 有 114 条、400 有 100 条…）。

**后果**：联动计算跑过之后，扫码默认数量和**出货标签上印的数量**会变成「当天总需求量」而不是「一盒的量」。
例如某组件一天需 5000、一盒装 500，标签会印 5000，仓库可能按 5000 拣货。

**待确认**：现场希望出货标签印**一盒的量**还是**当天总需求量**？

- 若印一盒的量 → 必须把两个语义拆开。方案 A：给映射加新字段（如 `daily_demand_qty`）专存计算结果，`quantity` 不动。
  方案 B：新建 `t_weekly_demand` 结果表，按「日期+组件+交货区」存，映射表完全不碰（粒度最干净，也顺带解决下面第 2 条）。
- 若印当天总需求量 → 当前实现即可，但建议加原值备份与回滚开关。

**当前风险等级**：暂不会引爆。1284 条映射的 `single_unit_usage` 全为 NULL，
计算引擎会全部走「跳过」分支，一条都不会写。等单根用量维护后才会真正生效。

---

### 2. 同一组件的多条映射被写入相同的全量需求

**现状**：506 个启用料号中，**没有一个是单映射**，全是 2/4/6/8/12 条
（对应多交货区 × A/B 双盒）。`WeeklyPlanCalcService` 对每条映射都写入**相同的**全量当天需求，
没有按交货区或盒子拆分。

**用户已确认**：P1 按现状用，暂不拆分。

**留档原因**：若日后发现数量偏大，此处是首查点。真要拆分时，需要现场给出口径——
同一组件有 2 个交货区各 2 个盒时，需求量该按比例分摊，还是各交货区都记全量。

---

## P1 · 数据待维护

### 3. 单根用量 1284 条全为空，联动计算实际不可用

`m_material_mapping.single_unit_usage` 全部为 NULL。按现行逻辑（空或 ≤0 视为未维护、跳过不计算），
现在触发计算会一条都刷不到。**这是数据待录入，不是代码 bug**。

维护后才能真正验证数值正确性。

### 4. 一条工位地址对不上的真实数据问题

```
扫码：35452248 @ TH80-OLE1-07-A05-A06
映射里该物料已维护的地址：TH80-OLE-03-A03-A04、手工台-14-A01~A02、手工台-32-A03、手工台-32-B03、手工台-37-A01、手工台-37-B01
```

`OLE1` vs `OLE`、`07` vs `03` 两处都不同，**不是全角/半角问题，归一化救不了**。
要么料号映射缺这个工位的记录，要么工位二维码印错了。

排查方式：拿该二维码扫一次，新版报错会列出该物料已维护的全部总装地址，对照即可判断补哪边。

### 5. 工位用料表（m_station_material）为 0 行

`resolveStation()` 查不到工位补充信息时会走兜底。不影响仓库代号选择（那个走料号映射），
但工位名称等字段会退回默认值。需确认是否要维护这张表。

### 6. 物料主数据（m_material）为 0 行

扫码预览取单位时 `findMaterialUnit()` 查不到，会退回默认「个」。
标签 ZPL 是代码里硬编码生成的，不依赖 `m_label_template`（该表也是 0 行），所以打印不受影响。

---

## P2 · 次要与留档

### 7. t_weekly_plan_row.version 恒为 0

字段建了但从未调用 `setVersion`，冲突确认弹窗里「库内版本」永远显示 0，形同虚设。
不影响功能（冲突判定靠年份+周次+工厂+8D号，不靠这个字段），哪次顺手再清理。

### 8. 周计划解析尚未用真实计划员文件验证

已用程序化往返测试验证通过（生成模板 → 填数 → 解析，含真日期单元格、合计行+签字区两个边界场景），
数量口径正确（14 班×10 = 140、7 白班×5 = 35）。但**没有用计划员手工填写的真实文件跑过**。

真实文件若列偏移、合并方式与样本不同，解析需微调。上线前建议实测一份。

### 9. 起始日未强制为周一

周计划模板页选哪天就从哪天连续算七天。若要防手滑选错，可加「必须为周一」校验。当前未加。

### 10. 多计划员独立账号未实现

指导书提到多个计划员各自独立账号密码以避免互相覆盖。当前靠「工厂隔离 + 冲突确认弹窗」兜底：
弋江与三山互不冲突，同厂内同 8D 号冲突时弹窗要求确认覆盖。独立账号体系尚未做。

---

## 附录 · 排查用 SQL

```sql
-- 1. quantity 是否已被周计划改写（对比同料号不同盒型的数值差异）
SELECT line_material_code, box_size, quantity, single_unit_usage
FROM m_material_mapping WHERE enabled=1
ORDER BY line_material_code, mapping_order LIMIT 20;

-- 2. 单根用量维护进度
SELECT COUNT(*) AS 总数, SUM(single_unit_usage IS NOT NULL) AS 已维护
FROM m_material_mapping WHERE enabled=1;

-- 3. 跨多工位的物料（联合匹配的必要性依据）
SELECT line_material_code, COUNT(DISTINCT delivery_address) addrs,
       GROUP_CONCAT(DISTINCT delivery_address SEPARATOR ' | ') AS 地址列表
FROM m_material_mapping WHERE enabled=1
GROUP BY line_material_code HAVING addrs>1 ORDER BY addrs DESC LIMIT 10;

-- 4. 扫码地址匹配不上映射的组合（找数据问题）
SELECT s.material_code, s.station_code,
 (SELECT GROUP_CONCAT(DISTINCT m.delivery_address SEPARATOR ' | ')
  FROM m_material_mapping m WHERE m.enabled=1 AND m.line_material_code=s.material_code) AS 映射里的地址
FROM log_scan s
WHERE s.material_code IS NOT NULL AND s.station_code IS NOT NULL
GROUP BY s.material_code, s.station_code
HAVING (SELECT COUNT(*) FROM m_material_mapping m
        WHERE m.enabled=1 AND m.line_material_code=s.material_code
          AND UPPER(REPLACE(REPLACE(m.delivery_address,'～','~'),' ','')) =
              UPPER(REPLACE(REPLACE(s.station_code,'～','~'),' ',''))) = 0;

-- 5. 周计划数据量核对
SELECT 't_weekly_plan_batch' t, COUNT(*) c FROM t_weekly_plan_batch
UNION ALL SELECT 't_weekly_plan_row', COUNT(*) FROM t_weekly_plan_row
UNION ALL SELECT 't_weekly_plan_shift_qty', COUNT(*) FROM t_weekly_plan_shift_qty
UNION ALL SELECT 't_simple_bom', COUNT(*) FROM t_simple_bom;
```
