# DataScope 改造文档索引

> 状态：DataScope `0.9.2` 已紧急回退，生产恢复旧版应用；Expand Schema 保留
> 整理日期：2026-09-01
>
> 代码核验基准：`7caa61a80f19630b14a45749a319de967f6ce67f`（当前工作区仍有未提交 DataScope 修改）
> 适用项目：`/opt/apps/material-pull`

## 1. 文档权威顺序

发生冲突时按以下顺序处理，不自行猜测：

1. 当前代码、Schema、运行环境事实冲突：重新只读核验，并更新 `02_当前代码核验基线.md`。
2. 业务归属、治理规则、技术选择冲突：只以 `03_业务归属与决策冻结.md` 中明确状态为准。
3. 安全原则冲突：以 `01_DataScope总体设计与安全规范.md` 为硬约束。
4. 实施顺序冲突：只以 `04_DataScope实施改造计划.md` 为主线。
5. Migration、索引、历史回填细节：以 `05_数据库迁移与历史数据.md` 为准。
6. 测试、验收、发布证据和回滚门禁：以 `06_测试验收发布与回滚.md` 为准。
7. AI/Codex 的执行方式：以 `07_AI执行规范.md` 为准，但不得覆盖 `01～06`。

## 2. 正式文档

| 文件 | 唯一职责 |
|---|---|
| `01_DataScope总体设计与安全规范.md` | 稳定目标、安全不变量、授权边界 |
| `02_当前代码核验基线.md` | 当前代码/Schema/数据事实和已确认缺口 |
| `03_业务归属与决策冻结.md` | 唯一决策源：已明确、已确认、待批准、待定 |
| `04_DataScope实施改造计划.md` | 唯一实施阶段主线 |
| `05_数据库迁移与历史数据.md` | Schema、Migration、索引、历史候选、回填、数据库回滚 |
| `06_测试验收发布与回滚.md` | 验收证据、发布门禁、上线监控、回滚 |
| `07_AI执行规范.md` | AI/Codex 每轮如何读取、修改、验证和停止 |

## 3. 当前执行入口

| 文件 | 用途 |
|---|---|
| `进度总结.md` | 当前完成度、已验证证据、未完成门禁和下一步 |
| `账号范围规划表.md` | 旧账号临时策略、新增账号与配送区域的唯一业务清单 |
| `新增账号SQL脚本.sql` | 已确认新增账号的受控初始化脚本；隔离验证已通过，生产使用仍需发布批准 |
| `阶段14_发布就绪评估.md` | 是否允许生产发布的唯一结论、阻塞、候选构建和解锁路径 |

## 4. 当前阶段

当前处于 **阶段 14：生产回退后稳定性确认**。

历史数据策略已冻结：历史未归属范围保持 `NULL`、不回填；普通范围账号不可见，ADMIN 可见。若未来改变策略，必须重新审批、备份和演练。

生产发布结论与解锁路径见 `阶段14_发布就绪评估.md`；用户已批准开始生产部署，执行仍必须遵循备份、受控重启、健康检查和回滚流程。

允许继续做：
- 隔离环境的 Migration、认证、Redis、WebSocket、客户端和性能验证；
- 只读核验生产数据统计；
- 以受控只读脚本生成生产历史归属候选，供业务审批；
- 修正文档事实，准备候选与回滚演练；已构建的候选仅可用于版本冻结与评审，不得直接部署。

不允许执行生产 Migration 或历史回填；应用生产部署和重启已获用户批准，但必须按受控流程执行。

当前关键阻塞只看 `03` 的“决策冻结总表”，不再维护单独的 123 项问卷。

## 5. 已移出正式文档集的内容

`DataScope_全部待确认事项_20260828.txt` 不再作为正式实施依据。其作用原本是收集确认后回填 `03`，继续并行维护会造成两套决策源。未解决事项已收敛到 `03`。

原 INDEX 中重复抄写的 DataScope 模型、代码基线摘要和 15 阶段清单已删除；对应内容分别只保留在 `01`、`02`、`04`。

以下历史/重复报告已删除，不再作为事实或执行依据：

- `Migration准备报告.md`：仅覆盖早期 `V0.9.3～V0.9.8`，且错误描述为 Flyway 自动执行；现由 `05` 和实际 SQL 文件取代。
- `SQL查询脚本.md`：与当前只读候选/结构核验 SQL 重复，且含基于旧 Schema 的查询；现由 `backend/src/main/resources/db/mysql/DATASCOPE_*.sql` 取代。
- `今日工作总结.md`、`开发进度.md`、`总体开发进度汇报.md`：早期 30%/35% 进度快照，与当前状态冲突；现统一为 `进度总结.md`。
