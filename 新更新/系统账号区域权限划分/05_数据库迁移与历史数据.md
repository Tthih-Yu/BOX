# 数据库迁移与历史数据

> 职责：只管理 Schema、Migration、索引、历史候选、回填和数据库级恢复。  
> 对象范围类型和字段语义只从 `03` 读取；整体实施顺序只看 `04`；发布验收只看 `06`。

## 1. 数据库原则

- MySQL 和 H2 分开维护兼容 Migration。
- Schema、索引、历史数据候选/回填分离。
- 生产不依赖 `ddl-auto=update`。
- 不覆盖已经执行的 Migration 版本。
- 新增范围列在 Expand 阶段优先可空并向后兼容。
- 未经过历史质量验证前不直接加 NOT NULL。
- 历史归属只生成候选，不用区域名称、默认值或第一条 Mapping 猜测。
- 任何生产写操作前创建并验证当次新备份。

## 2. 已知基础

项目没有启用可自动执行这些 SQL 的 Migration 运行器；MySQL Profile 使用 `ddl-auto=validate`。因此所有生产 Migration 必须在备份、只读结构预检和人工批准后显式执行，不能通过重启应用触发。

基线仓库已存在：

```text
V0.9.1__mapping_factory.sql
V0.9.2__task_print_factory.sql
idx_task_factory_created(factory, created_at)
idx_print_factory_created(factory, created_at)
```

实施前必须核验实际 MySQL `INFORMATION_SCHEMA`（或等价结构）；实体字段不能证明生产库已执行 Migration。

### 隔离环境实测（2026-08-31）

- 已从已验证备份恢复独立 MySQL 测试库；恢复结果已包含 `V0.9.1` 的 Mapping 工厂字段，因此按真实历史状态继续执行 `V0.9.2～V0.9.8`，全部成功。
- 只读结构核验确认 20 个范围及前置字段、`sys_user_delivery_area`、工厂字典和 17 个配送区域均存在。
- `V0.9.4` 初稿的五个 `utf8mb4` 业务键复合唯一索引超过 InnoDB 3072 字节限制；已改用完整业务键 SHA-256 生成列作为唯一索引，不截断业务字段，且保留历史 NULL 可并存语义。
- `V0.9.3` 字典及关系表已显式使用 `utf8mb4_unicode_ci`，消除与既有表比较时的排序规则冲突。
- 隔离 EXPLAIN 发现 Task 范围列表缺少复合索引，已新增独立的 `V0.9.9`：Task 的 `(factory, delivery_area, created_at)`、`(factory, delivery_area, status, created_at)`，以及 PrintJob 的 `(factory, delivery_area, created_at)`。保留原 Migration 不改写；等量数据性能仍待验收。
- `V0.9.10` 新增 `log_user_scope`：隔离库执行及结构核验通过；只审计账号角色、工厂、配送区域、启停状态、操作者与 TraceId，不保存密码、密码哈希或会话 Token。
- 新增账号脚本首次执行创建 5 个账号、17 条区域关联和 5 条审计，重复执行为零修改。脚本已显式统一 `utf8mb4_unicode_ci`，并避免 MySQL 临时表重复打开限制。
- 已演练删除新增账号并回滚 `V0.9.3～V0.9.10`：DataScope 表/列清零，780 条 Task 保留；随后从已校验备份恢复，20 个字段、5 个账号、17 条关联、5 条审计及 780 条 Task 全部恢复。原始 dump 只因 MySQL 补写等价 `CHARACTER SET utf8mb4` 文本而不同，移除该等价声明后的 SHA-256 完全一致。
- 验收证据保存在 `.runtime/datascope-test-results/final-acceptance-20260831/`。此结论只适用于隔离库；生产发布前仍必须重新做只读预检和当次备份验证。

早期的 `Migration准备报告.md` 与 `SQL查询脚本.md` 已删除：Migration 状态以本文件和 `backend/src/main/resources/db/mysql/` 下的实际 SQL 为准；结构核验、历史候选和回填分别使用同目录的 `DATASCOPE_schema_verify.sql`、`DATASCOPE_history_audit_candidates.sql` 及 `DATASCOPE_history_backfill_*.sql`。

生产预检使用 `deploy/scripts/datascope-production-readonly-preflight.sh`，它要求精确确认字符串、从 systemd 环境文件读取连接配置、用 `SET SESSION TRANSACTION READ ONLY` 执行 `SELECT`，不执行 Migration 或写入。由具备 sudo 权限的运维人员运行：

```bash
cd /opt/apps/material-pull
sudo sh -c 'DATASCOPE_PRODUCTION_READONLY_CONFIRM=I_UNDERSTAND_PRODUCTION_READ_ONLY \
  exec /opt/apps/material-pull/deploy/scripts/datascope-production-readonly-preflight.sh'
```

脚本只显示数据库名、结构缺项、字典/索引统计、候选新增账号冲突摘要、历史空范围计数和备份完整性/新鲜度（默认 24 小时）；不打印密码、Token 或密码哈希。2026-09-01 已扩展为核验 V0.9.3～V0.9.10 的所有关键表、字段、Inventory 生成列、20 个范围索引与 `log_user_scope` 审计表。预检通过不等于批准执行生产 Migration 或应用部署。

生产当次备份使用 `deploy/scripts/datascope-production-backup.sh`。该脚本要求独立确认、先核对可用磁盘空间，再以 `mysqldump --single-transaction --quick` 导出到受限权限临时文件，完成 gzip 校验后原子改名并记录 SHA-256；不删除历史备份。具备 sudo 权限的运维人员运行：

```bash
sudo sh -c 'DATASCOPE_PRODUCTION_BACKUP_CONFIRM=I_UNDERSTAND_PRODUCTION_BACKUP \
  exec /opt/apps/material-pull/deploy/scripts/datascope-production-backup.sh'
```

仅在输出“备份完成”和“校验文件”后，才可把该备份作为后续受控 Migration 的回滚点。

首个生产 Migration 使用 `deploy/scripts/datascope-production-migrate-v0.9.3.sh`。脚本只接受 `V0.9.3` 的精确确认，校验 SQL SHA-256 和“字段/表均不存在”的全新前置状态，执行后核验 1 个弋江工厂、17 个区域、0 个新增账号并保存回执；它拒绝重跑、拒绝执行其他版本，也不会重启应用。具备 sudo 权限的运维人员运行：

```bash
sudo sh -c 'DATASCOPE_PRODUCTION_MIGRATION_CONFIRM=EXECUTE_V0_9_3 \
  exec /opt/apps/material-pull/deploy/scripts/datascope-production-migrate-v0.9.3.sh'
```

成功后必须观察旧应用；未取得后续明确授权时，不得执行下一批 Migration。经预检和评审后，可对连续版本授予一次性、顺序执行且失败即停的批次授权。

### 生产只读预检与 V0.9.3 执行（2026-09-01）

- 连接目标已确认是 `material_pull`（MySQL 8.0.46）；未执行任何写入。
- V0.9.1/V0.9.2 的 `m_material_mapping.factory`、`t_replenishment_task.factory`、`t_print_job.factory` 均存在，可作为后续 Expand Schema 的前置条件。
- 预检时 `sys_factory`、`sys_delivery_area`、`sys_user_delivery_area` 均不存在，确认 V0.9.3 尚未执行。
- `500001～500005` 均不存在，无新增账号同名冲突。
- 原最近备份 `material_pull-20260812-173016.sql.gz` 已过期；随后已创建当次备份 `material_pull-datascope-preflight-20260901-095850.sql.gz`，脚本完成一致性导出、gzip 校验，并生成同名 `.sha256` 校验文件。该备份是后续受控 Migration 的回滚点。
- 在明确授权后，`V0.9.3__datascope_user_scope.sql` 于 10:03 成功执行；回执为 `db-backups/datascope-v0.9.3-20260901-100300.receipt.txt`（权限 `0600`）。执行后确认弋江工厂 1 条、配送区域 17 条、新增账号 0 条；未改现有账号、未执行 V0.9.4～V0.9.10、未重启应用。
- 在明确授权后，`V0.9.4__datascope_inventory.sql` 于 10:07 成功执行；执行前 `t_inventory` 为 0 行、约 49 KiB，执行后历史库存范围仍为 0 条。回执为 `db-backups/datascope-v0.9.4-20260901-100712.receipt.txt`（权限 `0600`）；未执行 V0.9.5～V0.9.10、未重启应用。
- 在明确授权后，`V0.9.5__datascope_box_label.sql` 于 10:11 成功执行；执行前 `t_box`、`t_label` 均为 0 行，执行后历史 Box/Label 范围均为 0 条。回执为 `db-backups/datascope-v0.9.5-20260901-101135.receipt.txt`（权限 `0600`）；未执行 V0.9.6～V0.9.10、未重启应用。
- 在明确的一次性批次授权后，`V0.9.6～V0.9.10` 于 10:17 按顺序执行并逐项验证：执行前 `t_print_job` 4,455 条、`t_production_plan`/`t_material_demand`/`t_purchase_requirement` 均为 0 条、`log_scan` 4,423 条、`sys_outbox_event` 15,191 条、`t_replenishment_task` 4,393 条。V0.9.6/0.9.7/0.9.8 均确认未写入历史范围，V0.9.9 索引和 V0.9.10 空审计表均已验证。回执为 `db-backups/datascope-v0.9.6-v0.9.10-20260901-101709.receipt.txt`（权限 `0600`）；未创建账号、未回填历史数据、未重启应用。
- 发布候选冻结审计确认：当前 `V0.9.3～V0.9.10` 八个 SQL 的 SHA-256 均与受控执行脚本的锁定值一致。生产回执保持 `0600` 受限权限；后续结构修正只能新增版本，严禁改写这八个已执行文件。
- 最终生产只读预检已完成并通过：1 个工厂、17 个配送区域、20 个 DataScope 索引、Inventory 范围生成列和空的 `log_user_scope` 审计表均存在；`500001～500005` 无冲突，当次备份完整且在 24 小时内。全程未执行 Migration、账号写入、回填或应用操作。
- 同次预检确认历史空范围：Task `factory` 为空 848 条、`delivery_area` 为空 0 条；PrintJob `factory` 为空 845 条、`delivery_area` 为空 4,455 条。它们是 Expand Schema 未回填的预期状态，不是 Migration 故障。业务已确认保留历史 `NULL`、不回填：普通范围账号按 Fail Closed 不可见，ADMIN 可见；未来若改变策略，必须重新审批、演练、备份后再执行。
- 修正后的生产历史候选汇总已只读完成：4,415 条 Task 中 848 条待处理记录里，847 条均为 `DELIVERY_AREA_CONFLICT`、1 条为 `NO_CANDIDATE`；4,455 条 PrintJob 中，3,610 条可从完整 Task 自动继承、843 条因对应 Task 范围不完整而等待、2 条找不到 Task。Task→Mapping 关联为 4,408/4,415（99.84%），PrintJob→Task 为 4,453/4,455（99.96%）。业务已决定不回填，以上记录继续保持历史 `NULL`；报告保留作审计证据，不得作为 UPDATE 输入。

先使用 `deploy/scripts/datascope-production-history-summary.sh` 输出不含业务编号的完整度、候选状态数量和关联覆盖率；它要求精确确认、从 root-only 环境文件读取连接，并以 `SET SESSION TRANSACTION READ ONLY` 执行。需要逐条审批时才使用 `deploy/scripts/datascope-production-history-audit.sh`：该脚本拒绝包含 DML/DDL 的候选 SQL，并把报告写入 `db-backups/datascope-production-history-audit-*.txt`（权限 `0600`）。报告可能含业务编号，只可在本机受限查看，不纳入 Git 或发布包。两个脚本都不会执行 Migration、账号写入或历史回填。
- 2026-09-01 10:46 已生成生产逐条候选报告：`db-backups/datascope-production-history-audit-20260901-104619.txt`（脚本设为 `0600`）。报告仅供人工核对 847 条区域冲突 Task、1 条无候选 Task 及下游 PrintJob 候选，未执行任何写入。

## 3. Expand Schema

基础账号结构：

```text
sys_user.factory
sys_user_delivery_area(user_id, delivery_area)
UNIQUE(user_id, delivery_area)
```

`sys_factory`、`sys_delivery_area` 及各业务对象范围字段，只按 `03` 当前已批准/已确认内容生成正式 DDL。

Expand 阶段禁止：
- 直接执行历史全表 UPDATE；
- 未验证数据质量就加 NOT NULL；
- 把未决范围类型写死进 DDL；
- 通过修改旧 Migration 伪造升级历史。

## 4. 字典数据

按 `03` 已确认结构准备：
- factory_code / factory_name；
- area_code / area_name；
- factory 与 area 归属；
- enabled / display_order；
- 必要的历史别名/规范化映射。

数据处理：
- 去除前后空格；
- 比较使用统一大小写规范；
- 停用值禁止新业务使用；
- 历史原值原则上保留，必要时用映射解释，不静默改写。

真实初始工厂、区域和现有账号映射在执行前从 `03` 与实际数据库共同核验。

## 5. 索引策略

不为所有表复制同一个 DataScope 索引。索引必须根据最终 SQL、数据分布和 `EXPLAIN` 设计。

Task 候选方向示例：

```text
(factory, delivery_area, created_at)
(factory, delivery_area, status, created_at)
(factory, delivery_area, print_generated, status)
```

索引评估至少考虑：等值列、area IN 集合、status 选择性、ORDER BY、数据基数、查询频率、写入成本。

SimpleBOM 等大批量导入表需单独评估索引维护成本。

## 6. 历史数据基线

2026-09-01 最终生产只读预检与候选汇总：

```text
Task total                4,415
Task factory IS NULL        848
Task delivery_area IS NULL     0
Task DELIVERY_AREA_CONFLICT   847
Task NO_CANDIDATE                1
PrintJob total             4,455
PrintJob factory IS NULL     845
PrintJob delivery_area IS NULL 4,455
PrintJob AUTO_CANDIDATE     3,610
PrintJob TASK_SCOPE_INCOMPLETE 843
PrintJob TASK_NOT_FOUND         2
Task → Mapping coverage   4,401/4,408 (99.84%)
PrintJob → Task coverage  4,453/4,455 (99.96%)
```

候选解释：
- 847 条 Task 虽命中唯一的启用 Mapping `(warehouse_code, material_code)`，但候选区域与 Task 已有 deliveryArea 冲突；按最终策略保留历史值/NULL，不用于 UPDATE。
- 1 条无候选 Task 及 2 条无 Task 的 PrintJob 必须人工决定保留未归属、补业务依据或其他受控处置；禁止默认区域或取第一条 Mapping。
- 843 条 PrintJob 只是在其 Task 完成范围确认后才可能成为继承候选；PrintJob 不得绕过 Task 单独猜测范围。

以上不是生产更新数量；正式回填前必须在目标验证环境重新生成候选并审批。

## 7. 历史候选与审批流程

1. 重新统计 NULL、空串、非法 factory/area 和分布。
2. 先为 Task 生成候选，不 UPDATE；生产候选报告使用受控只读审计脚本生成。
3. 验证关联唯一性和关键业务字段。
4. 多候选、无候选、只能凭区域猜测的记录进入人工清单。
5. 业务审批 Task 候选。
6. Task 确认后再生成 PrintJob 候选。
7. 从属对象按 `03` 已批准的权威来源生成候选。
8. 每条候选保存原值、新值、依据、审批人、批次号。
9. 在与生产同版本的验证环境演练。
10. 生产按批准批次执行并逐批校验。
11. 未确认记录保持未归属，仅 ADMIN 可见。

明确禁止：

```text
delivery_area = 1  => 猜归属
LP                 => 猜归属
Mapping 第一条     => 猜归属
直接全表 UPDATE
```

## 8. 导入与范围的数据库约束

普通账号：后端使用可信 factory；area 必须属于允许集合；文件不能扩大授权。

ADMIN：必须明确目标范围或从文件解析并校验合法范围；激活/覆盖前再次验证；冲突检测和唯一键只在批准的业务范围内计算。

数据库唯一约束是否包含范围维度，以 `03` 业务唯一键决策和真实冲突数据为依据，不能仅因“做 DataScope”机械加列。

## 9. Contract 阶段

仅在安全切换完成、数据质量稳定且独立评审通过后考虑：
- NOT NULL；
- 更严格 CHECK/UNIQUE/FK；
- 清理废弃字段或兼容逻辑。

Contract 不是 Expand 的顺手清理项，必须独立评审和演练。

## 10. 数据库恢复

必须分别准备：
- Schema 保留/回退策略；
- 历史回填反向恢复脚本；
- 数据校验 SQL；
- 批次级原值记录和备份位置。

原则：
- 向后兼容 Expand Schema 出现应用问题时通常优先回滚应用并保留新 Schema；是否删除列/表必须以验证环境演练为准。
- 历史回填异常立即停止后续批次，按原值/备份恢复已执行批次。
- 不在正式方案中固化未经演练的 DROP/ALTER 回滚语句。
