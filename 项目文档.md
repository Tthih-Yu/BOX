# 物料拉动系统 · 项目总文档

> 本文档由原 `README.md`、`Ubuntu部署方案_整合版.md`、`项目总文档.md`、`项目文档.md` 四份文档整合重写而成，是了解、部署、运维本系统的**唯一入口文档**。
> `新数据库.md` 是约 872 行料号映射的纯数据表（导入数据，非说明文档），其用途见「附录 B · 数据资产」，不并入本文。

工厂物料拉动 / 补货配送系统。覆盖真实工厂标签、扫码拉动、任务流转、库存一致性、导入校验、AGV / 外部接口，以及斑马标签自动打印。

系统由三部分组成：

- **后端**：Java 17 + Spring Boot 3.3.2（部署在 Ubuntu 服务器）
- **前端**：Vue 3 + TypeScript + Vite（Web 管理端，构建为静态文件由 Nginx 托管）
- **打印代理**：Windows C# 托盘程序（连 USB 斑马打印机的电脑上运行，自动打印标签）

另有安卓扫码端（Kotlin + Jetpack Compose），通过 `/api/scan/*` 接口与后端交互。

> 版本说明：后端 `pom.xml` 中 `version=0.8.4`，打包产物为 `material-pull-system-backend-0.8.4.jar`。文档中出现的 V0.8.5 / V0.8.6 是「生产化改造」的功能阶段代号（见第 10 章），pom 版本号未随之升位，以 jar 实际文件名为准。

---

## 目录

- [第 1 章　系统简介与当前部署状态](#第-1-章系统简介与当前部署状态)
- [第 2 章　快速开始（本地开发）](#第-2-章快速开始本地开发)
- [第 3 章　环境要求](#第-3-章环境要求)
- [第 4 章　配置文件 material-pull.env](#第-4-章配置文件-material-pullenv)
- [第 5 章　部署方式 A：脚本 / systemd（传统 Ubuntu，本机在用）](#第-5-章部署方式-a脚本--systemd传统-ubuntu本机在用)
- [第 6 章　部署方式 B：Docker（迁移新设备首选）](#第-6-章部署方式-bdocker迁移新设备首选)
- [第 7 章　★ 网络与 80 端口映射（核心专章）](#第-7-章--网络与-80-端口映射核心专章)
- [第 8 章　生产环境要点](#第-8-章生产环境要点)
- [第 9 章　斑马标签自动打印（拉模式）](#第-9-章斑马标签自动打印拉模式)
- [第 10 章　技术说明（架构 / 目录 / 接口 / 安全）](#第-10-章技术说明架构--目录--接口--安全)
- [第 11 章　生产化改造历程](#第-11-章生产化改造历程)
- [第 12 章　已知问题与风险](#第-12-章已知问题与风险)
- [第 13 章　日常运维速查](#第-13-章日常运维速查)
- [第 14 章　首次上线检查清单](#第-14-章首次上线检查清单)
- [附录 A · 常用命令汇总](#附录-a--常用命令汇总)
- [附录 B · 数据资产（新数据库.md）](#附录-b--数据资产新数据库md)
- [附录 C · 数据库可视化工具（dbtool）](#附录-c--数据库可视化工具dbtool)
- [附录 D · 配套文档索引](#附录-d--配套文档索引)

## 第 1 章　系统简介与当前部署状态

### 1.1 系统做什么

面向汽车线束工厂的物料拉动（看板补货）系统。核心闭环：

1. 工位物料盒贴二维码（内容是物料号，可扩展为 `物料号,工位,使用/备用`）。
2. 现场员工用安卓 App 或 Web 扫码，后端按「料号映射表」把物料号换算成仓库代号、盒子大小、数量。
3. 仓库后台生成补货任务，打印仓库条形码标签（条码=仓库代号）。
4. 仓库员工扫条形码拣料并配送（可对接 AGV），最后收货确认闭环，空盒回收。

围绕主流程还有：A/B 双盒轮换、库存锁定/扣减、周转盒池、标签中心、扫码规则、PPC/MRP 计划、SAP/IMS 接口、大屏看板、健康检查与维护恢复、用户角色权限等模块。

### 1.2 当前本机部署状态（已上线）

系统已在本机（Ubuntu）以「脚本 / systemd」方式落地运行，对外经 Nginx 80 端口。当前组件与端口：

| 组件 | 说明 | 端口 |
|---|---|---|
| Nginx | 对外唯一入口，提供前端页面 + 反代 `/api` 到后端 | 80 |
| 后端 Spring Boot | 业务后端，仅本机监听 | 127.0.0.1:8080 |
| MySQL | 数据库 | 127.0.0.1:3306 |
| 前端 Vue | 已构建为静态文件，由 Nginx 提供 | （`frontend/dist`） |
| 地址播报 | IP 变化时更新访问地址文件 | — |
| dbtool | 数据库可视化工具（见附录 C） | 8899 |

访问链路（前后端同源，无跨域问题）：

```text
各网段用户浏览器
  → http://<本机IP>/          (80 端口)
  → Nginx
      ├─ /              → 前端静态页面 (frontend/dist)
      └─ /api/、/api/ws → 反代 127.0.0.1:8080 (后端)
```

### 1.3 如何访问

1. 打开家目录的 `物料拉动系统-访问地址.txt`，里面是当前访问地址（形如 `http://10.242.36.66`）。
2. 用浏览器访问该地址即可打开系统。
3. 管理员默认账号 `admin`，初始密码见 `deploy/scripts/material-pull.env` 中的 `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD`。

> 同公司不同网段的设备均可直接访问，无需经 Windows Server 中转。为什么必须走 80 端口、换 IP 如何应对，见第 7 章。

## 第 2 章　快速开始（本地开发）

本地开发最简单：后端默认用 H2 文件数据库，不必先装 MySQL。需要两个终端。

第一个终端启动后端：

```bash
cd backend
export MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD='Admin123456'
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

第二个终端启动前端：

```bash
cd frontend
npm install
npm run dev
```

浏览器访问 `http://localhost:5173`，登录：

```text
用户名：admin
密码：Admin123456
```

首次管理员密码取自 `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD`，要求不少于 10 位且同时含字母和数字。登录成功后应删除该环境变量并重启，后续用户在「系统管理 → 用户角色」维护。

本地关键地址：

```text
前端         http://localhost:5173
后端 API     http://localhost:8080/api
Swagger      http://localhost:8080/api/swagger-ui.html   （仅 dev）
H2 控制台    http://localhost:8080/api/h2-console          （仅 dev）
```

局域网访问：前端用 `npm run dev:lan`，`hostname -I` 查本机 IP，其他电脑访问 `http://<本机IP>:5173`。防火墙如拦截，`sudo ufw allow 5173/tcp` 与 `sudo ufw allow 8080/tcp` 放行。

---

## 第 3 章　环境要求

服务器（Ubuntu）：

- Java 17（`openjdk-17-jdk`）
- Node.js 20 + npm
- Maven（用脚本 / 源码方式构建时需要；Docker 方式不需要本机 Maven）
- 可选：MySQL 8（生产库），Redis（多实例集群时）

从零安装基础环境：

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y openjdk-17-jdk maven
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
java -version    # 期望 17
node -v          # 期望 20.x
```

打印电脑（Windows，连 USB 斑马打印机）：

- Windows 10 1803+ / Windows 11（自带 .NET Framework 4.7.2）
- 已装斑马 GT800 驱动，能在「设备和打印机」看到打印机

国内网络加速（可选）：

- Maven：在 `~/.m2/settings.xml` 配阿里云镜像 `https://maven.aliyun.com/repository/public`。
- npm：`npm config set registry https://registry.npmmirror.com`。

---

## 第 4 章　配置文件 material-pull.env

脚本 / systemd 部署方式的所有参数集中在一个环境文件里。首次部署先复制模板：

```bash
cd deploy/scripts
cp material-pull.env.example material-pull.env
```

关键项：

| 变量 | 说明 |
| --- | --- |
| `PROJECT_ROOT` | 项目根目录，一般不用改 |
| `RUN_USER` | 运行用户，安装自启脚本会自动写入当前用户 |
| `JAVA_HOME` | Java 17 路径 |
| `SPRING_PROFILE` | `dev`（H2 文件库，开箱即用）/ `mysql`（生产库） |
| `BACKEND_JAR` | 后端 jar 名，随版本更新 |
| `MATERIAL_PULL_EXTERNAL_API_KEY` | 机器密钥：安卓扫码设备、打印代理、外部系统回调共用，**生产务必改成强随机值** |
| `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD` | 首次部署创建管理员用，成功后建议删掉 |
| `FRONTEND_MODE` | `dev:lan`（局域网可访问）/ `dev`（仅本机） |

生成 external-api-key（自己在服务器上生成的一串随机字符串，不需要去哪申请）：

```bash
openssl rand -base64 32
```

把输出填到 `MATERIAL_PULL_EXTERNAL_API_KEY=`，同一串之后要填进打印代理界面。

> Docker 部署方式用的是项目根目录的 `.env`（模板 `.env.example`），与这里的 `material-pull.env` 是两套，别混淆。见第 6 章。

## 第 5 章　部署方式 A：脚本 / systemd（传统 Ubuntu，本机在用）

本机当前采用的就是这套方式。

### 5.1 脚本启动（开发 / 调试推荐）

```bash
cd deploy/scripts
bash start.sh            # 同时启动前后端
bash start.sh backend    # 只启后端
bash start.sh frontend   # 只启前端
bash status.sh           # 查看状态
bash stop.sh             # 停止
```

启动后：前端 `http://<服务器IP>:5173`，后端 `http://<服务器IP>:8080/api`。

### 5.2 systemd 开机自启（长期运行，本机在用）

系统已配置为开机自动启动，重启机器后无需任何手动操作。启动顺序：

```text
开机 → mysql → 后端(8080) → nginx(80) → 地址播报
```

本机开机自启服务清单：

| 服务 | 作用 | 自启 |
|---|---|---|
| `mysql` | 数据库 | enabled |
| `material-pull-backend` | 后端 | enabled |
| `nginx` | 80 端口入口 + 托管前端静态页 | enabled |
| `material-pull-url-announcer` | 访问地址播报 | enabled |
| `material-pull-dbtool` | 数据库可视化工具（附录 C） | enabled |

一次性核对全部是否都已启用：

```bash
systemctl is-enabled mysql nginx material-pull-backend material-pull-url-announcer material-pull-dbtool
```

> 说明：`material-pull-frontend-dev` 显示 disabled 属**正常**。前端由 nginx 直接托管构建好的静态文件（`frontend/dist`），不需要 dev 热更新服务；该服务仅供开发调试用，生产不开。

常用命令：

```bash
sudo systemctl status material-pull-backend
sudo journalctl -u material-pull-backend -f
sudo systemctl restart material-pull-backend
```

### 5.3 更新部署

**更新前端**（改了前端代码后）：

```bash
cd /opt/apps/material-pull/frontend && npm run build
# dist 更新后 Nginx 立即生效，无需重启
```

**更新后端**（改了后端代码后）：

```bash
cd /opt/apps/material-pull/backend && mvn clean package -DskipTests
sudo systemctl restart material-pull-backend
```

### 5.4 从零 / 重装机器部署

在一台干净的 Ubuntu 上从零部署：

```bash
# 1. 构建前端
cd /opt/apps/material-pull/frontend && npm run build
# 2. 装后端开机自启（含 mysql profile；需 root）
sudo bash /opt/apps/material-pull/deploy/scripts/install-autostart.sh
# 3. 装 Nginx 80 入口（需 root）
sudo bash /opt/apps/material-pull/deploy/scripts/setup-nginx-gateway.sh
# 4. 装访问地址播报（需 root）
sudo cp /opt/apps/material-pull/deploy/systemd/material-pull-url-announcer.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now material-pull-url-announcer.service
```

数据库连接、初始管理员密码、CORS 等配置见 `deploy/scripts/material-pull.env`。

### 5.5 生产（MySQL + systemd + Nginx）手动部署概要

1. 装 MySQL、Nginx：`sudo apt install -y mysql-server nginx` 并 enable/start。
2. 建库建用户（生产替换强密码）：

```sql
CREATE DATABASE material_pull DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'material_pull'@'localhost' IDENTIFIED BY '改成强密码';
GRANT ALL PRIVILEGES ON material_pull.* TO 'material_pull'@'localhost';
FLUSH PRIVILEGES;
```

3. 首次建库导入升级 SQL：`mysql -u material_pull -p material_pull < docs/sql/V0.8.4__production_upgrade.sql`。
4. 打包 jar 放到部署目录，复制 `deploy/systemd/*.service` 到 `/etc/systemd/system/`，配好环境变量后 `daemon-reload && enable --now`。
5. 前端 `npm run build`，nginx 站点指向 `frontend/dist`，`nginx -t && systemctl reload nginx`。
6. 管理员首次登录成功后，从配置删除 `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD` 并重启。

## 第 6 章　部署方式 B：Docker（迁移新设备首选）

项目已容器化。一台装了 Docker 的机器一条命令即可起整套系统。Docker 方式用根目录 `.env`（模板 `.env.example`），前端 nginx 会把 `/api` 反代到后端容器，前后端同源、无需额外跨域配置。

### 6.1 Ubuntu 24.04 一键部署

全新机器把整个项目目录拷过去后：

```bash
cd IE
sudo bash deploy/scripts/ubuntu-setup.sh            # H2 文件库，开箱即用
sudo bash deploy/scripts/ubuntu-setup.sh --mysql    # MySQL（生产推荐，首发自动建表）
sudo bash deploy/scripts/ubuntu-setup.sh --offline  # 工厂无外网：先在联网机 offline-images.sh save 再拷过来
```

脚本会：从 Docker 官方源装 Docker Engine + Compose 插件并把当前用户加入 docker 组；无 `.env` 则从模板生成并自动给 `MATERIAL_PULL_EXTERNAL_API_KEY` 填强随机值（权限 600）；`docker compose up -d --build` 构建启动并打印访问地址与管理员账号。

### 6.2 手动分步

```bash
cd IE
cp .env.example .env
# 至少改 MATERIAL_PULL_EXTERNAL_API_KEY 和管理员密码
docker compose up -d --build
```

访问 `http://<服务器IP>`（默认 80 端口），登录 `admin` / `.env` 里的 `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD`。端口冲突时在 `.env` 改 `HTTP_PORT=8088`。

Docker 相关文件结构：

```text
docker-compose.yml        编排（含 mysql/redis/backup 可选 profile）
.env.example              环境变量模板
backend/Dockerfile        后端多阶段构建（支持 MAVEN_MIRROR_URL）
frontend/Dockerfile       前端多阶段构建（支持 NPM_REGISTRY）
frontend/nginx.conf       前端 nginx + /api 反代（HTTP）
frontend/nginx-tls.conf   HTTPS / TLS 版
deploy/mysql/init/        MySQL 首次初始化（字符集）
deploy/scripts/           备份/恢复/自签证书/离线镜像脚本
```

### 6.3 数据持久化与迁移

数据在命名卷中，重建容器不丢：`backend-data`（H2）、`mysql-data`（MySQL）。卷名前缀 `ie_` 来自目录名 `IE`，目录名不同用 `docker volume ls` 查。

- 整库迁移：拷整个 `IE/` 目录到新机器再 `docker compose up -d --build`；业务数据用下面备份方式带走。
- H2 数据带走：
```bash
docker run --rm -v ie_backend-data:/data -v "$PWD":/backup alpine \
  tar czf /backup/backend-data.tar.gz -C /data .
```
- MySQL 数据带走：
```bash
docker exec material-pull-mysql sh -c \
  'exec mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" material_pull' > dump.sql
```

## 第 7 章　★ 网络与 80 端口映射（核心专章）

> 本章整合自原 `Ubuntu部署方案_整合版.md`，是本系统能对外访问的关键。**为什么必须用 80 端口、换 IP 怎么办、排查证据、回退方式**全部在这里。整理日期 2026-07-18，状态：已解决并上线。

### 7.1 结论速览（TL;DR）

- **根因**：Windows 与 Ubuntu 跨网段，中间一台公司有状态安全设备**按目标端口白名单拦截**——标准 Web 端口 **80 放行**，非标准端口（8000/8443/8080 等）在应用数据传输阶段一律注入 RST 掐断。与方向、是否加密无关。
- **最终架构**：Ubuntu 本机装 Nginx 监听 **80**，前端静态文件 + `/api` 同源反代到本机 `127.0.0.1:8080` 后端。各网段用户直接访问 `http://<Ubuntu-IP>/`。
- **收益**：不经 Windows、不用隧道/TLS；前后端同源，**CORS 问题自然消失**。
- **抗 IP 变化**：服务端 `server_name _` + 反代 `127.0.0.1`，换 IP 零改动；开机自启的「访问地址播报」服务会在 IP 变化时自动把最新地址写到家目录 `物料拉动系统-访问地址.txt`。

### 7.2 最终架构

```text
各网段用户浏览器
  → http://<Ubuntu-IP>/        (80 端口，中间设备放行)
  → Ubuntu Nginx
      ├─ /              → 前端静态页面 (frontend/dist)
      └─ /api/、/api/ws → 反代 127.0.0.1:8080 (Spring Boot 后端)
```

原理：中间设备按目标端口白名单拦截，**只有 80 放行**。因此让 Ubuntu 自己用 Nginx 监听 80 对外，前端静态文件与后端 `/api` 全部同源经 80 出去，即可绕过拦截。无需 Windows 中转、无需隧道、无需 TLS。

相关文件（已随项目提供）：

- `deploy/nginx/material-pull.conf` — 原生 Nginx 配置（监听 80，反代 `127.0.0.1:8080`，`server_name _` 接受任意 IP/主机名）。
- `deploy/scripts/setup-nginx-gateway.sh` — 一键安装脚本（装 Nginx、部署配置、放行 dist 目录读取权限、测试并重载）。

### 7.3 部署与验证

```bash
# 1. 构建前端静态文件
cd /opt/apps/material-pull/frontend && npm run build
# 2. 启动后端（监听 127.0.0.1:8080）
bash /opt/apps/material-pull/deploy/scripts/start.sh backend
# 3. 安装并启用 Nginx 80 入口（需 root）
sudo bash /opt/apps/material-pull/deploy/scripts/setup-nginx-gateway.sh
```

验证：

```bash
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1/                   # 首页 200
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1/api/health/ready   # 反代 200
```

各网段用户浏览器访问 `http://<Ubuntu-IP>/`，用 `admin` / 见 env 的初始密码登录即可。

### 7.4 抗 IP 变化（换位置插网线）

这台机器可能换位置插网线导致 IP 变化。本方案对此免疫：

- **服务端零改动**：Nginx `server_name _` 接受任意 IP，反代目标是本机回环 `127.0.0.1:8080`，与外部 IP 无关。换 IP 后 Nginx / 后端都不用改。
- **地址自动更新**：`deploy/scripts/announce-access-url.sh` + `material-pull-url-announcer.service` 开机自启，检测到 IP 变化时（15 秒内）把最新访问地址写到家目录 `物料拉动系统-访问地址.txt`。

换位置后的唯一动作：打开 `物料拉动系统-访问地址.txt`，把里面的新地址发给使用者。

```bash
cat ~/物料拉动系统-访问地址.txt
```

> mDNS（`主机名.local`）已实测**跨网段不可用**（组播默认不跨三层网段 + 中间设备不转发），故不采用。若日后需要稳定域名，最优解是找 IT 在内网 DNS 分配固定域名。

### 7.5 排查证据（为什么定案是「按端口拦截」）

保留此节是为了日后 80 端口若也出问题时，能快速复用排查方法学。

**排查方法学**：`TcpTestSucceeded: True` 只证明 TCP 三次握手成功，不代表 HTTP 数据能完整传输——异常都发生在应用数据开始传输之后。有效方法是 Ubuntu 同时运行服务日志与 tcpdump，Windows 只发一次请求做受控测试。

**决定性证据（TTL 比对）**：路由修好后 HTTP 仍失败，抓包显示 GET 到达、Ubuntu 完整返回 200，但响应到达瞬间连接被 RST 掐断。

| 包 | 方向 | Flags | TTL |
|---|---|---|---|
| SYN / ACK / GET | Windows → Ubuntu | [SEW]/[.]/[P.] | **127** |
| RST | 伪装 Windows → Ubuntu | [R.] | **63** |

Windows 初始 TTL=128（观测 127），RST 初始 TTL=64（观测 63，Linux/BSD 内核特征）。两者初始 TTL 不同，**RST 绝非 Windows 本机发出**，而是中间一台独立网络设备伪装成客户端 IP 注入。

**分层受控测试**（Ubuntu 迁移到 10.242.36.66 后复测），规律高度一致：

| 测试 | 方向 | 端口 | TCP握手 | 应用数据 | 结果 |
|---|---|---|---|---|---|
| 明文 HTTP | Win→Ubuntu 入站 | 18000 | 通 | GET到达、返回200 | 响应被 RST |
| TLS | Win→Ubuntu 入站 | 18443 | 通 | ClientHello 发送时 | 被 RST |
| HTTP | Ubuntu→Win 出站 | 8080 | 通 | 等响应时 | 被 RST |
| HTTP | Ubuntu→Win 出站 | **80** | 通 | 完整 404 响应（连测3次稳定） | **✅ 放行** |
| HTTP | 各网段→Ubuntu | **80** | 通 | 完整页面 | **✅ 放行** |

**结论**：拦截既非按方向、也非按是否加密，而是**按目标端口白名单**。TLS 方案就此被证伪——加密的 ClientHello 在非标准端口上照样被掐。**只要服务走 80 端口即可绕过。** 该设备属公司网络基础设施，无法本地登录配置，因此在端点调整 IIS/安全软件/路由都无法改变其拦截行为。

> 排查期间还发现并修复过一个独立问题：Windows 侧去 Ubuntu 的流量偶尔走错网卡（NIC 2）导致「时通时断」，用临时 /32 路由 `route add <Ubuntu-IP> mask 255.255.255.255 10.243.111.1 metric 1 if 12` 修复（非持久，重启失效）。这与端口拦截是两个叠加的独立问题。

### 7.6 备用方案（当前未使用，留档）

若日后 80 端口也被封，或需经 Windows 统一入口：

- **方案 A（根本解法）**：向 IT 申请对指定会话有状态放行（申请模板见 7.8）。
- **反向隧道（FRP / SSH -R）**：Ubuntu 主动出站到 Windows。注意实测 Ubuntu→Windows 出站在非标准端口（8080）同样被 RST，采用前需先验证隧道端口未被拦并取得 IT 授权。

### 7.7 安全边界与回退

最终方案全部改动都在 Ubuntu 本机，未修改 Windows Server、IIS、公司网络设备或安全软件。Ubuntu 侧新增内容与回退方式：

- **Nginx（80 入口）**：`sudo systemctl disable --now nginx` 停用；`sudo apt remove --purge nginx nginx-common` 卸载。配置在 `/etc/nginx/sites-enabled/material-pull.conf`。
- **地址播报服务**：`sudo systemctl disable --now material-pull-url-announcer.service` 停用；删除对应 `/etc/systemd/system/*.service`。
- 当前对外入口为 **HTTP 明文 80**，内网可用；如需加密可另配 443（`frontend/nginx-tls.conf` 模板 + `gen-self-signed-cert.sh` 自签），但中间设备对 443 的放行需另行验证。
- 后端 8080 仅监听本机、经 Nginx 反代，未直接对外；`/api/actuator/` 已在 Nginx 层限制为内网网段可访问。

### 7.8 给 IT / 网络管理员的放行申请模板（方案 A）

```text
申请用途：
Windows Server 需将内部 Web 请求反向代理到 Ubuntu 主机。

现象：
10.243.111.152 → 10.243.129.77 的 TCP 目标端口，
TCP 握手与 HTTP 请求均可通过，
但服务器返回 HTTP 响应时，会话被一台 TTL=64 的设备注入 RST 掐断。
抓包证明该 RST 非两端主机发出（两端真实包 TTL=127，RST 包 TTL=63）。

申请：
对 源 10.243.111.152 → 目标 10.243.129.77 指定 TCP 端口
做有状态放行，不做应用层阻断（不注入 RST）。

无需开放：
SSH 22 / Ubuntu 对其他网段的访问 / 整个网段。
```

### 7.9 为什么项目本体放 Ubuntu 而非 Windows Server

直接把 Spring Boot + Vue + SQL 部署到 Windows Server 技术可行但不推荐：端口冲突（80/1433/3306/6688/8080/9090 已占用）；装 Java 可能与现有 Java/Tomcat 冲突；与生产业务争抢资源；动现有数据库风险高；**且部署在 Windows 也绕不开中间设备 RST 注入**，其它网段访问仍被拦。结论：项目本体全部部署在 Ubuntu，Windows 只做入口（若采用方案 A）。

## 第 8 章　生产环境要点

### 8.1 使用 MySQL

`.env`（Docker）或 systemd 环境变量里设 `SPRING_PROFILES_ACTIVE=mysql`，配好 `MYSQL_PASSWORD` / `MYSQL_ROOT_PASSWORD`（或 `MYSQL_USER` / `SPRING_DATASOURCE_URL`）。Docker 带 mysql profile 启动会额外拉起 MySQL 容器：

```bash
docker compose --profile mysql up -d --build
```

首次部署到空 MySQL 库（重要）：mysql profile 默认 `ddl-auto=validate`，要求表结构已存在。全新空库首发需让 Hibernate 先建表：

```bash
# 1. 首发临时用 update 自动建表
MATERIAL_PULL_MYSQL_DDL_AUTO=update docker compose --profile mysql up -d --build
# 2. 确认日志无错、表建好、能登录后，去掉该变量重建，回到 validate 稳态
docker compose --profile mysql up -d
```

> 为何不用完整建表脚本：29 张表若手写 DDL 易与实体漂移且离线无法验证，故采用「首发 update 建表 → 验证 → 切回 validate」策略。后续表结构变更用 `backend/src/main/resources/db/mysql/` 下的增量 SQL，在停机窗口手动执行。传统部署方式的首发升级脚本是 `docs/sql/V0.8.4__production_upgrade.sql`。

### 8.2 HTTPS（生产建议）

工厂内网也不应明文跑密码和设备密钥：

```bash
CERT_CN=material-pull.local CERT_IP=192.168.1.50 \
  sh deploy/scripts/gen-self-signed-cert.sh   # 生成 deploy/certs/server.crt 与 server.key
```

让前端容器用 TLS 配置：把 `frontend/Dockerfile` 复制的 `nginx.conf` 改成 `nginx-tls.conf` 重建，或在 compose 的 frontend 服务挂载 `nginx-tls.conf` 与 `deploy/certs` 并暴露 443。自签证书浏览器会提示不受信任，可把 `server.crt` 导入客户端/安卓设备信任，或用企业内部 CA。

> 注意：如第 7 章所述，中间网络设备对 443 端口的放行需另行验证，不保证像 80 一样通。

### 8.3 多实例 / 高可用（集群模式）

默认单实例：后端重启会导致登录态丢失，无法多副本，单机部署可接受。需要重启不掉线、多副本负载均衡时启用集群模式（会话/业务锁/定时任务去重/WebSocket 广播全部外置到 Redis）：

```bash
# .env 中设置
MATERIAL_PULL_CLUSTER_ENABLED=true
REDIS_PASSWORD=改成强密码
# 带 redis profile 启动
docker compose --profile mysql --profile redis up -d --build
```

集群模式要求 Redis 设置密码，否则后端拒绝启动。

### 8.4 数据库自动备份

```bash
docker compose --profile mysql --profile backup up -d          # 每天一次，保留14天，存 db-backups 卷
docker exec material-pull-db-backup sh /usr/local/bin/db-backup.sh   # 手动立即备份
docker exec -e MYSQL_PASSWORD=$MYSQL_PASSWORD material-pull-db-backup \
  sh /usr/local/bin/db-restore.sh /backups/material_pull-YYYYMMDD-HHMMSS.sql.gz   # 恢复（覆盖，谨慎）
```

> 备份卷在本机磁盘，磁盘损坏仍会丢。生产务必定期把 `db-backups` 内容同步到异地/对象存储。

### 8.5 监控（Prometheus）

后端已开 Actuator，指标端点 `/api/actuator/prometheus`，健康 `/api/actuator/health`。nginx 只允许内网网段访问 `/api/actuator/`，外部拒绝。建议至少对接内存、连接池、磁盘告警。

### 8.6 离线部署（工厂内网无外网）

联网机器打包镜像，拷到工厂导入：

```bash
# 联网机器：构建 + 导出
MAVEN_MIRROR_URL=https://maven.aliyun.com/repository/public \
NPM_REGISTRY=https://registry.npmmirror.com docker compose build
sh deploy/scripts/offline-images.sh save
# 工厂机器（拷入项目目录 + offline-images/ 后）
sh deploy/scripts/offline-images.sh load
docker compose up -d        # 无需再 build
```

### 8.7 时间同步（NTP）

系统用本机时间算任务超时、截止时间和审计时间。容器时区已设 `Asia/Shanghai`，但时钟跟随宿主机，务必保证宿主机校时：

```bash
sudo timedatectl set-timezone Asia/Shanghai
sudo timedatectl set-ntp true
timedatectl status        # 确认 System clock synchronized: yes
```

### 8.8 外部系统集成

打印机 / AGV / SAP 等对接点默认全部为空（不下发），需在真实环境联调后通过环境变量配置：

```text
MATERIAL_PULL_PRINT_SUBMIT_URL / PRINT_CANCEL_URL / PRINT_API_KEY
MATERIAL_PULL_AGV_DISPATCH_URL / AGV_CANCEL_URL / AGV_API_KEY
```

推送失败策略：`app.factory.ignore-push-failure` 默认 `true`，WebSocket 推送失败只记日志不阻断主流程（失败记入 outbox 表）。若业务要求「推送/打印失败必须感知」，评估是否改为 `false`。

## 第 8.5 章　安卓 APP 远程更新（APP 内检查更新 / 下载 / 安装）

APP「设备设置」页可检查更新、下载并安装新版 APK。Android 侧逻辑独立开发，本节只说明**服务器端**。

工作原理：APP 的服务器地址始终以 `/api` 结尾（如 `http://10.243.129.131/api`），更新时把 `/api` 换成 `/updates/android/latest.json`，与业务同源同端口。IP 变更只需在 APP「设备设置」里改服务器地址。

服务器端组成（本机部署在 `/opt/apps/material-pull`）：

| 部分 | 位置 | 说明 |
|---|---|---|
| 发布脚本 | `deploy/scripts/publish-android-apk.sh` | 复制 APK、算 SHA-256/大小、原子生成 `latest.json` |
| 源 APK 中转 | `/opt/apps/material-pull/upload/` | 手动放置，位置随意 |
| 发布目录 | `/opt/apps/material-pull/apk-updates/` | 仅脚本管理，含 APK 与 `latest.json` |
| Nginx 路由 | `location ^~ /updates/android/` | `alias` 到发布目录，apk/json MIME + `no-cache` |

`latest.json` 字段：`versionCode`、`versionName`、`apkUrl`（相对路径 `/updates/android/xxx.apk`）、`fileName`、`fileSize`、`sha256`、`releaseNotes`、`publishedAt`。

发布新版本（三步）：

```bash
# 1) 把 APK 放到 upload/（scp / U盘 / 共享目录皆可）
# 2) 发布（versionCode 必须比线上大，取 APK 内真实值）
cd /opt/apps/material-pull
./deploy/scripts/publish-android-apk.sh ./upload/你的APK.apk <versionCode> <versionName> "更新说明"
# 3) 自测
curl -fsS http://127.0.0.1/updates/android/latest.json
curl -I   http://127.0.0.1/updates/android/aptiv-<versionName>.apk
```

Web 下载入口：登录页版权行上方有「下载扫码枪 APP」按钮，指向下载页 `/app-download.html`（源文件 `frontend/public/app-download.html` + `app-download.js`）。该页 `fetch('/updates/android/latest.json')` 动态显示版本/大小/更新说明并给出 APK 下载链接，发布新版本后自动更新。两个易踩的坑：① 下载页必须由主站以 `text/html` 提供（放在 `/updates/android/` 目录会因 `default_type octet-stream` 被当文件下载）；② 站点 CSP 为 `script-src 'self'`，脚本必须是同源外部文件，不能内联。

注意：APK 必须用相同包名 + 相同签名证书才能覆盖升级；首版带更新功能的 1.3.0 仍需 USB 手动装一次。当前为 HTTP 内网方案，SHA-256 只防下载损坏，安装完整性最终由 Android 应用签名校验保障，条件允许可升级 HTTPS。详见 `新更新/2026/8/6/安卓APP远程更新部署说明.md`。

## 第 9 章　斑马标签自动打印（拉模式）

打印采用「拉模式」：后端生成待打印标签存库，Windows 打印代理定时领取并 RAW 发 ZPL 到 USB 打印机，全自动出标签。详见 `print-agent/README.md`。

### 9.1 打印链路

```text
后台生成标签任务
      │
      ▼
后端写入 t_print_job（状态 RENDERED，含 ZPL 内容）
      │
      ▼   打印代理每隔几秒轮询
GET /api/print-jobs/next      原子领取一批，置为 SENT
      │
      ▼
RAW 发 ZPL 到 USB 打印机（不弹窗、不走驱动排版）
      │
      ▼
POST /api/print-jobs/callback  回传 PRINTED / FAILED
      │
      ▼
后台「出货标签打印 / 打印作业」显示结果
```

打印作业状态机：`CREATED → RENDERED → SENT → PRINTED / FAILED / CANCELLED`。代理领取后若崩溃/断网未回传，后端在 `print-claim-stale-seconds`（默认 120 秒）后把作业重新分配，避免标签永远打不出来。

### 9.2 后端侧配置

```yaml
app:
  factory:
    print-pull-mode: true            # 拉模式，默认已开启
    print-claim-stale-seconds: 120   # 代理领取后多久未回传视为超时可重发
```

### 9.3 打印代理构建与运行

在有 .NET SDK 或 Visual Studio 的机器上 `cd print-agent && build.bat`，得到 `dist\AutoPrintAgent.exe`，把整个 `dist` 拷到打印电脑（打印电脑不用装 SDK）。

打印电脑上：双击运行 → 填服务器地址（如 `http://<服务器IP>/api`）→ 填 API 密钥（与后端 `MATERIAL_PULL_EXTERNAL_API_KEY` 完全一致）→ 选 GT800 打印机 → 测试连接 → 保存设置 → 开始轮询。勾选「开机自动启动」「启动后自动开始轮询」即全自动，关窗口最小化到托盘。可选用 NSSM 注册成 Windows 服务。

> 关键验证：GT800 有 ZPL / EPL 两种固件。用代理「立即打印一次」测一张，能出标签即 ZPL 固件直接用；打乱码需在打印机首选项切到 ZPL，或联系改造后端生成 EPL。

### 9.4 打印代理工程结构

| 文件 | 职责 |
| --- | --- |
| `Program.cs` | 入口，单实例互斥 |
| `MainForm.cs` | 配置界面 + 系统托盘 |
| `AppConfig.cs` | 配置读写（config.json） |
| `ApiClient.cs` | 领取作业 / 回传结果，X-Api-Key 鉴权 |
| `PrintWorker.cs` | 轮询 + 打印 + 回传 + 失败重试 |
| `RawPrinterHelper.cs` | winspool RAW 直发 ZPL |
| `Logger.cs` | 本地按天日志 |

配置存 exe 同目录 `config.json`，日志存 `logs\`。

### 9.5 定时自动打印（后端侧，无需代理触发）

后端可按系统参数定时挑选未打印任务自动提交打印，参数在后台「系统参数」页改，改完下一轮生效无需重启：

| 参数 | 默认 | 说明 |
| --- | --- | --- |
| `print.auto.enabled` | false | 定时自动打印开关 |
| `print.auto.interval-minutes` | 60 | 间隔分钟，最小 1（10=每10分钟，60=每小时） |
| `print.auto.printer-name` | 空 | 斑马打印机名，如 `ZDesigner GT800 (EPL)`；空则用默认打印机 |
| `print.auto.delivery-areas` | 空 | 只打印这些配送区域，逗号分隔如 `1,2`；空=所有区域 |
| `print.auto.print-type` | WAREHOUSE_BARCODE_LABEL | 打印作业类型 |
| `print.auto.last-run` | — | 系统记录上次执行时间（不可编辑） |

也可在前端「出货标签打印」页点「立即执行定时打印」，或调 `POST /api/print-jobs/auto-print/run`（ADMIN/WAREHOUSE）。开启前务必先配好打印服务地址与打印机名，否则会按「接口未配置」报错，不会静默假成功。

### 9.6 浏览器原生打印与历史记录补打

“浏览器打印”“打印本类别”及同一批量打印方法的入口不再打开固定尺寸子窗口。前端把标签 HTML 写入当前页面创建的隐藏 iframe，等待字体和文档加载完成后调用 `iframe.contentWindow.print()`，直接显示浏览器完整的原生打印对话框。打印机选择、“打印”和“取消”按钮均由浏览器提供，标签仍按 80×50 mm、边距 0 的打印样式生成。

打印记录表最右侧提供“补打标签”按钮，用于缺纸、卡纸、误操作或打印对话框已调起但标签未实际出纸时补救：

1. 点击某条记录后的“补打标签”；
2. 系统从该打印作业保存的 `payload` 历史快照恢复标签字段；
3. 重新生成真实 CODE_128 条码；
4. 调起浏览器原生打印对话框，每次只补打一张；
5. 补打后新增一条浏览器打印记录，已完成任务保持原终态。

补打使用历史快照，不要求任务仍在“待打印补货任务”中，也不会因基础数据后续变化而改变旧标签内容。浏览器 API 只能通知页面打印对话框已结束，不能可靠判断用户最后点击了“打印”还是“取消”，因此浏览器打印记录表示“已成功调起浏览器打印流程”，不等同于打印机物理出纸确认；物理未出纸时可再次使用“补打标签”。

## 第 10 章　技术说明（架构 / 目录 / 接口 / 安全）

### 10.1 技术栈

- **后端**：Java 17、Spring Boot 3.3.2、Spring Web、Data JPA、Validation、WebSocket/STOMP、H2（文件库，默认）/MySQL（生产）、Springdoc Swagger、Apache POI、Commons CSV、ZXing、spring-security-crypto（BCrypt）、Redis（集群可选）、Actuator + Micrometer/Prometheus。
- **前端**：Vue 3、TypeScript、Vite、Element Plus、Axios、Vue Router。
- **安卓**：Kotlin + Jetpack Compose，原生 `HttpURLConnection` 调后端 `/scan/*`。
- **打印代理**：C# / .NET Framework 4.7.2、WinForms、winspool RAW 打印。
- **部署**：Nginx + systemd 或 Docker Compose，SQL 升级脚本在 `docs/`。

### 10.2 目录结构

```text
material-pull                  项目根目录（本机 /opt/apps/material-pull）
├─ backend                     后端工程（Spring Boot）
│  └─ src/main/java/com/example/materialpull
│     ├─ common                通用返回、异常、请求上下文、安全/应用配置
│     ├─ security              角色注解与权限拦截器
│     ├─ config                跨域、WebSocket、Redis、首次管理员初始化
│     ├─ controller            REST API
│     ├─ dto                   请求/响应对象
│     ├─ entity                数据实体
│     ├─ enums                 业务枚举
│     ├─ repository            JPA 仓库
│     ├─ resilience            幂等、业务锁（本地/Redis）、操作保护
│     ├─ maintenance           健康检查与恢复
│     └─ service               业务服务（含 session/realtime 集群支撑）
├─ frontend                    Vue3 Web 管理端
├─ print-agent                 Windows 斑马标签自动打印代理（C#）
├─ dbtool                      数据库可视化工具（Python，见附录 C）
├─ deploy                      nginx / systemd / mysql init / 部署脚本
└─ docs                        SQL 升级脚本、导入模板、现场标签图片资源
```

### 10.3 关键接口与认证

外部 / 设备接口用 `X-Api-Key` 机器认证（对应 `MATERIAL_PULL_EXTERNAL_API_KEY`，SYSTEM 角色）：

```text
GET  /api/print-jobs/next            打印代理领取待打印作业
POST /api/print-jobs/callback        打印结果回调（PRINTED / FAILED）
POST /api/agv-jobs/callback          AGV 状态回调
/api/scan/*                          安卓扫码设备（工号+设备号，可选设备密钥/IP白名单）
/api/integrations/sap|ims|ppc/*      外部系统集成
```

请求头 `X-Api-Key: <与 MATERIAL_PULL_EXTERNAL_API_KEY 相同的值>`。

Web 管理端接口用登录会话（Bearer Token）+ 角色权限拦截：管理员、计划员、仓库员、产线员工、只读用户、系统接口。

### 10.4 安全要点

- 密码 BCrypt 存储，用户列表不返回密码字段；登录态在用户改密/改角色/禁用后失效。
- H2 Console / Swagger 默认关闭，仅 `dev` profile 开启。
- WebSocket 用 `/auth/ws-ticket` 短期一次性票据，不把长期 token 放 URL。
- 库存锁定/扣减在服务层校验，前端不能直接改；强制完成不吞库存扣减失败。
- 扫码、任务操作走业务锁 + 幂等键双重保护，防重复点击和并发。
- 外部接口未配置地址时直接拒绝，避免「假成功」。
- 非 dev 环境启动时检测默认密钥/弱口令则拒绝启动（`StartupSecurityGuard`）。
- `X-Api-Key` 等同外部接口访问权限，务必用强随机值并在网络层限制来源。

## 第 11 章　生产化改造历程

系统从「单机演示」迁移到「真实工厂生产环境」的改造已全部完成。总体原则：所有改动默认不破坏「H2 单实例开箱即用」模式；集群/Redis 能力通过 `app.cluster.enabled` 开关启用，关闭时回退内存实现。

### 阶段一　架构性硬伤（多实例 / 高可用）

- 会话 / WebSocket 票据外置（`SessionStore` 抽象：内存 + Redis 两实现）。
- 业务锁外置（`LockProvider` 抽象：本地 ReentrantLock + Redis SET NX 分布式锁）。
- 定时任务多实例去重（基于分布式锁的租约，`lockProvider.tryExecute`）。
- WebSocket 跨实例广播（Redis pub/sub 中继，`MessageRelay` 抽象）。
- CORS 白名单外置到 `MATERIAL_PULL_CORS_ALLOWED_ORIGINS`，移除硬编码 IP。

### 阶段二　数据库与数据安全

- MySQL 初始化脚本 + 首发 ddl 策略；H2 默认 URL 去掉 `AUTO_SERVER=TRUE` 消除多写风险。
- 数据库自动备份（`db-backup.sh` / `db-restore.sh` + compose 备份容器）。

### 阶段三　网络安全

- HTTPS（nginx TLS 配置 + 自签证书脚本 + compose 443）。
- 扫码设备鉴权加固（`app.security.device-auth.*`：设备密钥 + IP 白名单，默认不强制以兼容现有设备）。
- 启动时检测默认密钥 / 弱口令则拒绝启动。

### 阶段四　运维与可观测性

- 日志持久化（`logback-spring.xml`：应用日志留 30 天、错误日志留 60 天 + 卷）。
- Actuator + Prometheus 指标。
- compose 资源限制（内存/CPU）与 json-file 日志滚动。
- NTP 时间同步说明 + 容器时区 `Asia/Shanghai`。

### 阶段五　其它现实问题

- 外部系统集成说明 + 推送失败策略提示（`ignore-push-failure`）。
- 离线镜像打包脚本 + Maven/npm 镜像构建参数。

### 阶段六　现场易用性增强（功能代号 V0.8.5）

- 仓库任务页可设置的自动定时刷新（关闭/10秒/30秒/1分钟/5分钟，接入实时消息）。
- 修复「超时未自动升级紧急」bug：根因是整批单事务 + 乐观锁冲突导致回滚。改为逐条 `REQUIRES_NEW` 独立事务 + 悲观写锁重读，单条失败只记日志、下一轮再处理。
- 仓库补货任务页排序：第一优先级「配送区域」，第二「仓库地址」。
- 料号映射新增「配送区域」字段（默认 1，可手动改），任务与标签沿用。
- 斑马打印机定时自动打标签（见第 9.5 节）。

### 阶段七　业务一致性与生产风险修复（功能代号 V0.8.6）

- 扫码重复生成治理：补货去重覆盖全 active 状态（CREATED/ACCEPTED/PICKING/PICKED/DELIVERING/ARRIVED/EXCEPTION）。
- 收货扫码匹配收敛：只匹配 PICKED/DELIVERING/ARRIVED；多任务命中时要求扫任务号或更精确标签，避免误关单。
- 工位二维码解析修复：分隔符移除 `/`，避免带 `/` 的工位/地址被错拆。
- 料号映射仓库物料号修复：新增 `warehouseMaterialCode`，保存与扫码优先用仓库物料号，缺省回退仓库代号。
- 任务完成流程收紧：普通 `complete` 只允许 PICKED/DELIVERING/ARRIVED 且必须锁定扣减库存。
- 异常/重试/删除/空盒回收一致性：异常时释放库存锁/周转箱/打印任务/AGV；空盒回收仅终态/异常态且防重复派发；删除禁止 COMPLETED/DELIVERING/ARRIVED。
- AGV 集成容错与幂等：URL 未配置时跳过并让主流程继续；手动派发禁止终态任务并复用未完成作业。
- Docker MySQL Driver 错配治理：支持 `MATERIAL_PULL_DATASOURCE_DRIVER` 覆盖。

> 验证状态：前端 `npm run build` 通过；后端 `mvn clean package` 在联网机器上 BUILD SUCCESS。开发沙箱因无外网无法下载新依赖，编译验证需在联网机器执行。

### 阶段八　料号映射批量数据管理（功能代号 V0.8.7）

本阶段在「基础数据 → 料号映射」页面（`/mappings`）新增导出、覆盖上传、批量删除三项批量数据管理能力，并为高危操作引入「双重倒计时确认」交互。目标是让现场维护人员能整表导出备份、整表替换、按需清理，而不必逐条编辑。

#### 8.1 需求背景

原料号映射页只有单条「新增 / 编辑 / 删除」和追加式「导入」。当需要整表替换（如换线、数据重整）或批量清理时，只能逐条操作，效率低且易出错。本次按现场诉求补齐三个批量入口，并对不可逆操作强制多次确认，防止误触清空数据。

#### 8.2 新增功能清单

- **导出**：一键把当前料号映射全部数据导出为 `.xlsx`。列顺序与导入模板一致，便于「导出→改→覆盖上传」闭环。
- **覆盖上传**：先清空全部现存料号映射，再导入用户选择的文件。触发双重倒计时确认通过后才弹出选择文件框，选文件后才真正上传。
- **批量删除**（下拉菜单）：
  - 「删除全部数据」：物理删除全表料号映射。
  - 「按配送区域删除」：弹窗选择/输入配送区域（`deliveryArea`），只删该区域数据。
- **双重倒计时确认**：覆盖上传与批量删除点击后，先弹出第一次确认并倒计时 5 秒（倒计时期间「继续」按钮禁用），继续后再弹出第二次确认并倒计时 10 秒，两次都确认才执行；任一步取消则整个操作中止。

> 语义说明：本阶段的「批量删除 / 覆盖上传」是**物理删除**（`deleteAllInBatch` / `deleteByDeliveryArea`），与页面上单条「删除」按钮的**逻辑停用**（`enabled=false`，保留历史追溯）语义不同。批量删除用于清理/重建场景，请谨慎使用。

#### 8.3 后端改动

- `MaterialMappingRepository`：新增
  - `findAllByOrderByLineMaterialCodeAscMappingOrderAscIdAsc()`（导出用，全量有序）；
  - `countByDeliveryArea(String)`；
  - `@Modifying @Query` 的 `deleteByDeliveryArea(String)`（按配送区域物理删除）。
- `BasicDataService`：新增
  - `exportMappings()`：只读事务，返回全量映射；
  - `deleteMappings(scope, deliveryArea, ids)`：`scope=ALL` 清空全表 / `scope=AREA` 按区域删除 / 否则按 `ids` 删除，返回 `{deleted, mode, ...}`。
- `BasicDataController`：新增
  - `GET /mappings/export`：调用 `LogExportService.exportToXlsx` 生成 xlsx，`Content-Disposition` 带 UTF-8 文件名（复用日志导出同款响应头写法），角色 `PLANNER/WAREHOUSE/VIEWER` 可导出；
  - `DELETE /mappings`：请求体 `MappingDeleteRequest{scope, deliveryArea, ids}`，角色 `PLANNER`（含 `ADMIN`）；
  - 复用既有 `LogExportService`（SXSSF 流式写 xlsx），导出列常量 `MAPPING_EXPORT_COLUMNS` 与导入模板列顺序一致。
- `ImportController` / `ImportService`：`POST /imports/{type}` 新增可选参数 `overwrite`（默认 false）。`importExcel(...)` 增加 `overwrite` 重载，为 true 时先执行 `applyOverwrite(type)`。`applyOverwrite` 目前仅支持 `mappings`（先 `deleteAllInBatch` 再走原导入流程），其它类型传 true 会报错拦截，避免误清空别的表。

#### 8.4 前端改动

- 新增 `frontend/src/confirm.ts`：`doubleCountdownConfirm(action, detail?)`。基于 `ElMessageBox` 实现两段式倒计时确认（5 秒 + 10 秒），倒计时中禁用主按钮，倒计时归零后放开；取消抛出 `Error('cancel')` 供调用方中止。
- `components/ImportButton.vue`：新增 `overwrite`（透传 `overwrite=true` 到上传表单、按钮变红）与 `beforePick`（选文件前的异步守卫，返回 false 或抛错则不弹选择框）两个 props。
- `components/CrudTable.vue`：`defineExpose({ load })`，让父组件可在删除/覆盖后刷新表格。
- `views/Mappings.vue`：接入导出按钮、红色「覆盖上传」按钮（`before-pick` 走双重确认）、「批量删除」下拉（全部 / 按区域，均走双重确认），并新增「按配送区域删除」弹窗（区域下拉从当前数据聚合，支持手输）。

#### 8.5 接口速查

| 方法 | 路径 | 角色 | 说明 |
|---|---|---|---|
| GET | `/api/mappings/export` | PLANNER/WAREHOUSE/VIEWER | 导出全部料号映射为 xlsx |
| DELETE | `/api/mappings`（body `{scope:"ALL"}`） | PLANNER | 物理删除全表 |
| DELETE | `/api/mappings`（body `{scope:"AREA", deliveryArea:"1"}`） | PLANNER | 按配送区域物理删除 |
| DELETE | `/api/mappings`（body `{ids:[...]}`） | PLANNER | 按 id 批量物理删除 |
| POST | `/api/imports/mappings`（form `file`, `overwrite=true`） | ADMIN/PLANNER/WAREHOUSE | 覆盖上传：先清空再导入 |

#### 8.6 部署与验证注意

- **必须重新打包并重启后端**。若只 `mvn package` 不重启，旧进程仍在内存跑旧代码，`GET /mappings/export` 会返回 `HttpRequestMethodNotSupportedException`（表现为前端 500）。本机 systemd 服务 `material-pull-backend.service` 重启需 sudo；`ExecStart` 已指向新 jar，重启即加载新代码。
- **前端需重新 `npm run build`**，产物在 `frontend/dist` 由 Nginx 托管；发布后浏览器需强制刷新（Ctrl+Shift+R）避免旧 JS 缓存。
- 验证状态：前端 `vue-tsc` 类型检查通过、`npm run build` 通过；后端 `mvn clean package -DskipTests` BUILD SUCCESS。
- **数据安全提示**：覆盖上传、删除全部为不可逆物理删除，目前仅靠「双重倒计时确认 + 角色权限」保护，无自动备份。建议执行前先用「导出」留一份快照；如需更强保护，可后续加「覆盖/清空前自动导出备份」或将危险操作收紧到仅 `ADMIN`。

### 阶段九　新版安卓 APP 两阶段扫码适配（功能代号 V0.8.7）

配合新版安卓 APP 把扫码从「一步建单」改为「先预览、员工确认数量/单位、再建单」的两阶段流程。核心原则：员工输入只覆盖本次任务的 `requestQty/requestUnit`，绝不回写料号映射、标签、盒子、物料主数据；单位允许任意文本、不做换算。

#### 9.1 两阶段流程

1. 扫码后调 `POST /api/scan/preview`：只查询物料基础数据，返回后台默认数量 `defaultQty` 与默认单位 `defaultUnit`，**不创建任务、不切盒状态**。
2. APP 显示物料、默认数量与单位，员工可修改本次申请数量和单位（单位空值按「个」）。
3. 员工「确认发送」后调用原有 `POST /api/scan/empty` 建单，未修改时把预览得到的默认值原样提交。

#### 9.2 后端改动

- `ScanController`：新增 `POST /scan/preview`（`AuthTokenFilter` 已统一放行 `/scan/*`）。
- `ScanService.preview(...)`：`@Transactional(readOnly = true)`，复用既有扫码解析/标签解析/料号映射选择逻辑；默认数量优先盒子标准数量、退回标签标准数量，默认单位依次取标签单位 → 物料主数据单位 → 「个」；注入 `MaterialRepository`、新增 `findMaterialUnit`。
- `ScanService` 建单：`createTaskFromMapping/createTaskFromLabel/createTask` 三处改用 `resolveRequestQty(req, 默认值)`（未传用默认、必须为正）与 `resolveRequestUnit(req)`（限 32 字符、空值「个」、不做白名单/换算）；只给新任务赋值，不碰基础数据。
- 幂等摘要加入 `requestQty/requestUnit`，同一扫码值的不同数量/单位组合不会被误判为重复请求。
- DTO：`ScanRequest` 加 `requestQty/requestUnit`，`ScanResult` 加 `requestUnit`，新增 `ScanPreviewResult`。
- 实体：`ReplenishmentTaskEntity` 加 `requestUnit`（默认「个」）+ `normalize()` 默认值。

#### 9.3 数据库改动

- `backend/src/main/resources/db/mysql/V0.8.7__task_request_unit.sql`：`t_replenishment_task` 加 `request_unit VARCHAR(32) NOT NULL DEFAULT '个'`。生产 `ddl-auto=validate`，须**先执行 SQL 再重启后端**。

#### 9.4 前端改动

- `views/WarehouseTasks.vue`「需求数量」列由纯数字改为 `数量 + 单位` 显示。打印标签版式本次未改。

#### 9.5 接口速查

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/scan/preview` | 只查物料并返回 `defaultQty/defaultUnit`，不建任务 |
| POST | `/api/scan/empty` | 建单，可带 `requestQty/requestUnit`（本次任务专用，不回写基础数据） |

#### 9.6 部署与验证注意

- 顺序：备份 → 执行 `V0.8.7` 加字段 SQL → 打包重启后端 → 构建前端 → 验证。已按此上线。
- 验证状态：后端 `mvn clean package` BUILD SUCCESS、前端 `npm run build` 通过；MySQL 字段已确认；`/api/health/ready` 为 UP；`POST /api/scan/preview` 空 body 返回 HTTP 400（非 404），接口已生效。
- 待办：用现场真实扫码值跑端到端，确认 empty 建单带单位、基础数据 `quantity/standard_qty/unit` 不变、下次预览仍返回后台原始默认值。

### 阶段十　BOM 周计划联动（功能代号 V0.9.0）

打通「周计划 → BOM → 料号映射数量」链路。原先料号映射的数量靠人工逐条维护；现在计划员一次上传一周（七天）生产计划，系统按 BOM 把物料展开到组件、乘单根用量算出需求量，刷新料号映射数量。

**关键口径：料号映射的「数量」是「当天用量」，不是整周总量。** 计划一次录七天，系统默认每天上海时区 0 点按当天日期自动刷新；计划员仍可在页面选择日期手动预览/补刷。

#### 10.1 数据链路

```
周计划(一次录入七天，每天分白班/夜班)
    ↓ 取「指定某一天」各 8D 号数量：当天 = 白班 + 夜班
物料当天需求
    ↓ 用当前 ACTIVE 的简单BOM 展开（物料 → 组件，多物料共用组件则累加）
组件当天需求
    ↓ × 料号映射.单根用量
刷新 料号映射.quantity（= 当天用量）
```

三条现场口径（写进了计算引擎）：

1. **单根用量为空或 ≤0** 视为员工还没维护 → 跳过不计算，该条数量**保持原值不动**。绝不做 `×0`，否则会把数量刷成 0、现场按 0 拉料。
2. **计划里没出现的组件** → 保持原有数量不动，不清零。
3. **BOM 缺组件、组件缺映射** → 只记提示不阻断，可算的部分照常落库。

#### 10.2 新增数据表（3 张）

`backend/src/main/resources/db/mysql/V0.9.0__weekly_plan.sql`：

| 表 | 作用 |
|---|---|
| `t_weekly_plan_batch` | 上传批次，记录年份/周次/Excel 版本/上传人/统计 |
| `t_weekly_plan_row` | 计划行（按 8D 号），带库内 `version` 乐观锁用于冲突覆盖判定 |
| `t_weekly_plan_shift_qty` | 班次数量，按「日期 + 班次(DAY/NIGHT)」行存 |

班次数量**行存而非 14 列**：跨月跨年周、班次增减都不必改表结构。

#### 10.3 周计划模板与解析约定

模板结构（后端 POI 动态生成，含合并单元格，静态 CSV 表达不了）：

- 标题行：`E0Y&T2Y 生产计划 WK40_V4` —— 解析出周次 `WKxx` 与 Excel 版本。
- 表头两行：第一行固定 9 列（客户/生产工厂/项目/客户号/8D号/描述/标包/JPH/WH）+ 每天「白/夜」；第二行为日期（月/日），每个日期横跨白、夜两列。
- 数据行到「合计」行为止；合计行与签字区（生产计划主管/生产主管/班长确认等）一律跳过。
- 当天数量 = 白班 + 夜班；周数量 = 七天之和。
- 8D 号按数值取再校验为 8 位纯数字，避免科学计数法与前导零丢失。

**年份问题**：模板日期只有「月/日」不带年份，无法自证归属年，所以上传时由计划员选年份。跨年周（周次 ≤2 却出现 12 月、周次 ≥50 却出现 1 月）按周次自动往前/后归年。

#### 10.4 计算引擎的容错策略

原则是「**算不了的就不动，绝不写坏数据**」，而不是整体阻断：

| 情形 | 处理 |
|---|---|
| 单根用量为空或 ≤0 | 跳过该条，数量**保持原值**；计入 `skippedNoUsage` 并在弹窗单独提示 |
| 当天数量为 0（未排产） | 不参与计算 |
| BOM 找不到组件 | 记入 `warnings`，跳过，不阻断其余 |
| 组件找不到料号映射 | 记入 `warnings`，跳过，不阻断其余 |

为什么不做「×0 也照写」：单根用量没维护时若按 0 计算，会把数量刷成 0，现场按 0 拉料，比不刷新更危险。

支持 `apply=false` 仅预览，返回 `oldQuantity → newQuantity` 对比，先看清每条会变成什么再决定是否写入。

#### 10.5 自动刷新与手动补刷

默认每天 `00:00:00`（`Asia/Shanghai`）由 `WeeklyPlanScheduler` 自动执行：

```text
LocalDate.now(Asia/Shanghai) → WeeklyPlanCalcService.calculate(today, true)
```

- 自动与手动共用同一计算入口，不存在两套口径。
- 使用 `LockProvider` 防多实例重复执行，锁键包含日期、租约 1800 秒。
- 当天无计划、无有效 BOM 或所有数量为 0 时仅记 WARN 并跳过，不影响后端服务。
- 单根用量为空或 ≤0 的映射继续保持原数量，自动任务不会把它刷成 0。
- 计划员仍可在周计划页面选日期预览或手动补刷，适合午夜后才补传/修改计划的场景。

默认配置：

```yaml
app.scheduler.weekly-plan-refresh-cron: 0 0 0 * * *
app.scheduler.weekly-plan-zone: Asia/Shanghai
```

可用环境变量 `MATERIAL_PULL_WEEKLY_PLAN_REFRESH_CRON`、`MATERIAL_PULL_WEEKLY_PLAN_ZONE` 覆盖。Spring cron 使用六段格式「秒 分 时 日 月 周」。

#### 10.6 前端页面

| 页面 | 路径 | 说明 |
|---|---|---|
| 简单BOM | `/simple-bom` | CSV 高速导入、批次启用/归档、单条增删改（仅当前有效批次） |
| 周计划 | `/weekly-plan` | 上传（选年份）、冲突确认、明细查看、**选日期**预览/刷新当天用量 |
| 周计划模板 | `/weekly-plan-template` | 填客户/工厂/项目/客户号 + 选起始日，自动算七天日期预填后下载 |

#### 10.7 冲突检测与工厂隔离

业务键为「年份 + 周次 + **工厂** + 8D号」。带上工厂做隔离：**弋江与三山的计划员即使排到同一个 8D 号，也互不算冲突、互不覆盖**。

若不带工厂，A 计划员传弋江、B 传三山，只要有同一 8D 号，B 确认覆盖时会把 A 的整行连班次数据一起删掉。工厂为空时用 `is null` 判定，避免 `= null` 恒假导致漏检。

同厂内同 8D 号冲突时，前端弹窗列出冲突明细要求二次确认；确认后才删除旧行并写入新批次。

#### 10.8 接口速查

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/weekly-plan/import` | 上传周计划，需 `planYear`；冲突且未确认时返回 `needConfirm=true` 且不落库 |
| GET | `/api/weekly-plan/template` | 下载模板，可带 `startDate` 及预填字段 |
| POST | `/api/weekly-plan/calculate` | 按天计算：`planDate=YYYY-MM-DD`，`apply=false` 预览 / `true` 写入 |
| GET | `/api/weekly-plan/batches`、`/rows` | 批次列表、明细分页 |
| POST | `/api/simple-bom/rows` | 单条新增/编辑 BOM 行 |
| DELETE | `/api/simple-bom/rows/{id}` | 单条删除 BOM 行 |

#### 10.9 部署与验证注意

- 建表：顺序为 备份 → 执行 `V0.9.0__weekly_plan.sql` → 打包重启后端 → 构建前端。生产 `ddl-auto=validate`，表不存在会启动失败。**已于 2026-08-11 执行完成**（3 张表已建、服务已重启）。
- 验证状态：后端打包通过、前端构建通过、无 lint 错误。解析已用程序化往返测试验证（生成模板 → 填数 → 解析），数量口径正确（14 班×10=140、7 白班×5=35），真日期单元格与「合计行+签字区」两个边界场景均通过。
- **未验证**：计算引擎实际写库这一段。1284 条映射 `single_unit_usage` 全为 NULL，现在触发计算会全部走「跳过」分支，一条都不会刷。需先维护单根用量。
- **待办与风险**：见根目录 `潜在问题清单.md`。其中 P0「`quantity` 一字两用（既是每盒容量又是当天需求量，会影响出货标签印的数量）」按用户决定暂不修改，待现场确认标签应印当天总需求还是一盒容量后再定是否拆字段。
- **自动刷新**：已实现每天上海时区 0 点自动刷新；首次真实自动执行需在下一个 0 点查看 `当天用量自动刷新完成/跳过` 日志确认。

### 阶段十一　扫码「物料号 + 总装地址」联合匹配加固（功能代号 V0.9.1）

APP 扫码后选哪条料号映射，决定了仓库标签印哪个仓库代号、料送到哪个工位。**同一物料常被多个工位共用**，只按物料号匹配会印错标签、拉错料。

#### 11.1 为什么必须联合匹配（实测数据）

| 核对项 | 实测 |
|---|---|
| 启用物料总数 | 506 |
| **跨多个工位的物料** | **113**（单物料最多对应 6 个总装地址） |
| 总装地址为空的映射 | 0 |
| 扫码记录总数 / 已成功记录的工位地址为空 | 668 / **0**（历史成功建单均有工位；但 Android 两阶段提交流程曾在预览后丢失工位，失败发生在审计日志写入前，不能据此表推断所有正式请求都携带工位） |
| 去重「物料号+工位」组合匹配率 | 163 / 164 |

例：物料 `13799816` 对应 6 个工位——`物料架-01-F03`、`MECAL-07-F06`、`SONIC-46-A01~A02`、`B0RX-35-01-A01`、`物料架-10-E15`、`胶带机-12-A01~A02`。只按物料号选，等于随机挑一个工位。

#### 11.2 匹配规则（`ScanService.chooseMapping()`）

```
条件1：二维码物料号 = 料号映射.物料号(lineMaterialCode)
条件2：二维码工位地址 = 料号映射.总装地址(deliveryAddress)   ← 归一化后比较
   ↓ 两条都满足后
按用途择一：使用→NORMAL / 备用→URGENT，再由 A/B 双盒轮换机制定最终仓库代号
```

**关于「联合唯一」**：684 个「物料号+总装地址」组合中 598 个对应多条记录（最多 4 条），但那些是 **A/B 双盒**——同一工位同一物料本就有两个仓库代号，总装地址相同、**工位不会印错**，A/B 选择由既有轮换机制负责。所以「物料号+总装地址唯一确定**工位**」成立，这才是防印错的关键。

#### 11.3 地址归一化（修了一类隐蔽误判）

库里 **76 条地址含波浪号**，全角 `～` 与半角 `~` 混用。原先用 `equalsIgnoreCase` 精确比对，会把「看起来一样」的地址判为不等，误报「条件2未满足」让本该成功的扫码失败。

`normalizeAddress()` 归一全角/半角波浪号、连字符、括号、空格与大小写。**只归一字符形态、不删分隔符**，因此 `物料架-04-D04` 与 `物料架-04-D4` 仍判为不同，避免过度匹配把不同工位并成一个。

#### 11.4 匹配失败时的报错

条件2 失败时直接列出该物料**已维护的全部总装地址**，现场可立刻对照判断是二维码印错还是映射缺记录，不用去翻数据库。

另有兜底：工位地址为空且该物料对应多个总装地址时，拒绝建单并列出候选地址，不再任取第一条。正常 APP 流程应携带工位；若仍触发此分支，优先检查客户端是否在预览后丢失原始二维码，而不是放宽后端精准匹配规则。

#### 11.5 Android APP 两阶段扫码协议（1.4.0 修复）

APP 的扫码不是一次请求，而是两阶段：

```
扫码枪原始值
  13609637,物料架-11-E06,备用
        ↓ 第一次请求：/scan/preview（只预览，不建单）
显示物料、工位、默认数量，员工确认
        ↓ 第二次请求：正式提交（创建任务）
后端按「物料号 + 工位 + 用途」精准选择映射
```

`1.3.0` 曾有一个关键问题：预览接口响应里的 `scannedCode` 是拆分后的纯物料号，APP 用它覆盖了原始扫码值，导致第二次正式提交只发 `13609637`，工位 `物料架-11-E06` 与用途 `备用` 全部丢失。后端看到该物料对应多个地址后按安全规则拒绝建单。

`1.4.0` 已修复：`ScanPreview.scanCode` 始终保留扫码枪原始字符串，正式提交继续发送完整二维码。后端同时兼容以下格式：

```text
13609637,物料架-11-E06,备用
13609637|物料架-11-E06|备用
13609637(备用)
物料架-11-E06
```

多行格式在 `LabelResolverService.normalize()` 中将内部换行转换为逗号，不再直接删除粘连字段；`splitMaterialAndStation()` 还会提取物料号括号后的「使用/备用」。

#### 11.6 Android 版本与远程发布约定

当前已发布版本：

```text
versionCode = 6
versionName = 1.4.0
APK = /opt/apps/material-pull/apk-updates/aptiv-1.4.0.apk
清单 = /opt/apps/material-pull/apk-updates/latest.json
公开地址 = /updates/android/aptiv-1.4.0.apk
```

APP 判断有无更新看的是 **`versionCode`，不是 `versionName`**。服务器曾误配成 `versionCode=7 / versionName=1.4.0`，导致手机已安装 1.4.0 后仍提示发现 1.4.0；现已恢复为与 APK 一致的 `6 / 1.4.0`。

后续版本必须一一对应、同步递增：

| versionName | versionCode | 文件名 |
|---|---:|---|
| 1.4.0 | 6 | `aptiv-1.4.0.apk` |
| 1.4.1 | 7 | `aptiv-1.4.1.apk` |
| 1.4.2 | 8 | `aptiv-1.4.2.apk` |

用户自行构建 APK 后，直接以 `aptiv-<versionName>.apk` 命名并放入 `/opt/apps/material-pull/apk-updates/`，再运行：

```bash
cd /opt/apps/material-pull
./deploy/scripts/publish-android-apk.sh \
  ./apk-updates/aptiv-1.4.1.apk \
  7 \
  1.4.1 \
  "本次更新说明"
```

脚本会计算 SHA-256、文件大小并原子生成 `latest.json`；源和目标是同一路径时会跳过复制。完整步骤见 `新更新/2026/8/6/2.安卓APP远程更新部署说明.md`。

#### 11.7 验证与遗留

- 地址匹配用例 9/9 通过：含全角/半角波浪号、大小写、首尾空格必须匹配，以及 `手工台-34-A02` vs `手工台-34-B02`、`物料架-04-D04` vs `物料架-04-D4` 必须不匹配。
- 已打包重启并核对运行的是新 jar（`normalizeAddress`、多行扫码预处理与新报错文案均在 jar 内、健康检查 UP）。
- Android `1.4.0` 已由用户构建并发布，`latest.json` 和 APK 地址均返回 HTTP 200；手机安装后检查更新正常，不再重复提示同版本。
- 遗留一条真实数据问题（见 `潜在问题清单.md`）：扫码 `35452248 @ TH80-OLE1-07-A05-A06` 匹配不上，映射里是 `TH80-OLE-03-A03-A04`，`OLE1`/`OLE`、`07`/`03` 两处不同，非字符形态问题，需现场判断补映射还是重印二维码。

### 阶段十二　出货标签按配送区域/总装地址分组打印

设计分布在两个工厂，出货标签不能全部混在一起打印。出货标签打印页面现支持：

- **按配送区域分组**：使用任务的 `deliveryArea` 字段。
- **按总装地址分组**：使用任务的 `deliveryAddress` 字段；缺失时回退到 `sendStationAddress`、`stationName`、`stationCode`。
- 每个分类显示待打印标签数量。
- 每个分类可单独执行「打印本类别」，直接调起浏览器原生打印对话框并一次生成该类别全部标签。
- 每个分类可单独执行「提交本类别」，将该类别任务批量提交到本地打印代理队列。
- 原有全部筛选结果打印仍保留，搜索条件可以和分类组合使用。

分类只作用于当前待打印任务；已生成打印作业的任务不会再次出现在待打印分类中。后端已有 `ReplenishmentTaskEntity.deliveryArea`、`deliveryAddress` 等字段，页面复用 `GET /api/tasks` 和 `POST /api/print-jobs`，不新增数据库字段。

### 阶段十三　料号映射增加工厂参数（功能代号 V0.9.1）

系统现由两个工厂共同使用。为给后续跨工厂数据隔离与冲突治理提供基础数据，料号映射 `m_material_mapping` 新增可空字段 `factory VARCHAR(64)`。

本阶段严格限定为“增加参数”：

- 员工可在“料号映射”页面查看、新增和编辑工厂；
- 导入识别“工厂 / 所属工厂 / 生产工厂”表头，导出增加 `factory` 列；
- 两份料号映射 CSV 模板增加“工厂”列；
- 历史数据的工厂保持 NULL，由员工手工维护，不做自动推断或批量回填；
- 工厂暂不参与扫码选映射、A/B 盒轮换、周计划需求计算、标签打印、权限控制或查询过滤。

数据库升级脚本为 `backend/src/main/resources/db/mysql/V0.9.1__mapping_factory.sql`。生产库已于 2026-08-12 执行升级，升级前有效备份位于 `db-backups/material_pull-20260812-155900.sql.gz`，字段已确认存在且允许为空。

验证状态：前端与后端构建均通过。后端已于 2026-08-12 17:32 随 V0.9.2 发布完成重启，健康检查返回 `UP`，V0.9.1 的保存与导出逻辑已正式生效。

### 阶段十四　任务与打印工厂分类（功能代号 V0.9.2）

本阶段把料号映射中的工厂固化为补货任务和打印作业快照。标准工厂为“弋江”“三山”，空值和异常旧值在页面统一归入“未维护工厂”。历史任务与历史打印作业不按当前映射倒推，避免归属随基础数据变化。

所有建单入口从对应料号映射取得工厂；打印作业直接继承任务工厂。AGV、标签、盒子和周转箱不在本阶段扩展工厂字段。仓库任务页和出货打印页固定显示三个同级工厂大类，并以本地存储保存独立折叠状态。出货打印页禁止跨工厂批量操作，支持同工厂跨细分组及仓储地址自然升降序。

料号映射导入对工厂执行逐行严格校验：自动去除首尾空格，只允许“弋江”“三山”；非法或空工厂行失败并保留旧数据，合法行继续写入。导出 Excel 使用中文“工厂”表头，导入同时兼容英文 `factory`。

迁移脚本为 `backend/src/main/resources/db/mysql/V0.9.2__task_print_factory.sql`。生产库已于 2026-08-12 完成备份、迁移和服务重启：备份文件 `db-backups/material_pull-20260812-173016.sql.gz` 已通过 `gzip -t`；服务健康检查返回 `UP`；任务与打印接口在未登录抽查时返回预期 HTTP 401，说明路由和鉴权正常响应。

上线后结构复核显示，`t_replenishment_task.factory` 与 `t_print_job.factory` 实际均为 `VARCHAR(255) NULL`，两个 `(factory, created_at)` 索引存在。该长度与迁移脚本声明的 `VARCHAR(64)` 不一致，但不影响现有分类和打印业务；如后续要求结构完全一致，应另行评估并执行收窄迁移。历史 778 条任务和 768 条打印作业均无工厂快照，按设计归入“未维护工厂”。

### 阶段十五　浏览器原生打印与历史标签补打

浏览器打印链路由“固定尺寸子窗口 + 子窗口 `print()`”改为“隐藏 iframe + `iframe.contentWindow.print()`”。“浏览器打印”“打印本类别”和相关批量入口均直接调起完整的浏览器原生打印对话框，避免窗口尺寸或系统显示缩放导致原生操作按钮不可见。

打印记录表新增单张“补打标签”按钮。补打优先读取打印作业 `payload` 中的历史标签快照，恢复条码、物料、数量、仓储地址、工位地址、盒子大小、配送类型和送货人工号，重新生成 CODE_128 后调起原生打印。补打会新增浏览器打印记录，但不会改变已完成任务的终态。

本阶段仅修改前端 `FactoryPrint.vue`，无后端接口、表结构或迁移脚本变化。前端 `vue-tsc` 与 Vite 生产构建均通过，`frontend/dist` 已更新并由 Nginx 直接生效。

### 阶段十六　物料用量可视化看板与历史任务保留

原“工位用料”页面已整体替换为“物料用量看板”，但原工位用料实体、接口和数据没有删除，扫码、标签与计划计算仍可继续读取。看板使用料号映射作为物料全集，以扫码成功生成的补货任务作为历史事实：

- 按任务创建时间（现场扫码落库时间，北京时间）统计，默认最近 30 天；
- 重复扫码生成的任务全部累计，不去重；
- `CANCELLED` 计入扫码申请和取消统计，不计入有效统计；其余状态均为有效；
- 不跨物料汇总数量，同一物料按 `requestUnit` 分单位累计；
- 任务快照决定历史数量与归属，当前料号映射用于对照，差异显示“映射已变更”；
- 零扫码的料号映射仍出现在“所有物料信息”表中。

页面提供日期、工厂、物料、工位、配送区域、任务状态全局筛选，以及申请趋势、物料排行、工厂分布、工位排行、配送区域分布、取消分析六类图表。只读接口为：

`GET /api/material-usage-dashboard?from=yyyy-MM-dd&to=yyyy-MM-dd`

为永久保留统计历史，原任务删除接口路径保持不变，但后端行为改为逻辑取消：释放关联资源后把状态设为 `CANCELLED`，不再删除数据库记录；已完成任务禁止取消。此前已经物理删除的数据无法恢复，看板以当前数据库保留数据为历史起点。

前后端已于 2026-08-13 构建发布，后端服务 16:00:44 完成重启，无数据库迁移。前端另有现有接口兼容回退，但完整模式以新统计接口为准。

### 阶段十七　网页端现场扫码入口安全隐藏

浏览器端暂不需要“现场扫码工作台”，因此前端菜单、权限元数据和 `/scan` 路由已移除。直接访问 `/scan` 不再打开工作台。

本阶段采用可恢复的隐藏方式：保留 `SiteScan.vue` 源文件，并完整保留后端 `/scan/*` 接口、安卓 APP 扫码认证、扫码生成任务及相关服务。也就是说，网页端入口不可见，但现场安卓扫码枪业务继续正常运行。恢复网页入口时重新注册路由、权限元数据和菜单项即可。

## 第 12 章　已知问题与风险

以下来自一次代码通读（静态审查，未逐条实跑）。部分是产品有意的设计取舍，修改前建议结合现场实际使用方式确认。

> 另见根目录 **`潜在问题清单.md`**：那里记录的是**已发现但尚未处理**的问题（按 P0/P1/P2 分级，含排查用 SQL），处理完会移入 `修复与发布记录.md`。本章偏静态审查结论，两者互补。

### 严重

1. **安卓终端 `/scan/*` 弱鉴权**：`AuthTokenFilter` 中 `/scan/` 开头请求带任意 `X-Employee-No` / `X-Device-No` 即放行并绑 `LINE` 角色。这是「安卓免登录」产品决策，但内网任意设备可伪造扫码。阶段三已提供可选加固（`MATERIAL_PULL_DEVICE_REQUIRE_KEY` 设备密钥 + `DEVICE_ALLOWED_CIDRS` IP 白名单），生产应启用。
2. **集群/多实例下内存态失效**：会话、业务锁、ws 票据默认是内存 `ConcurrentHashMap`，多实例或重启会丢登录态、锁不跨实例。单实例无碍；多实例须开集群模式走 Redis（阶段一已支持）。
3. **`scanReceive`/`scanException` 幂等不足**：未像 `scanEmpty` 那样走 `idempotencyService.begin/finish`，`receiveBySiteScan` 把 `requestId` 设为每次都不同的 traceId，网络重试可能重复触发收货后的空盒 AGV 派发。（阶段七的收货匹配收敛已缓解部分风险，仍建议补真正幂等包装。）

### 中等

4. `doScanEmpty` 中 `status != IN_USE && status == FULL_STANDBY` 前半段冗余，等价于 `status == FULL_STANDBY`，意图含糊，建议简化。
5. 任务删除已改为逻辑取消并保留历史；已扣减任务的盒子状态不回滚仍属设计取舍，建议持续关注取消时的现场盒态一致性。
6. `ImportService.csvToWorkbook` 固定 UTF-8，国内 Excel 另存的 GBK CSV 会乱码，建议探测编码或文档强制 UTF-8 模板。
7. 料号映射导入「是否有序号列」靠 `isInteger` 启发式判断，物料号纯数字时易误判，建议按表头显式区分列。
8. 映射唯一约束只在「启用」时校验，建议确认 SQL 升级脚本对 `warehouseCode` 是否有无条件唯一索引。
9. `MaterialMappingBootstrapRunner` 正则要求 5 列齐全，缺列/特殊盒型行会被跳过，导入覆盖率应抽查。
10. 前端 `canTaskAction` 对 ADMIN 的 `forceComplete` 判断绕口，前后端行为一致但建议显式列权限。

### 轻微

11. 安卓 `MaterialPullApi` 同时发 `Authorization` 和 `X-Api-Key: token`，语义混乱，建议删掉 `X-Api-Key` 那行。
12. 前端 `idempotencyKey` 按 5 秒时间桶生成，边界两侧会得到不同 key，建议由调用方传稳定业务键。
13. `TimeoutScheduler` 的 `count++ > batchSize` 会多处理一条且未真正分页，大数据量下建议仓库分页查询。（阶段六已重写超时处理逻辑为逐条独立事务，此项主要指批量查询方式。）
14. H2 dev 默认库，生产已明确建议 MySQL。
15. CORS `allowCredentials(false)` 与前端 Bearer token 一致，无问题。

处理优先级建议：`/scan/*` 加固（1）→ 收货幂等（3）→ `doScanEmpty` 简化（4）→ CSV 编码/映射列判断（6、7）→ 多实例迁 Redis（2）。

## 第 13 章　日常运维速查

| 场景 | 操作 |
| --- | --- |
| 更新后端代码（脚本/systemd） | `cd backend && mvn clean package -DskipTests` 后 `sudo systemctl restart material-pull-backend` |
| 更新后端代码（Docker） | `docker compose up -d --build backend` |
| 更新前端代码 | 生产 `cd frontend && npm run build`（Nginx 立即生效，无需重启）；Docker `docker compose up -d --build frontend` |
| 改了 env 配置 | 重启对应服务生效 |
| 换了 API 密钥 | 后端 env 和打印代理界面同步改成同一串，各自重启 |
| 查后端日志（脚本/systemd） | `tail -f /opt/apps/material-pull/logs/backend.log` 或 `journalctl -u material-pull-backend -f` |
| 查后端日志（Docker） | `docker compose logs -f backend`；历史文件在 `ie_backend-logs` 卷 |
| 查打印代理日志 | 打印电脑 `print-agent\dist\logs\agent-*.log` |
| 查当前访问地址 | `cat ~/物料拉动系统-访问地址.txt` |
| 重启 Nginx（改了配置后） | `sudo nginx -t && sudo systemctl reload nginx` |
| 端口占用排查 | `ss -ltnp | grep -E ':80|:8080|:3306'` |

常见问题：

- **打不开页面 / 白页**：确认后端在跑 `curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1/api/health/ready`（应 200）；确认 Nginx 在跑；确认 `frontend/dist/index.html` 存在。
- **登录报错 / 403**：本架构前后端同源，正常无 CORS 403。若出现，检查是否有残留后端进程占用 8080：`ss -ltnp | grep 8080` 应只有 systemd 管理的那个。
- **别的网段打不开、本机能开**：确认访问用的是 **80 端口**（非标准端口会被公司中间网络设备拦截，详见第 7 章）。
- **换了 IP 后同事打不开**：让他们用 `物料拉动系统-访问地址.txt` 里的最新地址。
- **标签打不出来**：查代理是否运行 / 密钥是否一致 / GT800 是否 ZPL 固件 / 是否缺纸。

---

## 第 14 章　首次上线检查清单

- [ ] `MATERIAL_PULL_EXTERNAL_API_KEY` 改为 ≥16 位强随机值（默认值会被后端拒绝启动）
- [ ] MySQL / Redis 密码改掉模板默认值
- [ ] 用 MySQL 而非 H2，并完成首发建表流程（见 8.1）
- [ ] 确认对外走 80 端口，各网段可访问（见第 7 章）
- [ ] 启用 HTTPS（见 8.2，注意 443 放行需另行验证）
- [ ] `MATERIAL_PULL_CORS_ALLOWED_ORIGINS` 改为实际访问地址
- [ ] 启用自动备份并配置异地同步（见 8.4）
- [ ] 宿主机 NTP 校时（见 8.7）
- [ ] 按需启用扫码设备加固（`MATERIAL_PULL_DEVICE_REQUIRE_KEY` / `DEVICE_ALLOWED_CIDRS`）
- [ ] 接入监控并配置告警（见 8.5）
- [ ] 管理员首次登录后清空 `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD` 并重启/重建后端
- [ ] 核对全部开机自启服务已 enabled（见 5.2）

---

## 附录 A · 常用命令汇总

脚本 / systemd：

```bash
# 本地开发
cd deploy/scripts && bash start.sh        # 起前后端
bash status.sh ; bash stop.sh
# 生产
sudo systemctl restart material-pull-backend
sudo journalctl -u material-pull-backend -f
sudo nginx -t && sudo systemctl reload nginx
# 开机自启核对
systemctl is-enabled mysql nginx material-pull-backend material-pull-url-announcer material-pull-dbtool
# 端口占用
sudo lsof -i:8080 ; sudo lsof -i:80
```

Docker：

```bash
docker compose ps
docker compose logs -f backend
docker compose restart backend
docker compose up -d --build          # 改代码后重建
docker compose down                    # 停止（保留数据卷）
docker compose down -v                 # 连数据卷一起删（谨慎）
```

---

## 附录 B · 数据资产（新数据库.md）

`新数据库.md` 不是说明文档，而是一份纯数据表：约 872 行的料号映射数据（物料号、仓库代号、盒子大小、数量等），用于初始化 / 参考导入映射表。

- 导入方式：通过后台「料号映射」页导入，或由 `MaterialMappingBootstrapRunner` 首启解析。
- 相关风险见第 12 章问题 6、7、9（CSV 编码、序号列启发式判断、正则列数要求）。
- 建议：导入后抽查覆盖率，尤其是无仓库代号/数量为空、盒子大小含括号（如 `密封罐(半)`）的行；国内 Excel 导出的 CSV 请另存为 UTF-8。

---

## 附录 C · 数据库可视化工具（dbtool）

本机数据库管理工具，位于 `dbtool/`，纯 Python 标准库 + 系统 `mysql` 客户端实现（零第三方依赖）。用于查看、增删改查数据库记录。

- 访问：本机 `http://127.0.0.1:8899`，局域网 `http://<本机IP>:8899`。
- 登录口令、连接账号见 `dbtool/dbtool.env`（已加入 `.gitignore`，凭证另记于 `凭证清单-请勿上传.md`）。
- 功能：登录口令保护、29 张表中文描述、分页浏览、新增行、编辑行、勾选删除、SQL 控制台。
- systemd 服务名 `material-pull-dbtool.service`，开机自启：

```bash
sudo systemctl status|restart material-pull-dbtool
```

> 安全提示：该工具能直接查改删数据库且监听局域网，务必设强口令，仅限内网管理使用，不要对公网开放。

---

## 附录 D · 配套文档索引

根目录下几份文档各有分工，遇到问题按下表找：

| 文档 | 内容 | 什么时候看 |
|---|---|---|
| `项目文档.md`（本文） | 系统全貌、部署、架构、改造历程 | 想了解系统怎么组成、怎么部署 |
| `修复与发布记录.md` | 编译重启标准流程（第一部分）+ 已完成改动按时间倒序（第二部分） | 要发布上线，或想查某个改动的根因 |
| **`潜在问题清单.md`** | **已发现但尚未处理**的问题，按 P0/P1/P2 分级，附排查用 SQL | 接手工作、排查异常、决定下一步做什么 |
| `80端口访问技术说明.md` | Nginx 与网络链路细节 | 别的网段打不开、换 IP 后访问异常 |
| `新数据库.md` | 料号映射纯数据（见附录 B） | 初始化或参考导入映射 |

两份记录类文档的流转关系：问题在 `潜在问题清单.md` 里登记 → 处理完移入 `修复与发布记录.md`。所以**潜在问题清单只留未解决项**，看它就知道还剩什么活。









