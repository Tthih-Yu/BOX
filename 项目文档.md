# 物料拉动系统 · 项目总文档

> 本文档由根目录原有的 `README.md`、`STARTUP.md`、`DOCKER.md`、`总结.md`、`生产化改造进度.md` 整合重写而成，是项目的统一入口文档。`新数据库.md` 是料号映射的纯数据表（约 872 行导入数据），不属于说明文档，其用途见「附录 B · 数据资产」。

工厂物料拉动 / 补货配送系统。覆盖真实工厂标签、扫码拉动、任务流转、库存一致性、导入校验、AGV / 外部接口，以及斑马标签自动打印。

系统由三部分组成：

- 后端：Java 17 + Spring Boot 3.3.2（部署在 Ubuntu 服务器）
- 前端：Vue 3 + TypeScript + Vite（Web 管理端）
- 打印代理：Windows C# 托盘程序（连 USB 斑马打印机的电脑上运行，自动打印标签）

另有安卓扫码端（Kotlin + Jetpack Compose），通过 `/api/scan/*` 接口与后端交互。

> 版本说明：后端 `pom.xml` 中 `version=0.8.4`，打包产物为 `material-pull-system-backend-0.8.4.jar`。文档中出现的 V0.8.5 / V0.8.6 是「生产化改造」的功能阶段代号（见第 9 章），pom 版本号未随之升位，请以 jar 实际文件名为准。

## 文档导航

- 第 1 章　快速开始（本地开发，最省事）
- 第 2 章　环境要求
- 第 3 章　配置文件 material-pull.env
- 第 4 章　部署方式 A：脚本 / systemd（传统 Ubuntu）
- 第 5 章　部署方式 B：Docker（迁移新设备首选）
- 第 6 章　生产环境要点（MySQL / HTTPS / 集群 / 备份 / 监控 / 离线 / NTP）
- 第 7 章　斑马标签自动打印（拉模式）
- 第 8 章　技术说明（架构 / 目录 / 接口 / 安全）
- 第 9 章　生产化改造历程
- 第 10 章　已知问题与风险
- 第 11 章　日常维护速查
- 第 12 章　首次上线检查清单
- 附录 A　常用命令汇总
- 附录 B　数据资产（新数据库.md）

---

## 1. 快速开始（本地开发）

本地开发最简单：后端默认用 H2 文件数据库，不必先装 MySQL。需要两个终端。

第一个终端启动后端：

```bash
cd backend
export MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD='Admin123456'
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

第二个终端启动前端：

```bash
cd frontend
npm install
npm run dev
```

浏览器访问 `http://localhost:5173`，登录：

```text
用户名：admin
密码：Admin123456
```

首次管理员密码取自 `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD`，要求不少于 10 位且同时含字母和数字。登录成功后应删除该环境变量并重启，后续用户在「系统管理 → 用户角色」维护。

本地关键地址：

```text
前端         http://localhost:5173
后端 API     http://localhost:8080/api
Swagger      http://localhost:8080/api/swagger-ui.html   （仅 dev）
H2 控制台    http://localhost:8080/api/h2-console          （仅 dev）
```

局域网访问：前端用 `npm run dev:lan`，`hostname -I` 查本机 IP，其他电脑访问 `http://<本机IP>:5173`。防火墙如拦截，`sudo ufw allow 5173/tcp` 与 `sudo ufw allow 8080/tcp` 放行。

---

## 2. 环境要求

服务器（Ubuntu）：

- Java 17（`openjdk-17-jdk`）
- Node.js 20 + npm
- Maven（用脚本 / 源码方式构建时需要；Docker 方式不需要本机 Maven）
- 可选：MySQL 8（生产库），Redis（多实例集群时）

从零安装基础环境：

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y openjdk-17-jdk maven
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
java -version    # 期望 17
node -v          # 期望 20.x
```

打印电脑（Windows，连 USB 斑马打印机）：

- Windows 10 1803+ / Windows 11（自带 .NET Framework 4.7.2）
- 已装斑马 GT800 驱动，能在「设备和打印机」看到打印机

国内网络加速（可选）：

- Maven：在 `~/.m2/settings.xml` 配阿里云镜像 `https://maven.aliyun.com/repository/public`。
- npm：`npm config set registry https://registry.npmmirror.com`。

---

## 3. 配置文件 material-pull.env

脚本 / systemd 部署方式的所有参数集中在一个环境文件里。首次部署先复制模板：

```bash
cd deploy/scripts
cp material-pull.env.example material-pull.env
```

关键项：

| 变量 | 说明 |
| --- | --- |
| `PROJECT_ROOT` | 项目根目录，一般不用改 |
| `RUN_USER` | 运行用户，安装自启脚本会自动写入当前用户 |
| `JAVA_HOME` | Java 17 路径 |
| `SPRING_PROFILE` | `dev`（H2 文件库，开箱即用）/ `mysql`（生产库） |
| `BACKEND_JAR` | 后端 jar 名，随版本更新 |
| `MATERIAL_PULL_EXTERNAL_API_KEY` | 机器密钥：安卓扫码设备、打印代理、外部系统回调共用，**生产务必改成强随机值** |
| `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD` | 首次部署创建管理员用，成功后建议删掉 |
| `FRONTEND_MODE` | `dev:lan`（局域网可访问）/ `dev`（仅本机） |

生成 external-api-key（自己在服务器上生成的一串随机字符串，不需要去哪申请）：

```bash
openssl rand -base64 32
```

把输出填到 `MATERIAL_PULL_EXTERNAL_API_KEY=`，同一串之后要填进打印代理界面。

> Docker 部署方式用的是项目根目录的 `.env`（模板 `.env.example`），与这里的 `material-pull.env` 是两套，别混淆。见第 5 章。

---

## 4. 部署方式 A：脚本 / systemd（传统 Ubuntu）

### 4.1 脚本启动（开发 / 调试推荐）

```bash
cd deploy/scripts
bash start.sh            # 同时启动前后端
bash start.sh backend    # 只启后端
bash start.sh frontend   # 只启前端
bash status.sh           # 查看状态
bash stop.sh             # 停止
```

启动后：前端 `http://<服务器IP>:5173`，后端 `http://<服务器IP>:8080/api`。

### 4.2 systemd 开机自启（长期运行推荐）

```bash
cd deploy/scripts
sudo bash install-autostart.sh
```

脚本会自动打包后端、装前端依赖、注册并启动两个 systemd 服务。常用命令：

```bash
sudo systemctl status material-pull-backend-dev material-pull-frontend-dev
sudo journalctl -u material-pull-backend-dev -f
sudo systemctl disable --now material-pull-backend-dev material-pull-frontend-dev
```

### 4.3 手动源码打包（不走脚本时）

```bash
# 后端
cd backend
mvn clean package -DskipTests
java -jar target/material-pull-system-backend-0.8.4.jar --spring.profiles.active=dev

# 前端
cd frontend
npm install && npm run build     # 产物在 frontend/dist
npm run preview                   # 本地预览 http://localhost:4173
```

### 4.4 生产（MySQL + systemd + Nginx）手动部署概要

1. 装 MySQL、Nginx：`sudo apt install -y mysql-server nginx` 并 enable/start。
2. 建库建用户（生产替换强密码）：

```sql
CREATE DATABASE material_pull DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'material_pull'@'localhost' IDENTIFIED BY '改成强密码';
GRANT ALL PRIVILEGES ON material_pull.* TO 'material_pull'@'localhost';
FLUSH PRIVILEGES;
```

3. 首次建库导入升级 SQL：`mysql -u material_pull -p material_pull < docs/sql/V0.8.4__production_upgrade.sql`。
4. 打包 jar 放到 `/opt/material-pull-system/backend/`，软链 `app.jar`，属主给运行用户。
5. 复制 `deploy/systemd/material-pull-backend.service` 到 `/etc/systemd/system/`，在 `[Service]` 配环境变量：

```ini
Environment=SPRING_PROFILES_ACTIVE=mysql
Environment=MYSQL_USER=material_pull
Environment=MYSQL_PASSWORD=与MySQL用户一致
Environment=MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD=Admin123456
Environment=MATERIAL_PULL_EXTERNAL_API_KEY=强随机值
```

6. `sudo systemctl daemon-reload && sudo systemctl enable --now material-pull-backend`。
7. 前端 `npm run build`，把 `dist/*` 拷到 `/var/www/material-pull-system/`。
8. 复制 `deploy/nginx/material-pull-system.conf` 到 `sites-available` 并软链到 `sites-enabled`，删默认站点，`sudo nginx -t && sudo systemctl reload nginx`。访问 `http://服务器IP`。
9. 管理员首次登录成功后，从 service 文件删除 `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD`，`daemon-reload` 后重启。

---

## 5. 部署方式 B：Docker（迁移新设备首选）

项目已容器化。一台装了 Docker 的机器一条命令即可起整套系统。Docker 方式用根目录 `.env`（模板 `.env.example`），前端 nginx 会把 `/api` 反代到后端容器，前后端同源、无需额外跨域配置。

### 5.1 Ubuntu 24.04 一键部署

全新机器把整个项目目录拷过去后：

```bash
cd IE
sudo bash deploy/scripts/ubuntu-setup.sh            # H2 文件库，开箱即用
sudo bash deploy/scripts/ubuntu-setup.sh --mysql    # MySQL（生产推荐，首发自动建表）
sudo bash deploy/scripts/ubuntu-setup.sh --offline  # 工厂无外网：先在联网机 offline-images.sh save 再拷过来
```

脚本会：从 Docker 官方源装 Docker Engine + Compose 插件并把当前用户加入 docker 组；无 `.env` 则从模板生成并自动给 `MATERIAL_PULL_EXTERNAL_API_KEY` 填强随机值（权限 600）；`docker compose up -d --build` 构建启动并打印访问地址与管理员账号。

### 5.2 手动分步

```bash
cd IE
cp .env.example .env
# 至少改 MATERIAL_PULL_EXTERNAL_API_KEY 和管理员密码
docker compose up -d --build
```

访问 `http://<服务器IP>`（默认 80 端口），登录 `admin` / `.env` 里的 `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD`。端口冲突时在 `.env` 改 `HTTP_PORT=8088`。

Docker 相关文件结构：

```text
docker-compose.yml        编排（含 mysql/redis/backup 可选 profile）
.env.example              环境变量模板
backend/Dockerfile        后端多阶段构建（支持 MAVEN_MIRROR_URL）
frontend/Dockerfile       前端多阶段构建（支持 NPM_REGISTRY）
frontend/nginx.conf       前端 nginx + /api 反代（HTTP）
frontend/nginx-tls.conf   HTTPS / TLS 版
deploy/mysql/init/        MySQL 首次初始化（字符集）
deploy/scripts/           备份/恢复/自签证书/离线镜像脚本
```

### 5.3 数据持久化与迁移

数据在命名卷中，重建容器不丢：`backend-data`（H2）、`mysql-data`（MySQL）。卷名前缀 `ie_` 来自目录名 `IE`，目录名不同用 `docker volume ls` 查。

- 整库迁移：拷整个 `IE/` 目录到新机器再 `docker compose up -d --build`；业务数据用下面备份方式带走。
- H2 数据带走：
```bash
docker run --rm -v ie_backend-data:/data -v "$PWD":/backup alpine \
  tar czf /backup/backend-data.tar.gz -C /data .
```
- MySQL 数据带走：
```bash
docker exec material-pull-mysql sh -c \
  'exec mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" material_pull' > dump.sql
```

---

## 6. 生产环境要点

### 6.1 使用 MySQL

`.env`（Docker）或 systemd 环境变量里设 `SPRING_PROFILES_ACTIVE=mysql`，配好 `MYSQL_PASSWORD` / `MYSQL_ROOT_PASSWORD`（或 `MYSQL_USER` / `SPRING_DATASOURCE_URL`）。Docker 带 mysql profile 启动会额外拉起 MySQL 容器：

```bash
docker compose --profile mysql up -d --build
```

首次部署到空 MySQL 库（重要）：mysql profile 默认 `ddl-auto=validate`，要求表结构已存在。全新空库首发需让 Hibernate 先建表：

```bash
# 1. 首发临时用 update 自动建表
MATERIAL_PULL_MYSQL_DDL_AUTO=update docker compose --profile mysql up -d --build
# 2. 确认日志无错、表建好、能登录后，去掉该变量重建，回到 validate 稳态
docker compose --profile mysql up -d
```

> 为何不用完整建表脚本：29 张表若手写 DDL 易与实体漂移且离线无法验证，故采用「首发 update 建表 → 验证 → 切回 validate」策略。后续表结构变更用 `backend/src/main/resources/db/mysql/` 下的增量 SQL，在停机窗口手动执行。传统部署方式的首发升级脚本是 `docs/sql/V0.8.4__production_upgrade.sql`。

### 6.2 HTTPS（生产建议）

工厂内网也不应明文跑密码和设备密钥：

```bash
CERT_CN=material-pull.local CERT_IP=192.168.1.50 \
  sh deploy/scripts/gen-self-signed-cert.sh   # 生成 deploy/certs/server.crt 与 server.key
```

让前端容器用 TLS 配置：把 `frontend/Dockerfile` 复制的 `nginx.conf` 改成 `nginx-tls.conf` 重建，或在 compose 的 frontend 服务挂载 `nginx-tls.conf` 与 `deploy/certs` 并暴露 443。自签证书浏览器会提示不受信任，可把 `server.crt` 导入客户端/安卓设备信任，或用企业内部 CA。

### 6.3 多实例 / 高可用（集群模式）

默认单实例：后端重启会导致登录态丢失，无法多副本，单机部署可接受。需要重启不掉线、多副本负载均衡时启用集群模式（会话/业务锁/定时任务去重/WebSocket 广播全部外置到 Redis）：

```bash
# .env 中设置
MATERIAL_PULL_CLUSTER_ENABLED=true
REDIS_PASSWORD=改成强密码
# 带 redis profile 启动
docker compose --profile mysql --profile redis up -d --build
```

集群模式要求 Redis 设置密码，否则后端拒绝启动。

### 6.4 数据库自动备份

```bash
docker compose --profile mysql --profile backup up -d          # 每天一次，保留14天，存 db-backups 卷
docker exec material-pull-db-backup sh /usr/local/bin/db-backup.sh   # 手动立即备份
docker exec -e MYSQL_PASSWORD=$MYSQL_PASSWORD material-pull-db-backup \
  sh /usr/local/bin/db-restore.sh /backups/material_pull-YYYYMMDD-HHMMSS.sql.gz   # 恢复（覆盖，谨慎）
```

> 备份卷在本机磁盘，磁盘损坏仍会丢。生产务必定期把 `db-backups` 内容同步到异地/对象存储。

### 6.5 监控（Prometheus）

后端已开 Actuator，指标端点 `/api/actuator/prometheus`，健康 `/api/actuator/health`。nginx 只允许内网网段访问 `/api/actuator/`，外部拒绝。可监控 JVM 内存、HikariCP 连接池、HTTP 请求，建议至少对接内存、连接池、磁盘告警。

### 6.6 离线部署（工厂内网无外网）

联网机器打包镜像，拷到工厂导入：

```bash
# 联网机器：构建 + 导出
MAVEN_MIRROR_URL=https://maven.aliyun.com/repository/public \
NPM_REGISTRY=https://registry.npmmirror.com docker compose build
sh deploy/scripts/offline-images.sh save
# 工厂机器（拷入项目目录 + offline-images/ 后）
sh deploy/scripts/offline-images.sh load
docker compose up -d        # 无需再 build
```

### 6.7 时间同步（NTP）

系统用本机时间算任务超时、截止时间和审计时间。容器时区已设 `Asia/Shanghai`，但时钟跟随宿主机，务必保证宿主机校时：

```bash
sudo timedatectl set-timezone Asia/Shanghai
sudo timedatectl set-ntp true
timedatectl status        # 确认 System clock synchronized: yes
```

### 6.8 外部系统集成

打印机 / AGV / SAP 等对接点默认全部为空（不下发），需在真实环境联调后通过环境变量配置：

```text
MATERIAL_PULL_PRINT_SUBMIT_URL / PRINT_CANCEL_URL / PRINT_API_KEY
MATERIAL_PULL_AGV_DISPATCH_URL / AGV_CANCEL_URL / AGV_API_KEY
```

推送失败策略：`app.factory.ignore-push-failure` 默认 `true`，WebSocket 推送失败只记日志不阻断主流程（失败记入 outbox 表）。若业务要求「推送/打印失败必须感知」，评估是否改为 `false`。

---

## 7. 斑马标签自动打印（拉模式）

打印采用「拉模式」：后端生成待打印标签存库，Windows 打印代理定时领取并 RAW 发 ZPL 到 USB 打印机，全自动出标签。详见 `print-agent/README.md`。

### 7.1 打印链路

```
后台生成标签任务
      │
      ▼
后端写入 t_print_job（状态 RENDERED，含 ZPL 内容）
      │
      ▼   打印代理每隔几秒轮询
GET /api/print-jobs/next      原子领取一批，置为 SENT
      │
      ▼
RAW 发 ZPL 到 USB 打印机（不弹窗、不走驱动排版）
      │
      ▼
POST /api/print-jobs/callback  回传 PRINTED / FAILED
      │
      ▼
后台「出货标签打印 / 打印作业」显示结果
```

打印作业状态机：`CREATED → RENDERED → SENT → PRINTED / FAILED / CANCELLED`。代理领取后若崩溃/断网未回传，后端在 `print-claim-stale-seconds`（默认 120 秒）后把作业重新分配，避免标签永远打不出来。

### 7.2 后端侧配置

```yaml
app:
  factory:
    print-pull-mode: true            # 拉模式，默认已开启
    print-claim-stale-seconds: 120   # 代理领取后多久未回传视为超时可重发
```

### 7.3 打印代理构建与运行

在有 .NET SDK 或 Visual Studio 的机器上 `cd print-agent && build.bat`，得到 `dist\AutoPrintAgent.exe`，把整个 `dist` 拷到打印电脑（打印电脑不用装 SDK）。

打印电脑上：双击运行 → 填服务器地址（如 `http://<服务器IP>/api`）→ 填 API 密钥（与后端 `MATERIAL_PULL_EXTERNAL_API_KEY` 完全一致）→ 选 GT800 打印机 → 测试连接 → 保存设置 → 开始轮询。勾选「开机自动启动」「启动后自动开始轮询」即全自动，关窗口最小化到托盘。可选用 NSSM 注册成 Windows 服务。

> 关键验证：GT800 有 ZPL / EPL 两种固件。用代理「立即打印一次」测一张，能出标签即 ZPL 固件直接用；打乱码需在打印机首选项切到 ZPL，或联系改造后端生成 EPL。

### 7.4 打印代理工程结构

| 文件 | 职责 |
| --- | --- |
| `Program.cs` | 入口，单实例互斥 |
| `MainForm.cs` | 配置界面 + 系统托盘 |
| `AppConfig.cs` | 配置读写（config.json） |
| `ApiClient.cs` | 领取作业 / 回传结果，X-Api-Key 鉴权 |
| `PrintWorker.cs` | 轮询 + 打印 + 回传 + 失败重试 |
| `RawPrinterHelper.cs` | winspool RAW 直发 ZPL |
| `Logger.cs` | 本地按天日志 |

配置存 exe 同目录 `config.json`，日志存 `logs\`。

### 7.5 定时自动打印（后端侧，无需代理触发）

后端可按系统参数定时挑选未打印任务自动提交打印，参数在后台「系统参数」页改，改完下一轮生效无需重启：

| 参数 | 默认 | 说明 |
| --- | --- | --- |
| `print.auto.enabled` | false | 定时自动打印开关 |
| `print.auto.interval-minutes` | 60 | 间隔分钟，最小 1（10=每10分钟，60=每小时） |
| `print.auto.printer-name` | 空 | 斑马打印机名，如 `ZDesigner GT800 (EPL)`；空则用默认打印机 |
| `print.auto.delivery-areas` | 空 | 只打印这些配送区域，逗号分隔如 `1,2`；空=所有区域 |
| `print.auto.print-type` | WAREHOUSE_BARCODE_LABEL | 打印作业类型 |
| `print.auto.last-run` | — | 系统记录上次执行时间（不可编辑） |

也可在前端「出货标签打印」页点「立即执行定时打印」，或调 `POST /api/print-jobs/auto-print/run`（ADMIN/WAREHOUSE）。开启前务必先配好打印服务地址与打印机名，否则会按「接口未配置」报错，不会静默假成功。

---

## 8. 技术说明

### 8.1 系统做什么

面向汽车线束工厂的物料拉动（看板补货）系统。核心闭环：

1. 工位物料盒贴二维码（内容是物料号，可扩展为 `物料号,工位,使用/备用`）。
2. 现场员工用安卓 App 或 Web 扫码，后端按「料号映射表」把物料号换算成仓库代号、盒子大小、数量。
3. 仓库后台生成补货任务，打印仓库条形码标签（条码=仓库代号）。
4. 仓库员工扫条形码拣料并配送（可对接 AGV），最后收货确认闭环，空盒回收。

围绕主流程还有：A/B 双盒轮换、库存锁定/扣减、周转盒池、标签中心、扫码规则、PPC/MRP 计划、SAP/IMS 接口、大屏看板、健康检查与维护恢复、用户角色权限等模块。

### 8.2 技术栈

- 后端：Java 17、Spring Boot 3.3.2、Spring Web、Data JPA、Validation、WebSocket/STOMP、H2（文件库，默认）/MySQL（生产）、Springdoc Swagger、Apache POI、Commons CSV、ZXing、spring-security-crypto（BCrypt）、Redis（集群可选）、Actuator + Micrometer/Prometheus。
- 前端：Vue 3、TypeScript、Vite、Element Plus、Axios、Vue Router。
- 安卓：Kotlin + Jetpack Compose，原生 `HttpURLConnection` 调后端 `/scan/*`。
- 打印代理：C# / .NET Framework 4.7.2、WinForms、winspool RAW 打印。
- 部署：Nginx + systemd 或 Docker Compose，SQL 升级脚本在 `docs/`。

### 8.3 目录结构

```text
IE
├─ backend                     后端工程（Spring Boot）
│  └─ src/main/java/com/example/materialpull
│     ├─ common                通用返回、异常、请求上下文、安全/应用配置
│     ├─ security              角色注解与权限拦截器
│     ├─ config                跨域、WebSocket、Redis、首次管理员初始化
│     ├─ controller            REST API
│     ├─ dto                   请求/响应对象
│     ├─ entity                数据实体
│     ├─ enums                 业务枚举
│     ├─ repository            JPA 仓库
│     ├─ resilience            幂等、业务锁（本地/Redis）、操作保护
│     ├─ maintenance           健康检查与恢复
│     └─ service               业务服务（含 session/realtime 集群支撑）
├─ frontend                    Vue3 Web 管理端
├─ print-agent                 Windows 斑马标签自动打印代理（C#）
├─ deploy                      nginx / systemd / mysql init / 部署脚本
└─ docs                        SQL 升级脚本、导入模板、现场标签图片资源
```

### 8.4 关键接口与认证

外部 / 设备接口用 `X-Api-Key` 机器认证（对应 `MATERIAL_PULL_EXTERNAL_API_KEY`，SYSTEM 角色）：

```text
GET  /api/print-jobs/next            打印代理领取待打印作业
POST /api/print-jobs/callback        打印结果回调（PRINTED / FAILED）
POST /api/agv-jobs/callback          AGV 状态回调
/api/scan/*                          安卓扫码设备（工号+设备号，可选设备密钥/IP白名单）
/api/integrations/sap|ims|ppc/*      外部系统集成
```

请求头 `X-Api-Key: <与 MATERIAL_PULL_EXTERNAL_API_KEY 相同的值>`。

Web 管理端接口用登录会话（Bearer Token）+ 角色权限拦截：管理员、计划员、仓库员、产线员工、只读用户、系统接口。

### 8.5 安全要点

- 密码 BCrypt 存储，用户列表不返回密码字段；登录态在用户改密/改角色/禁用后失效。
- H2 Console / Swagger 默认关闭，仅 `dev` profile 开启。
- WebSocket 用 `/auth/ws-ticket` 短期一次性票据，不把长期 token 放 URL。
- 库存锁定/扣减在服务层校验，前端不能直接改；强制完成不吞库存扣减失败。
- 扫码、任务操作走业务锁 + 幂等键双重保护，防重复点击和并发。
- 外部接口未配置地址时直接拒绝，避免「假成功」。
- 非 dev 环境启动时检测默认密钥/弱口令则拒绝启动（`StartupSecurityGuard`）。
- `X-Api-Key` 等同外部接口访问权限，务必用强随机值并在网络层限制来源；打印代理与后端间建议走内网或受限出口 IP。

---

## 9. 生产化改造历程

系统从「单机演示」迁移到「真实工厂生产环境」的改造已全部完成。总体原则：所有改动默认不破坏「H2 单实例开箱即用」模式；集群/Redis 能力通过 `app.cluster.enabled` 开关启用，关闭时回退内存实现。

### 阶段一　架构性硬伤（多实例 / 高可用）

- 会话 / WebSocket 票据外置（`SessionStore` 抽象：内存 + Redis 两实现）。
- 业务锁外置（`LockProvider` 抽象：本地 ReentrantLock + Redis SET NX 分布式锁）。
- 定时任务多实例去重（基于分布式锁的租约，`lockProvider.tryExecute`）。
- WebSocket 跨实例广播（Redis pub/sub 中继，`MessageRelay` 抽象）。
- CORS 白名单外置到 `MATERIAL_PULL_CORS_ALLOWED_ORIGINS`，移除硬编码 IP。

### 阶段二　数据库与数据安全

- MySQL 初始化脚本 + 首发 ddl 策略；H2 默认 URL 去掉 `AUTO_SERVER=TRUE` 消除多写风险。
- 数据库自动备份（`db-backup.sh` / `db-restore.sh` + compose 备份容器）。

### 阶段三　网络安全

- HTTPS（nginx TLS 配置 + 自签证书脚本 + compose 443）。
- 扫码设备鉴权加固（`app.security.device-auth.*`：设备密钥 + IP 白名单，默认不强制以兼容现有设备）。
- 启动时检测默认密钥 / 弱口令则拒绝启动。

### 阶段四　运维与可观测性

- 日志持久化（`logback-spring.xml`：应用日志留 30 天、错误日志留 60 天 + 卷）。
- Actuator + Prometheus 指标。
- compose 资源限制（内存/CPU）与 json-file 日志滚动。
- NTP 时间同步说明 + 容器时区 `Asia/Shanghai`。

### 阶段五　其它现实问题

- 外部系统集成说明 + 推送失败策略提示（`ignore-push-failure`）。
- 离线镜像打包脚本 + Maven/npm 镜像构建参数。

### 阶段六　现场易用性增强（功能代号 V0.8.5）

- 仓库任务页可设置的自动定时刷新（关闭/10秒/30秒/1分钟/5分钟，接入实时消息）。
- 修复「超时未自动升级紧急」bug：根因是整批单事务 + 乐观锁冲突导致回滚。改为逐条 `REQUIRES_NEW` 独立事务 + 悲观写锁重读，单条失败只记日志、下一轮再处理。
- 仓库补货任务页排序：第一优先级「配送区域」，第二「仓库地址」。
- 料号映射新增「配送区域」字段（默认 1，可手动改），任务与标签沿用。
- 斑马打印机定时自动打标签（见 7.5）。

### 阶段七　业务一致性与生产风险修复（功能代号 V0.8.6）

- 扫码重复生成治理：补货去重覆盖全 active 状态（CREATED/ACCEPTED/PICKING/PICKED/DELIVERING/ARRIVED/EXCEPTION）。
- 收货扫码匹配收敛：只匹配 PICKED/DELIVERING/ARRIVED；多任务命中时要求扫任务号或更精确标签，避免误关单。
- 工位二维码解析修复：分隔符移除 `/`，避免带 `/` 的工位/地址被错拆。
- 料号映射仓库物料号修复：新增 `warehouseMaterialCode`，保存与扫码优先用仓库物料号，缺省回退仓库代号。
- 任务完成流程收紧：普通 `complete` 只允许 PICKED/DELIVERING/ARRIVED 且必须锁定扣减库存，不能从 CREATED/ACCEPTED/PICKING 跳过库存和盒子校验。
- 异常/重试/删除/空盒回收一致性：异常时释放库存锁/周转箱/打印任务/AGV；重试按已发生节点恢复；空盒回收仅终态/异常态且防重复派发；删除禁止 COMPLETED/DELIVERING/ARRIVED。
- AGV 集成容错与幂等：URL 未配置时跳过并让主流程继续；手动派发禁止终态任务并复用未完成作业。
- Docker MySQL Driver 错配治理：支持 `MATERIAL_PULL_DATASOURCE_DRIVER` 覆盖。

> 验证状态：前端 `npm run build` 通过；后端 `mvn clean package` 在联网机器上 BUILD SUCCESS（约 160 源文件，redis/actuator/prometheus 依赖正常打入 jar）。开发沙箱因无外网无法下载新依赖，编译验证需在联网机器执行。

---

## 10. 已知问题与风险

以下来自一次代码通读（静态审查，未逐条实跑）。部分是产品有意的设计取舍，修改前建议结合现场实际使用方式确认。

### 严重

1. **安卓终端 `/scan/*` 弱鉴权**：`AuthTokenFilter` 中 `/scan/` 开头请求带任意 `X-Employee-No` / `X-Device-No` 即放行并绑 `LINE` 角色。这是「安卓免登录」产品决策，但内网任意设备可伪造扫码。阶段三已提供可选加固（`MATERIAL_PULL_DEVICE_REQUIRE_KEY` 设备密钥 + `DEVICE_ALLOWED_CIDRS` IP 白名单），生产应启用。
2. **集群/多实例下内存态失效**：会话、业务锁、ws 票据默认是内存 `ConcurrentHashMap`，多实例或重启会丢登录态、锁不跨实例。单实例无碍；多实例须开集群模式走 Redis（阶段一已支持）。
3. **`scanReceive`/`scanException` 幂等不足**：未像 `scanEmpty` 那样走 `idempotencyService.begin/finish`，`receiveBySiteScan` 把 `requestId` 设为每次都不同的 traceId，网络重试可能重复触发收货后的空盒 AGV 派发。（阶段七的收货匹配收敛已缓解部分风险，仍建议补真正幂等包装。）

### 中等

4. `doScanEmpty` 中 `status != IN_USE && status == FULL_STANDBY` 前半段冗余，等价于 `status == FULL_STANDBY`，意图含糊，建议简化。
5. `deleteTask` 后已扣减任务的盒子状态不回滚，属设计取舍，建议补注释说明。
6. `ImportService.csvToWorkbook` 固定 UTF-8，国内 Excel 另存的 GBK CSV 会乱码，建议探测编码或文档强制 UTF-8 模板。
7. 料号映射导入「是否有序号列」靠 `isInteger` 启发式判断，物料号纯数字时易误判，建议按表头显式区分列。
8. 映射唯一约束只在「启用」时校验，建议确认 SQL 升级脚本对 `warehouseCode` 是否有无条件唯一索引。
9. `MaterialMappingBootstrapRunner` 正则要求 5 列齐全，缺列/特殊盒型行会被跳过，导入覆盖率应抽查。
10. 前端 `canTaskAction` 对 ADMIN 的 `forceComplete` 判断绕口，前后端行为一致但建议显式列权限。

### 轻微

11. 安卓 `MaterialPullApi` 同时发 `Authorization` 和 `X-Api-Key: token`，语义混乱，建议删掉 `X-Api-Key` 那行。
12. 前端 `idempotencyKey` 按 5 秒时间桶生成，边界两侧会得到不同 key，建议由调用方传稳定业务键。
13. `TimeoutScheduler` 的 `count++ > batchSize` 会多处理一条且未真正分页，大数据量下建议仓库分页查询。（阶段六已重写超时处理逻辑为逐条独立事务，此项主要指批量查询方式。）
14. H2 dev 默认库，生产已明确建议 MySQL。
15. CORS `allowCredentials(false)` 与前端 Bearer token 一致，无问题。

处理优先级建议：`/scan/*` 加固（1）→ 收货幂等（3）→ `doScanEmpty` 简化（4）→ CSV 编码/映射列判断（6、7）→ 多实例迁 Redis（2）。

---

## 11. 日常维护速查

| 场景 | 操作 |
| --- | --- |
| 更新后端代码（脚本/systemd） | `bash start.sh backend` 或 `sudo systemctl restart material-pull-backend-dev`（会重新打包 jar） |
| 更新后端代码（Docker） | `docker compose up -d --build backend` |
| 更新前端代码 | 生产 `npm run build`；开发热更新自动生效；Docker `docker compose up -d --build frontend` |
| 改了 env 配置 | 重启对应服务生效 |
| 换了 API 密钥 | 后端 env 和打印代理界面同步改成同一串，各自重启 |
| 查后端日志（脚本/systemd） | `tail -f logs/backend.log` 或 `journalctl -u material-pull-backend-dev -f` |
| 查后端日志（Docker） | `docker compose logs -f backend`；历史文件在 `ie_backend-logs` 卷 |
| 查打印代理日志 | 打印电脑 `print-agent\dist\logs\agent-*.log` |
| 标签打不出来 | 查代理是否运行 / 密钥是否一致 / GT800 是否 ZPL 固件 / 是否缺纸 |

---

## 12. 首次上线检查清单

- [ ] `MATERIAL_PULL_EXTERNAL_API_KEY` 改为 ≥16 位强随机值（默认值会被后端拒绝启动）
- [ ] MySQL / Redis 密码改掉模板默认值
- [ ] 用 MySQL 而非 H2，并完成首发建表流程（见 6.1）
- [ ] 启用 HTTPS（见 6.2）
- [ ] `MATERIAL_PULL_CORS_ALLOWED_ORIGINS` 改为实际访问地址
- [ ] 启用自动备份并配置异地同步（见 6.4）
- [ ] 宿主机 NTP 校时（见 6.7）
- [ ] 按需启用扫码设备加固（`MATERIAL_PULL_DEVICE_REQUIRE_KEY` / `DEVICE_ALLOWED_CIDRS`）
- [ ] 接入监控并配置告警（见 6.5）
- [ ] 管理员首次登录后清空 `MATERIAL_PULL_BOOTSTRAP_ADMIN_PASSWORD` 并重启/重建后端

---

## 附录 A · 常用命令汇总

脚本 / systemd：

```bash
# 本地开发
cd deploy/scripts && bash start.sh        # 起前后端
bash status.sh ; bash stop.sh
# 生产
sudo systemctl restart material-pull-backend
sudo journalctl -u material-pull-backend -f
sudo nginx -t && sudo systemctl reload nginx
# 端口占用
sudo lsof -i:8080 ; sudo lsof -i:5173 ; sudo lsof -i:80
```

Docker：

```bash
docker compose ps
docker compose logs -f backend
docker compose restart backend
docker compose up -d --build          # 改代码后重建
docker compose down                    # 停止（保留数据卷）
docker compose down -v                 # 连数据卷一起删（谨慎）
```

---

## 附录 B · 数据资产（新数据库.md）

`新数据库.md` 不是说明文档，而是一份纯数据表：约 872 行的料号映射数据（物料号、仓库代号、盒子大小、数量等），用于初始化 / 参考导入映射表。

- 导入方式：通过后台「料号映射」页导入，或由 `MaterialMappingBootstrapRunner` 首启解析。
- 相关风险见第 10 章问题 6、7、9（CSV 编码、序号列启发式判断、正则列数要求）。
- 建议：导入后抽查覆盖率，尤其是无仓库代号/数量为空、盒子大小含括号（如 `密封罐(半)`）的行；国内 Excel 导出的 CSV 请另存为 UTF-8。










